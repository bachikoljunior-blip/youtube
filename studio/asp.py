"""**成果報酬のリンク（ASP）を、出す本の説明欄に入れる一点**（**API 0単位**）。

**2026-09-18 20:3x・optimizer・1周 1体 が書いた**（JOURNAL 同刻）。

**なぜ在るか（固定2 の答えの、ただ 1本 の道）**:
期限（12/13）までに 月20万 に届く道は、測った範囲で **門（YPP）の外の 2つ だけ**です
（広告は登録 37人 → 1,000人 が要り、登録/日 の中央 1.6人 では約600日 ＝ 期限の外）。
その 2つ のうち **企業案件は相手の yes が要り**、**成果報酬だけが こちらの台本と説明欄で動きます**
（`docs/FOR_OWNER.md` §3・`studio/trend.perf_need_rate`）。
**2026-09-18 15:4x の訊き `asp_link` は 20:2x に返りました**（`data/studio/asp_links.json`）。
**それまで、こちらの側の率は構造的に 0 でした** —— 説明欄の行き先は 国税庁・区役所・年金機構 だけで、
**こちらの取り分になるリンクが 1本 もなかった** ＝ 再生が何回 来ても分子は 0 のまま。
**この file が、その 0 を 0 でなくします。**

**門は 1か所 です**: `compose()` だけが説明欄に足します。撃つ所は 2つ（`yt.upload` と `yt.update_meta`）で、
**どちらも API の口そのもの** ＝ 「足し忘れた本」が構造として出ません。
比べる側（`cli.drift_fields` / `cli.desc_appended`）も同じ `compose()` を通した字と比べます
（通さないと、上げた本が毎周「食い違い」に見えて 50単位 の直しが空撃ちされます）。

**置き場所は 説明欄の いちばん上**（2026-09-18 20:3x の決め）。理由: スマホの説明欄は
**「…もっと見る」より上の 2行 しか出ません**。下に置くと、開いた人にしか届かない。
**覆る条件（この決めが覆る数）**:
 (1) 上に置いた本の 24h 中央が、置いていない本（09/18 の年金連作 5本・中央 369回）の **半分 未満**なら、
     題材の差ではなく**この塊が配りを削っている** ＝ 下（末尾）へ移すこと。`compose(top=False)` で足りる。
 (2) **成果が 30日 で 0件 なら**、リンクの置き場所ではなく**当てている案件**を疑うこと
     （いまは FP・保険の無料相談 2本。楽天は入れていない —— 成果 2%（購入完了）は
      1件あたり 数十円で、月20万 の分子に数えられる大きさではないため。
      数えられるのは `hoken_total_professional` ¥13,800 と `fp_no_madoguchi` ¥5,000 の 2本 だけで、
      **月20万 ＝ ¥13,800 なら 15件/月・¥5,000 なら 40件/月**）。
 (3) **`youtube_ok` が false の案件が来たら**、その案件は `OFFERS` に入れないこと（規約違反は口ごと失います）。

**表示の決まり（景表法・ステマ規制 2023-10〜）**: 広告であることを、平の人が分かる字で、
リンクと同じ塊に書くこと。**`【PR】` と「アフィリエイト」の 2語 を必ず出します**（検査 `tests/test_studio_asp.py`）。
"""
from __future__ import annotations

import json
from pathlib import Path

from .common import BRAND_NAME

ROOT = Path(__file__).resolve().parents[1]
LINKS = ROOT / "data/studio/asp_links.json"

#: この塊が在るかを見分ける印（**足し直しを 1度 に保つ**）。字を変えると、
#: 既に上げた本が「まだ入っていない」側に見えます —— 変えるときは retrofit を撃ち直すこと。
MARK = "【PR】"
#: リンクの見分け（印の字を変えた回でも、二重に足さないための 2つ目の目）。
HOST = "af.moshimo.com"

#: 説明欄に出す案件（**順は 1件あたりの額の高い順** ＝ 上の行ほど押される）。
#: `data/studio/asp_links.json` の `id` で引く。**ここに無い id は出しません**（上の覆る条件 (2)）。
OFFERS = [
    ("hoken_total_professional", "老後のお金と保険を、専門家（FP）に無料で相談できます"),
    ("fp_no_madoguchi", "保険を売らない中立のFPに相談したい方はこちら（無料）"),
]

# ---------------------------------------------------------------------------
# **声と画面が指す先は「プロフィールのリンク」**（2026-09-19 08:xx・optimizer・Fable 5.1・ultracode）
# ---------------------------------------------------------------------------
#
# **なぜ**（推測ではなく YouTube の決まり・2026-09-19 07:5x に読んだ）:
#   YouTube は **2023-08-31 から Shorts の説明欄とコメント欄の URL を押せなくしています**
#   （固定コメントも同じ）—— `support.google.com/youtube/answer/13748639`
#   「URLs placed in YouTube Shorts comments and Shorts descriptions are non-clickable」。
#   このチャンネルの配りの **93.9% は Shorts のフィード**（`trend`）＝ 09/18 20:3x（説明欄）・22:1x（コメント欄）に
#   置いた 2つ の面は、**ほぼ全員に「押せない字」**でした。声が「コメント欄の無料相談から」と言っても、
#   行った先の URL は押せません（＝ 分子は構造的に 0 のまま。09/18 の 5本・約3,800回 はこの状態で配り終えた）。
#
# **Shorts の画面から押せる面は 1つ**: **チャンネルページのリンク**（登録ボタンの横・最大 14本・
#   スマホでも押せる・同じ help の「Channel profile links」）。**API に口が無い**（`channels.update` の
#   `brandingSettings` にリンクの欄は無い）＝ **置くのはオーナーの手**（`docs/FOR_OWNER.md` 09/19 09:00 の窓【2】）。
#   置かれたかは公開ページで 0単位 で読めます（`pubcheck.channel_public` の `asp_link`）。
#
# **だから声と画面は、URL の在る面ではなく「押せる面」を指します**: チャンネル名をタップ → リンク。
#   説明欄・コメント欄の塊は残します（長尺と PC の watch ページでは押せる・広告の表示はここに要る）が、
#   **塊の 2行目 で「押せない・押せる面はこちら」と言います**（`PROFILE_NOTE`）。
#
# **覆る条件**:
#  (1) YouTube が Shorts の説明欄／コメント欄の URL をまた押せるようにしたら（help の字が変わったら）、
#      `CTA_SEG` を「説明欄」へ戻してよい（`PROFILE_NOTE` は嘘になるので先に消すこと）。
#  (2) オーナーがリンクを置いて 7日、ASP のクリックが 0 のままなら、面ではなく**声の文**を疑うこと
#      （「チャンネル名をタップ」が伝わっていない側）。**先に疑う語は「プロフ」** —— 相手は 65歳以上 が 66%・
#      55歳以上 が 95%（`analytics.audience` 2026-09-19 10:4x）で、略語は通じない側。差し替えの候補は
#      「チャンネルのページ」。**ASP の 1枚 が来る前に替えないこと**（METHOD §5 09/19 10:4x 決め (2)）。
#      **【2026-09-19 11:xx】1枚目 が来ました: クリック 11件（09/01〜09/19・分母 約2,100回 ＝ 0.52%・台帳 `asp_report`）＝ 0 ではない**
#      ので、この条件は引かれず、声の文（「プロフ」）は**触りません**。次に読むのは 2枚目（リンク欄・説明欄が在る期間）。
#      **2枚目 で率が 1枚目 の半分 未満なら**、面が増えたのに落ちた ＝ 1枚目 にオーナーの確かめ押しが混じっていた側を疑う。
#  (3) `pubcheck` の `asp_link` が 09/22 16:00 を越えて `False` のままなら、窓【2】の頼み方を疑うこと
#      （2度 空振り ＝ 刻を疑う・JOURNAL 09/19 07:0x）。
#: 「プロフィールのリンク」＝ チャンネルページの **2つ の面** のどちらでもよい（09/19 09:xx に 2つ目 を足した）:
#:   リンク欄（オーナーの手・窓【2】）と **チャンネルの説明欄**（`channel_description()`・`channels.update` 51単位・機械の手）。
#:   声（`CTA_SEG`「プロフのリンクから」）と この字の「リンク」は、どちらの面も指せる。
#:   **この字を変えないこと** —— 上がっている本の説明欄はこの字で組まれていて、変えると `drift_fields` が
#:   「説明欄 食い違い」を毎周 印字し、直しに行く周が 50単位/本 を払う（2026-09-19 09:xx に 1度 変えかけて戻した）。
PROFILE_NOTE = "スマホのショートではこのURLは押せません。チャンネルのアイコンをタップ → 「リンク」から開けます。"

#: コメント欄の塊の 2行目（`comment_block()` だけが使う。説明欄の `PROFILE_NOTE` とは別の字）。
#: **なぜ別か**（2026-09-19 10:xx・optimizer）: 「リンク」欄はオーナーの手（窓【2】）で、置かれたかは 16:00 の `status` まで
#: 分からない。機械が置ける面は **説明欄（概要）** で、スマホのチャンネルページではそれは「…もっと見る」の中に出る
#: （リンク欄も同じ画面に出る）。「「リンク」から」だけだと、リンク欄が無い日にタップした人は「リンク」を探して見つけられない。
#: 「もっと見る」は **どちらの状態でも当たる字**。コメントはまだ 1本 も置いていない（`cta_comment` 台帳の 09/18 分は
#: 旧文で、置き直さない ＝ 検査 (7)）ので、この字は `drift_fields` に掛からない（説明欄は掛かるので `PROFILE_NOTE` は触らない）。
#: **覆る条件**: `pubcheck` の `asp_link`（リンク欄）が 3日 続けて True なら「もっと見る」を「リンク」に戻してよい（短い方が読まれる）。
COMMENT_NOTE = "スマホのショートではこのURLは押せません。チャンネルのアイコンをタップ → 「…もっと見る」を開くと、押せるリンクがあります。"

#: 最後のコマ（**ショート用**）。**`say` は 41字 まで** —— 在庫の本の字数の蓋（`lint` の「合計 N字まで」＝ その本の
#: 実測の字/秒 × 95秒 × 0.97）に、前の出口（42字）が既に当たっていた本が在る（`2026-09-21-kuriage-63sai-short` 448/447）。
#: 出口を長くすると本文を削ることになり、それは輪（§4 (1)）を開け直す ＝ **出口の 1コマ だけを替える**のがこの決め。
#: 「名」は読みが割れる（な／めい）ので使わない（`lint` の「読みが固定されていない」）。「プロフ」は声だけ・画面は「プロフィール」。
#: `hear` で割れる助数詞を入れない。長尺は説明欄の URL が押せるので、長尺の型はこの限りではない。
#: 「リンクへ」は `hear` が助詞の「へ」を「え」と割り（コマ2 と重なって `near_repeats` が鳴った・2026-09-19 07:4x）、「から」にした。
CTA_SEG = dict(
    say=f"{BRAND_NAME}でした。相談はアイコンをタップ、プロフのリンクから。広告です。",
    show="相談は\nプロフィール\nのリンク",
    sub="無料相談は チャンネルのアイコンをタップして プロフィールのリンクから 広告のリンクです",
    tag="", board=[BRAND_NAME, "無料相談は？", "アイコンをタップ", "プロフィールのリンク（広告）"])

DISCLOSURE = ("※上の2つは広告（アフィリエイト）です。相談は無料で、申し込みがあると"
              "このチャンネルに紹介料が入ります。動画の数字は公的な資料だけで計算していて、"
              "紹介料とは関係ありません。")


def links() -> dict[str, dict]:
    """`data/studio/asp_links.json` の `id` → 行。**無ければ空**（この file は止めません）。"""
    try:
        raw = json.loads(LINKS.read_text(encoding="utf-8"))
    except (OSError, ValueError):
        return {}
    return {r["id"]: r for r in raw.get("links", []) if r.get("url")}


def offers() -> list[tuple[str, str]]:
    """出せる案件（`OFFERS` の順・`youtube_ok` が false の物は落とす）→ [(url, 一行)]。"""
    have = links()
    out = []
    for oid, line in OFFERS:
        r = have.get(oid)
        if not r or r.get("youtube_ok") is False:
            continue
        out.append((r["url"], line))
    return out


# ---------------------------------------------------------------------------
# **案件の扉（否認条件）が、この相手に開いているか**（2026-09-20 01:4x・optimizer・Opus 5・ultracode）
# ---------------------------------------------------------------------------
#
# **なぜ在るか**: 上の 覆る条件 (2) は「**成果が 30日 で 0件 なら、置き場所ではなく当てている案件を疑うこと**」
# でした。この回は **30日 を待たずに、案件の否認条件を読んで** その (2) を引きました
# （公開の字・**API 0単位**・費用 0・オーナーの手も要りませんでした）。
#
# **読んだもの（原文のまま・出どころ付き）**:
#
#   `hoken_total_professional`（¥13,800 ＝ いまの分子の 9割）
#     成果承認条件: 「WEBより無料相談申込後、14営業日以内の相談日確定、**60日以内に2度目の面談実施完了後**」
#     否認条件:     「学生、独身でパート・アルバイト、**無職**などの**年収が300万円未満**で
#                    保険加入・ライフプラン設計ができない方」
#                   「**会社員、公務員、経営者、専門職以外の職業**で収入が安定しない、将来設計が難しい職業の場合」
#                   「入院中・治療中・病歴・職業などの条件によりサービスの提供が出来ない場合」
#                   「未成年(20歳未満)、その他意思能力、判断能力のない方」
#     出どころ: もしもアフィリエイトの案件詳細の写し（note.com/lux_sh/n/n58c42dd58b06・2026-09-20 に読んだ）
#
#   同じ棚の別の案件（`みんなの生命保険アドバイザー`・比べるために読んだ）
#     否認条件: 「**20歳未満、60歳以上の方**」「**無職**・学生の方」「世帯年収100万円未満の方」
#     出どころ: media-analytics.jp/affisearch/promotions/minnano-seimei-hoken-adviser
#     ＝ **この棚には「60歳以上」を名指しで落とす案件が実在します**（うちの案件にその行は在りませんでした）。
#
# **この相手**: **65歳以上 が 66%・55歳以上 が 95%**（`analytics.audience` 09/19 10:4x・実測）。
# 定年を過ぎた年金生活者は **無職**であり、**会社員・公務員・経営者・専門職のどれでもありません**。
# 年金の額は、このチャンネルが毎日 扱っている数そのもの ＝ **月10万〜25万（年120万〜300万）**。
#
#   ＝ **否認条件の 1行目 と 2行目 が、相手の 3分の2 を名指しで落とします。**
#
# **これは「成約率が低い」ではありません。「扉が閉まっている」です。**
# 09/20 00:3x の周（訊き `asp_terms`）は「**外からは見分けがつきません**」と書きましたが、
# **見分けられました** —— 訊く先が オーナーの画面ではなく **案件詳細の公開の写し** だった、というだけです。
# **その周が疑った「年齢の上限」は、うちの案件には在りませんでした**（在ったのは 無職・年収・職業 の門）。
# **＝ 仮説の向きは当たっていて、名指しした仕組みが外れていた。**
#
# **何が覆ったか**: `trend.perf_short` の「相場の中段に対して 1.0倍」は、
# **押した人が受け付けてもらえる**前提の数でした。その前提が、いまの 2案件 では成り立ちません。
# 前の周の「**40件 まで押し続ける**」は、**この 2案件 のままなら 40件 積んでも 0件** です。
#
# **覆る条件**:
#  (1) **オーナーの画面（もしもアフィリエイトの案件詳細）の字が上の写しと違っていたら、画面のほうが本当です**
#      —— 上は第三者の写しで、一次は提携中の管理画面にしかありません（訊き `asp_terms`）。
#  (2) `fp_no_madoguchi`（¥5,000）の否認条件は **まだ読めていません**（公開の写しが見つからなかった）。
#      読めたら同じ形でここに足すこと。「同じ棚だから同じ条件だろう」は **推測です** ——
#      **数にしていないので、`CLOSED_TO_RETIREES` には入れていません。**
#  (3) **成果が 1件 でも承認されたら、この段は外れます**（扉は開いていた）＝ `asp_report` の `actions`。
#  (4) 相手の `p65` が **50% を下回ったら** この段は出しません（`DOOR_P65_GATE`・`trend.audience_split`）。
#
# **扉が開いている棚**（同じ相手・同じ話題の隣・2026-09-20 に読んだ・**まだ提携していません**）:
#   **もしもアフィリエイトの中**（＝ オーナーがすでに持っている口座・新しい ASP の登録が要らない側）:
#     **小さなお墓KOBO**   成果 **¥5,000**  成果条件「申し込み完了」
#     **墓じまい 資料請求** 成果 **¥1,000**  成果条件「資料請求獲得」
#   **別の ASP（口座が要る ＝ オーナーの手が増える側・いまは採らない）**:
#     家墓（レントラックス） 成果 ¥13,200「新規購入後の決済完了」
#     お墓さがし（マネートラック） リード ¥1,000・承認は「資料請求フォーム申込完了後、30日以内に電話での本人確認完了」
#   出どころ: media-analytics.jp/affisearch/categories/haka（2026-09-20 に読んだ）
#
#   **どれも 年収・職業・年齢 の否認条件がありません**（＝「年金の手取り」と同じ相手に扉が開いている）。
#   **¥5,000 なら 月40件 で 20万円** ＝ いまの保険案件（¥13,800 で 月15件）と**同じ桁の分母**
#   ＝ **`PERF_YEN_PER_ACTION_BAND` の中段も倍率の計算もやり直さなくてよく、
#      変わるのは「扉が開いているか」だけです。**
#   **売れるとは言っていません**（`perf_need_rate` の註と同じ向き）。
#   **覆る条件 (5)**: お墓・終活は「年金の手取り」から話題が 1つ 遠い（相続のほうが近い）。
#     提携が通ったあと押される率が 0.52%（`asp_report`）を割ったら、**案件ではなく話題の距離**を疑うこと。
#     **まだ測っていません。**

#: 否認条件が「無職・年収300万円未満・会社員/公務員/経営者/専門職 以外」を落とす案件 ＝
#: **この相手（65歳以上 66%）には扉が閉まっている側**（上の註・覆る条件 (1)(2)(3)）。
#: **読めた物だけ入れること** —— 読めていない案件を「同じ棚だから」で入れない（覆る条件 (2)）。
CLOSED_TO_RETIREES = ("hoken_total_professional",)

#: 扉の段を出し始める `p65`（%）。下回ったら出しません（覆る条件 (4)）。
DOOR_P65_GATE = 50.0


def closed_offers(p65: "float | None" = None) -> list[str]:
    """いま出している案件のうち、**この相手に扉が閉まっている**物の id。

    `p65` が `None` か `DOOR_P65_GATE` 未満なら **空**
    （＝ 相手が老いている側だと測れていないなら、言わない）。
    **決めと出どころは上の註 ＝ ここへ写さないこと。**
    """
    if p65 is None or float(p65) < DOOR_P65_GATE:
        return []
    have = links()
    return [oid for oid, _ in OFFERS if oid in CLOSED_TO_RETIREES and have.get(oid)]


def door_short(p65: "float | None" = None) -> str:
    """`status`／`trend` に足す 1行（`trend.perf_short` の尻）。**空なら足しません。**

    **決めと覆る条件は上の註 ＝ ここへ決めを書かないこと。**
    """
    shut = closed_offers(p65)
    if not shut:
        return ""
    return ("。**⚠ ただし、その成約率をかける先の扉が閉まっています** —— "
            f"いま出している案件 {len(offers())}本 のうち **{len(shut)}本**"
            f"（`{'`・`'.join(shut)}`・**分子の 9割**）の否認条件は "
            "「学生、独身でパート・アルバイト、**無職**などの**年収が300万円未満**で"
            "保険加入・ライフプラン設計ができない方」"
            "「**会社員、公務員、経営者、専門職以外の職業**で収入が安定しない、将来設計が難しい職業の場合」。"
            f"相手は **65歳以上 が {float(p65):.0f}%**（定年後 ＝ **無職**・年収は年金の額 ＝ 月10万〜25万）"
            " ＝ **クリックを 40件 積んでも成果は構造的に 0**。"
            "**『成約率が低い』ではなく『受け付けてもらえる相手が画面の向こうにほとんどいない』です。** "
            "**直す手は率ではなく案件の差し替え** —— お墓・終活の棚は 無職/年収/年齢 の門が無く、"
            "**もしもの中に 小さなお墓KOBO ¥5,000（申し込み完了）・墓じまい資料請求 ¥1,000（資料請求）が在ります**"
            "（¥5,000 なら 月40件 で 20万 ＝ いまと同じ桁の分母）＝ **倍率の計算は変わりません**"
            "（原文・出どころ・覆る条件は `studio/asp.py`「案件の扉」の註・**API 0単位**）")


def block() -> str:
    """説明欄に入れる塊。**案件が 1本 も無ければ空**（＝ `compose` は素通し）。"""
    got = offers()
    if not got:
        return ""
    head = f"{MARK}この動画は一般的な例です。自分の場合いくらになるかを相談したい方へ\n{PROFILE_NOTE}"
    body = "\n".join(f"・{line}\n{url}" for url, line in got)
    return f"{head}\n{body}\n{DISCLOSURE}"


def has_block(desc: str | None) -> bool:
    """もう入っているか（印か、リンクの host のどちらかが在れば入っている）。"""
    d = desc or ""
    return MARK in d or HOST in d


#: YouTube の説明欄の上限（字）。**越えると `videos.insert` / `videos.update` が 400 で落ちます**
#: ＝ **落ちるのは塊ではなく、その本の予約そのもの**（1,650単位 が無駄になる）。
#: いまいちばん長い台本は 3,459字（`2026-09-18-minou-ushinau-3tsu`）＋ 塊 345字 ＝ 3,804字 で、
#: **余りは 1,200字** あります。この蓋は「まだ踏んでいない所に置いた門」です（`docs/GOAL.md` の教訓 1つ目）。
LIMIT = 5000


def compose(desc: str | None, top: bool = True) -> str:
    """説明欄に成果報酬の塊を入れた字を返す。**冪等**（2度 呼んでも 1つ）。

    `top=True`（既定）＝ いちばん上（スマホで「もっと見る」より上に出る側）。
    **上限を越えるときは、塊ではなく台本の側の尻を行の切れ目で落とします** ——
    塊を落とすと、その本だけ分子が 0 になるため（落とした時は末尾に `…`）。
    """
    d = (desc or "").strip()
    b = block()
    if not b or has_block(d):
        return d[:LIMIT]
    if not d:
        return b
    room = LIMIT - len(b) - 2
    if len(d) > room:
        cut = d[:max(0, room - 1)]
        nl = cut.rfind("\n")
        d = (cut[:nl] if nl > room // 2 else cut).rstrip() + "…"
    return f"{b}\n\n{d}" if top else f"{d}\n\n{b}"


# ---------------------------------------------------------------------------
# **コメント欄の側**（2026-09-18 22:xx・optimizer・Opus 5・ultracode が足した）
# ---------------------------------------------------------------------------
#
# **なぜ足したか（固定2 の答えの、その 1本 の道の「詰まっている所」）**:
# 20:3x の回が説明欄に塊を置いて、分子は 0 でなくなりました。**ただし置いた面が合っていません** ——
# `trend` の同じ周の実測で **配りの 93.9% が Shorts のフィード**（`analytics` の `ショートのフィード`）。
# Shorts のフィードでは、説明欄は**題を叩いて開いた人にしか出ません**。
# 一方 **コメントは、画面に件数つきのボタンが出ていて 1タップ**です。
# ＝ いまの置き場所は「開く人がいちばん少ない面」で、こちらの取り分になる唯一の口を回しています。
#
# **値段の差がこの手の値打ちです**: 1本 出すのに **1,650単位**、この塊を 1本 に足すのは **50単位**（1/33）。
# 日枠が戻って 5本/日 出しても 250単位 ＝ 測る側（600）にも 5本目 にも当たりません。
#
# **旧 `scripts/post_pending_comments.py`（§8 で止めた自動投稿）とは別物です。**
# 止めた理由は「全本に同じ1文を機械で置く」形そのものではなく、
#   (1) 中身が「この計算は毎日1本ずつ出しています」＝ **分子にならない自己紹介**だったこと、
#   (2) `run_marker` / `ahead_sweep` と同じ **背景の kick から毎時 勝手に走っていた**こと
#       （METHOD §「起こす口を全部 外すこと」）。
# こちらは **(1) 分子そのもの**（門の外の唯一の腕）で、**(2) 背景から呼ぶ口を作りません** ——
# `cli cta` を人（立ったサブ）が撃つときだけ動き、1本ごとに台帳 `cta_comment` が残ります。
#
# **撃てるのは公開ずみの本だけです**（private／予約中の本にはコメントを付けられない ＝ `upload` の口には置けない）。
#
# **覆る条件**:
#  (1) 塊を置いた本の 24h の再生の中位が、置いていない本の **半分 未満**なら、コメントが配りを
#      削っている（コメント欄の1番上が広告だと滞在が落ちる）＝ この口を畳んで説明欄だけに戻すこと。
#  (2) 置いた本のコメントが **`comments` の「消えた」に立った**ら、YouTube 側がリンクを落としています
#      ＝ 口ごと失う手前なので、リンクを外して「コメント欄の固定」ではなく説明欄へ誘う字に替えること。
#  (3) **30日 で成果 0件** なら、面ではなく**当てている案件**を疑うこと（`block()` の覆る条件 (2) と同じ 1か所）。
#  (4) この環境の分類器に止められたら **迂回しないこと**（METHOD §11 15:0x の `reply` と同じ口）——
#      文を `docs/FOR_OWNER.md` に置いて、オーナーが YouTube Studio で貼れる形にする。
#
# **固定にはできない所（オーナーの手）**: **API には「固定（ピン留め）」の口がありません。**
# 付けた直後は件数が 0〜2件 なので「上位のコメント」の先頭に出ますが、
# 伸びた本では下がります。**ピン留めはオーナーが Studio で 1タップ**（`docs/FOR_OWNER.md`）。

#: コメントの上限（YouTube の決め・字）。**越えると `commentThreads.insert` が 400 で落ちます**。
COMMENT_LIMIT = 9000


def comment_block() -> str:
    """コメント欄に置く塊。**案件が 1本 も無ければ空**（＝ `cli cta` は撃たない）。

    説明欄の塊（`block()`）より短くします —— コメントは 4行 ほどで畳まれ、
    「続きを読む」の下は説明欄と同じく開く人しか読みません。
    **`【PR】` と「アフィリエイト」の 2語 は、説明欄と同じく必ず 1行目 の塊に出します**
    （景表法・ステマ規制。検査 `tests/test_studio_asp.py`）。
    """
    got = offers()
    if not got:
        return ""
    head = (f"{MARK}動画の数字は一般的な例です。"
            "「自分の場合はいくら？」は、下から無料で相談できます（アフィリエイトです）。\n"
            f"{COMMENT_NOTE}")
    body = "\n".join(f"・{line}\n{url}" for url, line in got)
    tail = "※申し込みがあるとこのチャンネルに紹介料が入ります。動画の計算は公的な資料だけで出していて、紹介料とは関係ありません。"
    return f"{head}\n{body}\n{tail}"[:COMMENT_LIMIT]


# ---------------------------------------------------------------------------
# **チャンネルの説明欄（概要）**（2026-09-19 09:xx・optimizer・Fable 5.1・ultracode が足した）
# ---------------------------------------------------------------------------
#
# **なぜ足したか（固定2 の答えの、その 1本 の道の「押せる面」を、機械の手で 1つ 作る）**:
# 07:2x〜08:xx の周が「Shorts から押せる面はチャンネルページのリンクだけ・API に口が無い ＝ オーナーの手（窓【2】）」
# と書きました。**半分だけ正しい。** チャンネルページには URL の載る面が **2つ** あって、
#   (a) リンク欄（登録ボタンの横・`brandingSettings` に欄が無い ＝ オーナーの手）
#   (b) **チャンネルの説明欄（概要）** —— `brandingSettings.channel.description`（`channels.update` **50単位**）。
# **(b) の URL は押せます**（2026-09-19 09:1x に公開ページで確かめた・**Data API 0単位**）:
# `UCX6OQ3DkcsbYNE6H8uQQuVA` の説明欄の `https://mrbeast.store` は、ページの中で
# `"navigationEndpoint":{...,"urlEndpoint":{"url":"https://www.youtube.com/redirect?…"}}` を持つ ＝ 押せる字として組まれている。
# 07:5x の「12 host」は、この (b) の側でした（(a) の印 `channelExternalLinkViewModel` はページに 0件）。
#
# **そして、うちの (b) は 前のチャンネルの字のままでした**（同じ刻に読んだ）:
# 「制度の解説ではなく…扱っている範囲：残業代と割増賃金、失業給付、所得控除、社会保険、税金の手続き。」
# ＝ 年金・退職金 の字が 1つ も無く、リンクも無い。**ショートからアイコンを叩いた人が着く面が、別のチャンネルの説明**でした。
#
# **決め**: `channel_description()` の字を `channels.update` で置く（`cli channel-desc`・`catchup` の 1手）。
# (a) は窓【2】のまま残す（両方 在るほうが押されやすい・重ねても害は無い）。
#
# **覆る条件**:
#  (1) `channels.update` が 200 を返して **説明欄が動かなかったら**（題と同じ型・`rename_pending` の註）、
#      `CHANNEL_DESC_REFUSE_CAP` 回 で止め、この面はオーナーの手（窓）へ移すこと。
#  (2) 置いて 7日、`pubcheck` の `asp_link_desc` が True なのに ASP のクリックが 0 なら、面ではなく**声の文**を疑うこと。
#  (3) YouTube が説明欄の URL を押せなくしたら（`navigationEndpoint` が消えたら）、(a) だけに戻す。

#: YouTube のチャンネル説明欄の上限（字）。**越えると `channels.update` が 400 で落ちます**。
CHANNEL_DESC_LIMIT = 1000

#: 1行目（スマホのチャンネルページで「…もっと見る」の上に出る側）。
CHANNEL_DESC_HEAD = (f"{BRAND_NAME}。年金・退職金・税金の「手取り」を、条文と公表資料の式で計算して出しています。"
                     "前提は毎回 画面に出しているので、自分の場合に置き換えられます。")
CHANNEL_DESC_TAIL = ("※ 一般的な情報提供で、個別の助言ではありません。制度は改正されます。"
                     "実行前に公式の情報・専門家にご確認ください。")


def channel_description() -> str:
    """チャンネルの説明欄に置く字。**案件が 1本 も無ければ、リンク無しの字**（この file は止めません）。

    `【PR】` と「アフィリエイト」の 2語 は、説明欄・コメント欄と同じく塊の 1行目 に出します（景表法・ステマ規制）。
    上限 `CHANNEL_DESC_LIMIT`（1,000字）は蓋で、いまの字は約 500字。
    """
    got = offers()
    if not got:
        return f"{CHANNEL_DESC_HEAD}\n\n{CHANNEL_DESC_TAIL}"[:CHANNEL_DESC_LIMIT]
    pr = (f"{MARK}「自分の場合はいくら？」は、下から無料で相談できます（アフィリエイトです）。\n"
          + "\n".join(f"・{line}\n{url}" for url, line in got)
          + f"\n{DISCLOSURE}")
    return f"{CHANNEL_DESC_HEAD}\n\n{pr}\n\n{CHANNEL_DESC_TAIL}"[:CHANNEL_DESC_LIMIT]
