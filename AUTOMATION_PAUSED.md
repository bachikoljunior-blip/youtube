# AUTOMATION PAUSED — 2026-08-30

## Status

**動画の生成・アップロード・再スケジュールを停止する。分析とデータ保全だけは続けてよい。**

## Why

YouTube の現行チャンネル収益化ポリシーは、次を収益化不可として明示している。

- mass-produced / generic / repetitive / template-based content
- AI-generated personas presenting themselves as human experts on sensitive topics
- AI personas providing financial guidance or interpreting legal rules
  （**原文はその手前に条件を持っている** —— "presents itself as a **human expert providing advice**"。
  **禁じられているのは題ではなく、名乗りと助言のほう。** 下の「進捗」4 を読むこと）

Current channel configuration conflicts with that policy:

- niche: お金・税金・キャリア
- persona: 「元・事業会社の経理／人事」と名乗る
- narration: synthetic TTS
- production: automated template pipeline

このまま投稿を増やしても、目標である YouTube 収益化の到達可能性を上げず、審査時の不適合材料を増やすおそれがある。

Official policy reviewed 2026-08-30:
https://support.google.com/youtube/answer/1311392

## What is blocked

- `python -m src.pipeline`
- upload entry points
- batch/Shorts generation
- title/thumbnail/link/reschedule automation

`src/pause_guard.py` と `src/config.py` が二重に止める。

## What remains allowed

- analytics/status/reach/retention の読取
- 既存データの保存
- monetizable replacement format の調査
- dry analysis that does not generate, upload, schedule, retitle or otherwise modify channel content

## Resume gate

次の全条件が記録されるまで解除しない。

**この一覧は写しです。正本は `data/resume_gate.jsonl`** ——
**`python scripts/eta.py --gate` を撃って確かめること**（写した瞬間に古くなります。
実際、この節は 08-30 の夜まで「2件が閉じました」のまま、
台帳では 5件 閉じていました）。

1. sensitive-topic AI persona を使わない  **← 2026-08-30 に閉じた（下の「進捗」）**
2. human expert を装わない                **← 2026-08-30 に閉じた（下の「進捗」）**
3. final videos are materially varied and demonstrate a clear original creative contribution
   **← 2026-08-30 に閉じた**（入口 `script_writer.opening_form()`／`closing_form()` の 4×4 ＋
   出口 `verify._check_frame_repeat()`。1つの型に揃う本は多くても 30.1%）
4. policy-compliant channel concept is reviewed against the current official policy
   **← 2026-08-30 夜に閉じた（下の「進捗」4 の末尾）。** 公式ポリシーは
   **Last updated: July 15, 2025 のまま**で、当たる条項は (A)(B) の2つ。
   最後まで残った (B)「量そのもの」は、**13本/日 が機械の上限**として入りました
   （`batch_build.cap_by_density()`・検査 `tests/test_density_cap.py`）
5. 既存動画の扱いと新旧テーマ混在リスクを決める  **← 2026-08-30 に閉じた**
   （引っ込めない。理由は `docs/CONSTRAINTS.md` B14 ——名乗りは控え694本に 0件、
   残るのは型で、**出た本を消しても直りません**）
6. monetization path and acquisition economics are recalculated  **← 2026-08-30 に閉じた**
   （`scripts/shorts_subs.py` の解き直し。いちばん近い門を縛っているのは
   密度ではなく変換）

**6件とも記録されました（2026-08-30 夜）。それでも、この回は解除していません。**

**理由は3つ。どれも「待っているから」ではありません。**

**1. この6件を閉じたのは、全部こちら側の自己申告です。** 上の条件文は
「**次の全条件が記録されるまで解除しない**」と言っており、これは**必要条件**です。
**「記録した」が「解除してよい」と同じ意味だとは、どこにも書かれていません。**
そして 1・2・4 が扱っているのは**なりすまし**で、`CLAUDE.md` はそれを
「手段として成立しない2つ」の片方に置いています ——
**同じファイルが「消して再開しないこと」とも書いています。**

**2. この8件は、人の手で `origin/main` へ直接 入った介入です**
（08-30 08:54〜08:56・英語・`Co-Authored-By` も `Claude-Session` も無い ＝
この repo の自動の回の形と、どれも一致しません）。
**自分で閉じて、自分で外す**のは、その介入の意味を消します。

**3. 機械の上限は、まだ1度も走っていません。** `batch_build.py` は
`pause_guard.BLOCKED_ENTRYPOINTS` に載っているので、**止まっている間は
`--pick-only` すら撃てません**（迂回しないこと）。効いているのは
`tests/test_density_cap.py` の 10件だけで、**`main()` を通した実行は 0回**です。
前の回が「入れた上限を1度も試さずに解除するのが、いちばん危ない」と書いたのは、
まさにこの形のことです。

### **だから、解除はオーナーの判断で行うこと**

**この回がやったのは、判断に要るものを全部そろえるところまで**です。
`python scripts/eta.py --gate` が 6/6 を出し、根拠は `data/resume_gate.jsonl` に
1件ずつ入っています。**残っているのは「外す」という1手だけ**で、
それは `AUTOMATION_PAUSED.md` を消せば終わります（消す＝`src/pause_guard` が開く）。

**解除したら、最初の1周でやること**（順番を崩さない）:

    - `python -m src.density_verdict`  上限が実際に効いているか（API 0単位）
      **上限（`day_cap`）を外した側も、同じ画面に出ます**（2026-08-30 夜に足した ——
      それまで `main()` は生の ×0.003 だけを出し、外した側の ×0.26 を黙っていた）。
      **外した側が 0.5 を超えていたら、上限は間隔の話ではありません** → 4番を開き直すこと
    - 20本 出したところで `python -m src.frames`   型が本当に散っているか
    - どちらかが外れていたら、**その場で止め直すこと**（このファイルを戻す）

**そして、解除を待たずに済む穴が1つ残っています**（2026-08-30 夜に測って足した）:

    - `python -m src.descriptions --refresh`（約15単位・**日枠は JST 16:00 に戻る**）
      1・2・5 を閉じた `legacy_corpus` が見たのは**台本の控え694本**で、
      **説明欄は分母に1本も入っていません。** 出口の門
      `verify._check_no_human_expert_claim()` は説明欄も見ているので、
      **これから作る本では見ている面を、既にある735本では一度も見ていない**形です。
      審査する側から見ると、説明欄は**再生しなくても読めます。**
      **名乗りが1本でも出たら 1・2 を開き直すこと**（`--open-gate 1 --evidence "..."`）。
      **これは読取だけなので、停止中に撃てます。** 2026-08-30 22:31Z は
      `quotaExceeded` で 0/735 —— **止まっているのは枠であって、判断ではありません。**
      残りは毎周 `python scripts/eta.py --gate` が出します（測り終われば消えます）

**[!] この節に `1.` `2.` で始まる行を書かないこと。** `src/resume_gate.conditions()` は
`## Resume gate` から次の `##` までの**番号つきの行を全部 条件として数えます** ——
**字下げしたコードブロックの中も同じです。** この節を書いたとき、
上の3行を `1.` `2.` `3.` で書いたせいで、**門が 6件 から 9件 に増えました**
（`python scripts/eta.py --gate` が「9/9」と印字した）。
いまは `conditions()` の側でも字下げした行を落としますが、
**ここは正本なので、書く側でも踏まないこと。**

**[!] 止めたままでも、予約済みの本は公開され続けます**（08/30 時点で 475本・
11.9本/日・10/09 まで）。**止めることは、止まっている間の安全を意味しません** ——
本数は `python scripts/eta.py --gate` が出します。

---

## 進捗

### 1・2 —— 2026-08-30 に閉じた（`config/channel.yaml` ＋ `src/verify.py`）

**入口**: `config/channel.yaml` の `persona` から、実在しない人間の実務経歴を落とした。

    旧  元・事業会社の経理／人事で、制度を実務で回してきた立場から解説する。
    新  肩書きも経歴も資格も名乗らない。一人称で職業や勤続年数を語らない。
        自分を専門家だと言わない（実在の経歴が1つも無いので、書けば嘘になる）。
        根拠にするのは経験ではなく、その回に自分で置いた前提と、そこから引いた計算式そのもの。
        …… 助言ではなく試算として書く

旧版は `src/script_writer.py:1086` から**毎本の台本の指示文**にそのまま入っていた。
つまりこの停止の中心的な前提は推測ではなく、**設定ファイルに書いてあった事実**である
（2026-08-30 00:5x の回が「コードに無い」と報告したのは誤り。`docs/JOURNAL.md` 01:5x）。

**出口**: `src/verify._check_no_human_expert_claim()` を足した。
`script_only_problems()` に入れてあるので、**22本のクリップを焼く前**に当たる。

**なぜ設定だけでは閉じないか**: `persona` は台本を書かせる指示文の一部でしかなく、
書き手（LLM）はそこに無い経歴を自分で足せる。「元・経理」を消しても
narration に「私が担当していたころは」と書かれれば、視聴者から見える形は同じ。
**入口だけを塞ぐと、次に人格を書き換えた回が黙って穴を開け直せる。**

**実測**（`tests/test_no_human_expert_claim.py`・20件が緑）:

    落とす   元・経理として／私は人事の担当でした／税理士としての経験から／
             実務で回してきた／自分の経験上／私は10年間、経理を担当してきました／
             専門家として断言します
    通す     税理士に確認してください（相手が専門家）／会社員として働く人は（主語が視聴者）／
             説明欄の定型文「専門家にご確認ください」／普通の計算の文
    偽陽性   **投稿済み735本の題を全部通して 0件**（`data/uploaded.jsonl`）

**落としたのは経歴であって、企画ではない。**
「制度を解説するのではなく、自分で計算した結果を発表する」（`CLAUDE.md` の根幹）は、
架空の実務経歴を1文字も必要としない —— 「元・経理」をやめても `src/calc/` の数字は減らない。

**覆る条件**: 実在する人間が実名で出演し、その経歴が事実になったら「装う」に当たらない。
そのときは検査ごと外してよい。**偽陽性が出ても、`persona` に経歴を戻すことでは直さないこと。**

### 4 —— 公式ポリシーに当てて読み直した（2026-08-30・**この停止の読みが1か所ずれていた**）

出典: https://support.google.com/youtube/answer/1311392 （**Last updated: July 15, 2025**）
と、YouTube ヘルプの AI ペルソナに関する説明。**取ってきたのは 2026-08-30。**

#### 当たる条項は2つある。**別々の条項で、閉じ方が違う。**

**(A) AI ペルソナ ／ センシティブな題**

> channels using AI-generated personas to deliver information on sensitive topics,
> including any content that **presents itself as a human expert providing advice**
> on topics such as health, legal issues, finances, or politics

> - an AI "doctor" providing medical diagnoses, health advice, or wellness remedies
> - AI-generated podcast hosts offering financial guidance, investment tips, or wealth management advice
> - AI personas giving legal advice or interpreting laws

**(B) 汎用・反復（Generic or Repetitive Content）**

> Similar or repetitive content with low educational value, commentary,
> narratives, or minimal variation

> AI-generated content made with generic or unoriginal templates
> giving the impression of mass production

#### **ここが、この停止の書き方とずれていた**

`AUTOMATION_PAUSED.md` の冒頭は (A) を
「**AI personas providing financial guidance or interpreting legal rules**」と写している。
**原文は、その手前に条件を1つ持っている** ——
**「presents itself as a human expert providing advice」**。
禁じられているのは *金融や制度の話をすること* ではなく、
**人間の専門家として、助言をすること**のほうである。

**この差は小さくない。** 前者の読みだと、この企画は題ごと成立しない。
後者の読みだと、**閉じ方が2つある**:

    1. 人間の専門家として名乗らない   ← 2026-08-30 に閉じた（上の「1・2」）
    2. 助言ではなく試算として書く     ← 同じ回に `persona` へ入れた
                                        （「あなたはこうすべき」ではなく
                                          「この前提で計算するとこうなる」）

**`CLAUDE.md` の根幹（「制度を解説するのではなく、自分で計算した結果を発表する」）は、
偶然ではなく、この条項のちょうど外側を指している。**
説明欄の定型文（`channel.yaml` の `footer`）も
「一般的な情報提供を目的としたもので、個別の助言ではありません」と言っており、
**2番の側はもともと守られていた。** 欠けていたのは1番だけだった。

#### **だから、いま縛っているのは (A) ではなく (B) である**

(A) は上の2手で外側に出た。**残っているのは (B) の側**で、
そちらのほうが**この企画にとって厳しい**:

    実測  735本 ／ 1日 6〜16本 ／ 同じ台本の型 ／ 同じ図の作り ／ 同じ合成音声
          → 「giving the impression of mass production」に、正面から当たる

**Resume gate の3番（本ごとに実質的な差があること）が、本当の関門である。**
1・2 を閉じたことで、**残り4件のうち3番が最優先**になった。

**次の回への申し送り**: 3番は「差がある」と**言う**のではなく、**数で示す**こと。
材料はもうある —— `visuals.theme_for()`／`batch_build.theme_base()`（配色の分散）、
`verify._check_not_repeat`／`_check_adjacent_repeat`（棒の重なり）、
`src/calc/` が本ごとに違う数字を出していること。
**足りないのは、それを1つの尺度にまとめて「何本ぶん違うか」を出す道具のほう。**

**覆る条件**: このページは更新される（写した版は **July 15, 2025** 最終更新）。
**次に当たる回は、日付を見て、変わっていたらこの節ごと取り直すこと。**

### 5 —— **予約済み482本は、そのまま公開させる**（2026-08-30 に決めた。**待っていない**）

`data/uploaded.jsonl` の実測（2026-08-30 10:5x JST）:

    控えにある本                735本
    **すでに公開済み             209本**
    **これから公開される予約     482本**（2026-08-30 11:00 〜 2026-10-09 23:00 JST・41日・1日6〜16本）
                                 うち尺の分かるもの: 長尺143 ／ ショート69 ／ 不明270

**予約はすでに YouTube 側に入っている。機械が1回も起きなくても482本は公開される。**
停止が止めたのは「新しく作って足すこと」であって、**すでに並んでいる列ではない。**
そして引っ込める道具（`scripts/reschedule.py`）は `src/pause_guard` の対象に入っている。

#### 決定: **引っ込めない。そのまま公開させる。**

**08/30 02:4x の回は、これを3択にしてオーナーへ上げ、「答えが来るまで動かさない」と
書いて終わっている。** `CLAUDE.md` の冒頭がその形を名指しで禁じている ——
**「『オーナーの許可がほしい』と書いたら、その時点で間違いです」**（オーナー原文・3回目）。
**だからここで決める。**

**決め手は1つ。審査は2年先にある。**

    登録者（純）  23人       門は 1,000人 —— **あと 977人**
    いまの速さ    1.28人/日  → 門1 は **765日後（2028-10-03）**（`scripts/eta.py`）

**482本が公開されても、審査は起きない。落ちようがない。**
「審査のときに古い形の本が並んでいる」という心配は**2年先の話**で、
そのときまでに**消すことも、非公開に戻すことも、いつでもできる。**

    引っ込めない側の損  審査で不利になるかもしれない —— **2年先・回復可能**
                        （門に近づくまでに消せる。消す判断は、そのとき実測でできる）
    引っ込める側の損    生成が止まっている以上、**482本が唯一 残っている入力**。
                        引っ込めれば、チャンネルへの入力は6週間 **ゼロ**になる。
                        **即時・確実・回復不能**（消した視聴時間は戻らない）

**片方は「2年先の、回復できる損」、もう片方は「いまの、確実で回復できない損」。**
`CLAUDE.md`「投稿が途切れるのが最大の損失」もこちら側を向いている。

**そして期待値の符号を決めているのは、なりすましが**動画に**入っているかどうかだが、
それは今日は測れなかった**（API の日枠切れ）。**測れないうちに、
回復できないほうへ倒さない** —— これがこの決定の形である。

#### **覆る条件（3つ。どれかが起きたら、この決定を撤回して引っ込めること）**

1. **公開済みの本の説明欄を読んで、人間の専門家の名乗りが実際に入っていた**
   （`videos().list(part=snippet)` を 735本に当てる。50件/回・15回・読み取りだけ）。
   **入っていれば、審査より前に「なりすましのまま公開し続けている」ことになる。**
   題は735本とも 0件 だったが、**題は短いので証拠として弱い**
2. **登録者が 500人 を超えた**（門1 の半分）。そこからは審査が現実の日程に入るので、
   「2年先」という決め手が消える。**そのときは残りを止めて、公開済みを整理すること**
3. YouTube から警告・収益化の否認・チャンネルに対する措置が実際に来た

**1番は次に日枠がある回の最初の仕事**。**2番は `scripts/eta.py` が毎回 印字している。**

#### 5番の残り（**これで閉じてはいない**）

決めたのは**予約済み482本**だけ。5番はあと2つを言っている:

    公開済み 209本    …… まだ決めていない（上の「覆る条件」1番の測定が要る）
    これから作る本    …… 3・4番が決まってから（新しい形が決まらないと、混ざり方も決まらない）

### 6 —— 採算を解き直した（2026-08-30・**この道は、いまの形では通っていない**）

**結論をここに書き置きません。** 数はすべて `python scripts/shorts_subs.py --offline`
が毎回 出し直します（API 0単位）。**下は 2026-08-30 の写しで、腐ります。**

#### まず、条件そのものを取りに行った（**API の問いではなかった**）

出典 https://support.google.com/youtube/answer/72851 （取得 2026-08-30）

    「1,000人 ＋ 4,000時間/12か月」 **または** 「1,000人 ＋ ショート1,000万再生/90日」
    "any qualified watch hours from Shorts views in the Shorts Feed
     won't count towards the 4,000 qualified watch hours threshold"

**`scripts/shorts_subs.py` は 08-25 から「入るかどうかは、この API では
確かめられません」と書いて両方の日数を出していました。** API の話としては正しく、
**問いを取り違えています** —— これは公表条件の問いで、取りに行けば1行 載っています。
**その5日、「あと 349日」という枝が生きていました。実際は 24,698日（67.7年）。70倍。**

#### 実測（`data/shorts_subs.json`・窓 2020-01-01〜2026-08-25）

    登録の門（**どちらの道でも要る**）  残り 978人／変換 0.2476人/千再生
                                        要る再生 3,950,605（95% 2.53M〜6.56M）
      いまの速さ 5,885再生/日           → **671日**
      **測った天井 6,478再生/日**       → **610日**（10本/日 × 熟した本 648回）
      ＝ **天井まで出しても 62日（10%）しか縮まない**
      1年で開けるには 10,824再生/日 ＝ いまの 1.84倍・**天井の 1.67倍（＝天井の外）**
      または変換 0.4553人/千再生（いまの 1.84倍）

    道A 4,000時間   長尺だけで 24,698日（67.7年）。**天井（44分/日）でも 14.9年**
    道B 1,000万/90日 いまの速さで 18.9倍／**天井でも 18.0倍 足りない**

#### この解き直しが言っていること

1. **どちらの視聴の門も、測った天井の外にあります。** 待っても届きません
2. **いちばん近い門（登録1,000人）を縛っているのは、密度ではなく変換です。**
   `data/runs.jsonl` の 358件 でいちばん多く引かれた腕は `density`（103件）——
   **その腕は、いちばん近い門に対して 10% です**
3. だから **1.84倍**（1本あたり再生 × 登録率）が、この企画の本当の合格点です。
   `eta.py` の軌跡が置いている per_video ×3.05・sub_rate ×6.40 は
   **おまけではなく、道そのもの**でした
4. **そしてその倍率は、停止中は1つも動きません**（本が出ないので）。
   ＝ **採算の側から見ても、律速は門のほうです**

**覆る条件**: 変換 0.2476 の 95%区間は 0.149〜0.387 と**2.6倍 幅があります**。
区間の上端なら要る再生は 2.53M（**1年なら 6,930再生/日 ＝ 天井の内側**）。
**登録が2桁のうちは、この幅が結論そのものです。** 登録が増えたら解き直すこと。
天井の側も `src/day_cap.window()` が (A)本数 と (B)時刻 を切り分け終えていません。

### 3 —— **枠を振り分けて、揃った枠を投稿前に落とす**（2026-08-30 に閉じた）

申し送りは「**3番は『差がある』と言うのではなく、数で示すこと**」でした。
**まず測りました**（`python -m src.frames`。API 0単位・実測1.4秒・控え694本）。

尺度は **実効の型数 ＝ `exp(シャノンエントロピー)`** ——
「この N本は、実質 **何本ぶんの型**でできているか」。

    長尺 134本    1行目の頭4文字  「計算しま」 **84%**（113本）＝ 実効 **2.4本ぶん**
                  最終行の頭4文字 「明日やる」 **61%**（82本） ＝ 実効 **3.8本ぶん**
                  最終行の末尾6字 「てください。」**57%**        ＝ 実効 **9.0本ぶん**
    ショート 558本  最終行の頭4文字 「あなたの」 **45%**（251本）＝ 実効 **24.4本ぶん**
                  最終行の末尾6字 「てください。」**40%**（222本）

**134本の長尺は、入り方で言えば 2.4本ぶんしかありません。**
これが "giving the impression of mass production" の中身です。

#### **中身は散っていました。揃っていたのは枠だけです**

    題の族            526 / 694本
    出だしの1行の形    689 / 694本（数字を N に潰しても 99.3%）
    画の変化率        本ごとの中央値 0.131

#### **そして、それは書き手の癖ではありませんでした**

`src/script_writer.py` の `ROLE` に、**文句が直書きしてありました**:

    「調べてみました」ではなく「計算しました」          → 84%
    **長尺のみ**: 最後のセグメントは「明日やること」    → 61%
    例「あなたの手当は全員同額ですか。…」              → 45%

**上の3つの割合は、この3行の写しです。** 3行とも「意図」だけを残して
**文句を消しました**（例文も消しています —— 例文がそのまま型になっていました）。

#### 入口 ＋ 出口（**解除条件1・2 と同じ形**）

**入口**: `script_writer.opening_form()` / `closing_form()` が、
テーマIDのハッシュで **4通りずつ**を割り当て、`generate()` が毎本 指示文を足す
（塩は `open:` / `close:` —— 既存の3つ（無塩 / `hook:` / `midreq:`）と別。
重なると、どちらが効いたのか永久に分かりません）。

    入り方  結論 ／ 差分 ／ 境界 ／ 反転
    締め（長尺）  手順 ／ 置換 ／ 分岐 ／ 限界   ←「手順」は4つのうちの1つ。既定ではない
    締め（ショート） 依頼先 ／ 数字先 ／ 条件先 ／ 対比先

**生成は止まっていますが、割り当てはハッシュなので今日 数えられます。**
台帳の実在テーマID **712件**で実測（`python -m src.frames --forms`）:

    opening        最頻 **28%**（実効 3.99通り）
    closing_long   最頻 **30%**（実効 3.97通り）
    closing_short  最頻 **30%**（実効 3.97通り）

**＝ 1つの型に揃う本は、多くても 30.1%。**（84% / 61% / 45% から）

**出口**: `verify._check_frame_repeat()` が**直近20本**（同じ向き・自分のIDは除く）と
見比べ、同じ枠が **50%** を超えたら落とします。`script_only_problems()` に
入れてあるので、**22本のクリップを焼く前**に当たります。
**旧の長尺 80本を当て直すと 78本が落ちます。**

**ショートの締めは、4通りとも「登録」を要求します** ——
`src/endcard_verdict.is_request()` が最終行の「登録」だけで処置群を作っており、
`config/hypotheses.yaml` 期限 2026-10-11 がそこにぶら下がっているためです。
**変えるのは依頼の位置と前置きだけで、依頼の有無ではありません**（群は1本も減りません）。

検査は `tests/test_frame_forms.py`（20件・緑）。

#### **この閉じ方が言っていないこと**

**「出来上がりの実測」ではありません。上限です。** 生成が止まっているので、
新しい本は1本もありません。**再開後 20本 で `python -m src.frames` を撃ち直すこと**
—— そこで最頻が 30% を下回っていなければ、この閉じ方は効いていません。

**覆る条件**: 通り数を4から増やしたら `verify.FRAME_MAX_SHARE` を下げること
（4通りで 0.5 は「期待の2倍」。8通りなら 0.3 が同じ厳しさ）。
偽陽性が出ても、**指示文へ決まり文句を書き戻すことでは直さないこと** —— それが元の穴です。

#### **当て直した（2026-08-30 夜）—— 塞いだのは読み上げだけで、画面が残っていた**

**この閉じ方を実測で当て直した回の記録です。閉じたままにしていますが、
根拠が1軸ぶん増えています**（開き直していない理由も下に）。

上の入口（`OPENING_RULES` / `CLOSING_RULES`）も出口（`_check_frame_repeat`）も、
**読み上げの文しか見ていませんでした。** 控えの `*.plan.json`（**655本**・API 0単位）を
**同じ4文字**で測ると、揃っているのは画面のほうです（`python -m src.frames`）:

    長尺 134本    読み上げの最終行の頭「明日やる」 61%
                  **最後のコマの見出し「明日やる」 83%**（実効 **2.1本ぶん**）
    ショート 521本 最後のコマの見出しが「あなたの」29% ＋「あなたは」25%

**続けて数本 見た人が最初に気づくのは、耳より目のほうです。**
読み上げを4通りに散らしても、画面に同じ見出しが並べば同じ動画に見えます。

    入口  `script_writer.CLOSING_HEADLINE_RULE`（締めの型を見出しにも当てる）
    出口  `verify._check_screen_frame_repeat()`（`_check_frame_repeat` から呼ぶ ＝
          **同じ1回で両方 当たる**。窓・閾値は読み上げ側と同じものを使い、写さない）
    測る  `frames.screen_axes()` / `plan_of()` / `screen_concentration()`
    検査  `tests/test_screen_frames.py` 14件（実物に当てる3件を含む）

**開き直さなかった理由**: 当て直しは閉じた側を**否定していません** ——
`--forms` の上限 30.1% は撃ち直しても同じで、**足りなかったのは軸の数**でした。
その軸を同じ回に入口と出口の両方へ入れたので、**条件の満たし方は当時より広くなっています。**
**開き直すのは、この節の数が実測で覆ったとき**（下の「覆る条件」）。

**覆る条件（この追記ぶん）**:

- 再開後 20本 で `python -m src.frames` を撃ち、**`screen_closing` の最頻が 50% を
  超えていたら、解除条件3 を開き直すこと**
  （`python scripts/eta.py --open-gate 3 --evidence "…"`）
- 画面の軸を増やすときは `frames.SCREEN_AXES` に足すこと。**`verify` に写さない**
- **1枚目の `kind` は 100% `stat` ですが、門にしていません** ——
  4通りの入り方が全部「結論の数字を先に言い切れ」と言っているので、
  そこは維持率の要件そのものです。散らしても維持率が落ちないと実測できたら足すこと

### 4 —— **公式ポリシーを取り直した（変わっていない）。残りは1件だけ**

**2026-08-30 に取り直しました。** 前の節（102行目〜）の写しは
**Last updated: July 15, 2025**。**今日 取り直しても同じ日付**でした
＝ 前の節の「覆る条件」（日付が変わっていたら取り直す）は満たされており、
**あの読み（(A) は名乗り条件つき／いま縛るのは (B)）はそのまま効きます。**

**同じページから、前の節に写っていなかった一文が出ました**（(B) 側の続き）:

> content that appears to be produced using a template or
> **where each video doesn't deliver creative, educational, or other value**

**「各本が価値を届けているか」まで書いてあります。** これは
`CLAUDE.md` の根幹（制度の解説ではなく**自分で計算した結果を発表する**）が
そのまま答えになる条項で、`src/calc/` が本ごとに**どこにも公開されていない数字**を
出し、前提と計算式を画面に出しています。

#### 条項ごとの当たり方（いまの構想で）

    (A) 人間の専門家として助言   **外側**  解除条件1・2（入口 `channel.yaml` の persona ＋
                                          出口 `verify._check_no_human_expert_claim()`。
                                          控え694本に 0件・行動の指図 1件）
    (B) 型どおり・反復           **外側**  解除条件3（上の節。1つの型に揃う本は ≤30.1%、
                                          出口の門つき）＋ 配色は本ごと（08-28）＋
                                          chart の棒は計算結果そのもの（`_check_not_repeat`）
    (B) 各本が価値を届けるか     **外側**  `src/calc/` が本ごとに計算する。前提と式を画面に出す
    (B) **量そのもの**           **未決**  ← **残っているのはここだけです**

#### **残り1件: 1日の本数**（この回に測った。**decision がまだ**）

**「giving the impression of mass production」は、揃い方だけでなく量の話でもあります。**
そして測ったら、**目標の側からも同じ向きを指していました**
（`python -m src.density_verdict`・API 0単位）:

    詰めた日（1日16本以上）  1本あたり再生の中央値 **2回**（5日・119本）
    1時間きざみの日（8〜13本） 1本あたり再生の中央値 **716回**（4日・42本）
    倍率 **0.003**（`falsified_if` は 0.5 未満）→ **falsified**

**前提「予約の間隔を1時間より詰めても、1本あたりの再生は落ちない」は外れました。**

**そして `scripts/eta.py` の `PLAN_PUBLISH_PER_DAY` は 25 です。**
到達日の段1〜4は**全部その本数の上に乗っています** ——
**いま外れたばかりの帯の中の数字で、日付が出ています。**

**だから4番は、まだ閉じません。** 閉じるには、次の回がこれをやること:

    1. `PLAN_PUBLISH_PER_DAY` を、測れている帯（8〜13本/日）へ落とす。
       **到達日は動きます。** 動いた日付が本当の予測です
    2. `scripts/batch_build.py` に**機械の上限**を入れる（文書だけにしない）。
       いまの実績は 08/27 19本・08/28 22本 で、**再開したらそこへ戻ります**
    3. `config/hypotheses.yaml` の該当前提へ `next_if_false` の処置を書き込む
       （「1日あたりの本数はチャンネル側で頭打ちと確定させ、
         `docs/MEANS.md` M14 の『1日92本』を到達不能として畳む」）
    4. そのうえで 4番を閉じ、**6/6 になったら解除して再開すること**

**この順番を崩さないこと。** いま解除すると、機械は 19〜22本/日 へ戻ります ——
**それは (B) のいちばん目立つ材料であり、同時に1本あたり再生が中央値 2〜56回 の帯**です。
**ポリシーの側と目標の側が、はじめて同じ数字を指しています。**

#### **4件とも終わりました。4番は閉じます（2026-08-30 夜）**

    1  PLAN_PUBLISH_PER_DAY 25 → 13          済（前の回）
    2  batch_build に**機械の**上限          済（この回）
    3  hypotheses へ処置を書き戻す           済（この回）
    4  4番を閉じる                           **この節**

**2 が、この4件のうち唯一「機械が変わる」ものでした。** 残り3件は文書です ——
そして**文書はすでに3か所 同じことを書いていました**（`docs/MEANS.md` M14 は
08-25 から「崩れる点は 10本/日」、`config/hypotheses.yaml` の `next_done` は
08-28 から「頭打ちと確定させた」、`scripts/eta.py` の註も同じ）。
**それでも 08/27 に 19本、08/28 に 22本 置いています。**
**3か所が同じ結論を書いていて、置いている本数だけが別だった** ——
だから 2 を「文書だけにしない」と書いた前の回は正しく、
**ここで閉じてよいのは、機械が実際に落とすようになったからです。**

**入れたもの**: `scripts/batch_build.cap_by_density()` を `slots()` の出口
（`main()` の 0.7）に置きました。**生成の前**に当たるので、上限を超えた本は
1秒も作りません（題材は在庫に残り、次の回が別の日へ置けます）。

**なぜ出口に置いたか。** 置き先を決める道は5本あり、既にあった `_per_day_soft()` は
`_band_walk()` の**帯の中しか数えません**（`busy & set(grid)` ＝ 09:00〜13:30 の枠）。
素通りしていたのは4本 —— 長尺の `ring`（20:00〜）／`--hours` の明示／
`--step-min 30` の `_slots_fine`／**同じ日に2回 走った回**。
**19〜22本/日 は、この4つの足し算でした。**

**上限の出どころは1か所です**: `src.density_verdict.HOUR_HI`（測れている帯の上端
13本/日）。`scripts/eta.py` の `PLAN_PUBLISH_PER_DAY` も同じ数を持ち、
**3つが同じ所を読んでいるかを `tests/test_density_cap.py`（10件）が見ます** ——
ずれた瞬間に赤くなるので、**また「言っている所と、している所が別」に戻れません。**

**なぜ 10 ではなく 13 か。** `src/day_cap.py` の 10 は「その日に**再生が付いた**本数」、
13 は「**1本あたりが保っていると測れている帯の上端**」で、**別の物差し**です。
到達日の段1〜4 は後者に乗っているので、機械の上限も後者に合わせました
（前者に合わせると、計画が出せない本数で日付を出します）。

#### この節が (B) に対して主張していること（**上限であって、実測ではない**）

    (A) 人間の専門家として助言   外側   解除条件1・2（入口 `channel.yaml` ＋ 出口 `verify`）
    (B) 型どおり・反復           外側   解除条件3（1つの型に揃う本は ≤30.1%・出口の門つき）
    (B) 各本が価値を届けるか     外側   `src/calc/` が本ごとに計算し、前提と式を画面に出す
    (B) **量そのもの**           **外側**  **13本/日 が機械の上限**（この節）

**ただし言えるのは上限です。** 再開後に実際が何本になるかは、
**`python -m src.density_verdict` を撃ち直して確かめること**（API 0単位）。

**覆る条件**: 倍率が 0.5 以上に戻ったら `HOUR_HI` ごと動かしてよい
（3か所が同じ所を読んでいるので、直すのは1か所）。
逆に **8本/日 より下でも1本あたりが保つと測れたら、そちらへ落とすこと** ——
落とせるほど、量そのものが (B) から遠ざかります。**その向きは1度も測っていません。**

### 測れなかったこと（2026-08-30）

**公開済みの本の説明欄を読んで、実際に名乗りが入っているかを数えようとしたが、
YouTube Data API の日枠が尽きていて取れなかった**（`quotaExceeded`・同日に92本 投稿済み）。
題は735本とも 0件だったが、**題は短いので「入っていない」の証拠としては弱い。**
**次に枠がある回は、`videos().list(part=snippet)` で説明欄を全部通すこと**
（50件/回・15回・読み取りだけ。停止の「やってよいもの」の中）。
そこが 0件 に近ければ 5番の判断は軽くなり、多ければ重くなる。

#### **そのうち「画面の文字」は、API を使わずに測れました（2026-08-30 夜）**

この節と `src/legacy_corpus._as_script()` は、どちらも
**「説明欄と画面の文字は控えに無い」**と書いていました。**画面のほうは在りました**
—— `data/critique_queue/<video_id>.plan.json` が **655本ぶん**、割った後のコマを
そのまま持っています（`<id>.json` の `slides_plan` は `true`／`false` の**印**だけなので、
それを「中身が無い」と読んだのが出どころだと思われます）。

**測った結果**（`verify._check_no_human_expert_claim()` と `_ADVICE_PATTERNS` を、
新しい本と同じ関数で当てた）:

    画面に人間の専門家の名乗り  **0 / 655本**（うち予約 458・公開済み 189）
    画面に行動の指図            **0 / 655本**

**解除条件1・2・5 の根拠は、この軸を足しても動きません**（台本側と同じ 0件）。
**説明欄だけが、いまも Data API 待ちです。**

**「見ていない」と書いてあるものは、本当に見ていないのかを確かめること。**
見ていない側に置いたまま門が3件 閉じており、**画面は一度も測られていませんでした。**

### 外部の執行実態を当てた（2026-08-31・停止中の調査回。**追記のみ**）

**ポリシー本文は変わっていない。執行が変わっている。** 取ってきたのは 2026-08-31。

- **本文**: answer/1311392 は英語版・日本語版とも **Last updated: July 15, 2025 のまま**
  （両方 WebFetch で確認）。ゲート4 の「覆る条件」（日付が変わっていたら取り直す）を
  当てた —— **変わっていないので、読み（(A)は名乗り条件つき／縛るのは(B)）はそのまま効く。**
  日本語圏の記事が言う「2026年の新ポリシー」は、**この 2025-07-15 版の執行の波**のこと。
- **執行・世界**: 2026年に入り、テンプレ量産チャンネルの**閉鎖**が実際に起きている
  （登録者390万〜595万の大規模チャンネルを含む12件以上。bloomeria.jp 2026-02-03 が
  BusinessInsider / Guardian / Verge 等をまとめたもの。一次発表のURLは記事に無い）。
- **執行・日本語圏**: **2026年1〜2月、「ゆっくり」「ずんだもん」系（合成音声）チャンネルの
  収益化剥奪が一斉に起きた**（note.com の編集者コミュニティ複数が報告）。
  対象として名指しされている形は **「静止画と合成音声中心」「量産型」** ——
  **この機械の出力形式そのもの**である。手作業で作っていても
  「量産に見える」だけで判定される、という報告が複数ある。
  復活・生存側の共通項として挙がっているのは
  **「独自の視点の台本」「動きのある演出（静止画スライドからの脱却）」「一貫したテーマ」**。
  うちは1つ目（独自計算）と3つ目は既にあり、**2つ目が最大の欠け**
  （`opening_motion` の対照が2本 残っている前提と同じ場所を指している）。

**この追記が解除条件の判定を動かすか**: 動かさない（開き直す実測ではない）。ただし——

- **5番（予約482本を流す決定）の決め手「審査は2年先・落ちようがない」は、
  執行の実態より楽観側にある。** 収益化剥奪の波は審査を待たずに来ており、
  未収益化チャンネルでも「量産」の分類は審査時の材料として積み上がる。
  B14 の覆る条件（うちのチャンネルへの措置・登録500人・名乗りの実在）は
  どれもまだ満たされていないので**決定は覆さない**が、
  **解除を判断する人は、この執行実態を材料に入れること。**
- **再開後の形は「動き」を最優先にすること。** 生存側の3条件のうち、
  欠けているのはそこだけ（上）。型の分散（3番）だけでは
  「静止画+合成音声」という判定面の外に出ない。

## Override

緊急の手動検証だけ、明示的に次を設定したプロセスで許可する。

`ALLOW_POLICY_PAUSED_AUTOMATION=I_ACCEPT_YPP_POLICY_RISK`

通常の自動運転や恒久解除には使わない。
