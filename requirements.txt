google-api-python-client>=2.140.0
google-auth>=2.34.0
google-auth-httplib2>=0.2.0
requests>=2.32.0
playwright>=1.47.0
Pillow>=10.4.0
PyYAML>=6.0
pydantic>=2.9.0
# `docs/trigger_main.md` §4 が「検査を足したら pytest tests/ を叩いて件数を見ろ」と
# 言っているのに、**入っていなかった**（2026-08-15 の回が `No module named pytest`）。
# 手順が要求する道具は、手順と一緒に入っていないと毎回1手ぶん無駄になる。
pytest>=8.0.0
# `scripts/probe_yomi.py` が合成音声のスペクトルを比べるのに使う。
# 読み違いは耳でしか見つからなかったので、機械が聞けるようにするための道具。
numpy>=2.0.0
# `scripts/niche_ceiling.py --source free`（外の帯を Data API の日枠の外で撃つ・0単位）。
# search.list は 100単位/語 で、09/02 は2回とも 429 で外の数が主実行に届かなかった（2026-09-03）。
yt-dlp>=2026.8.19
# `src/yomi_hear.py`（**完成音声を最初から最後まで聞き取り、予定の読みと照合する門**）。
# オーナー原文 2026-09-03「Google TTSで生成した完成音声の最初から最後までを機械で聞き取り、
# 予定していた読みと照合し、誤読があれば修正・再生成して、もう一度全文照合する」。
# 実測（2026-09-03）: Google Cloud Speech-to-Text は **同じ鍵では撃てません** ——
# `GOOGLE_TTS_API_KEY` で speech.googleapis.com を叩くと 403 API_KEY_SERVICE_BLOCKED
# （鍵が TTS に絞ってある。鍵を広げるのはオーナーの手が要る）。
# faster-whisper は鍵が要らず、模型は初回に自動で降りてくる（medium で 1.5GB・CPU で音の 0.63倍）。
faster-whisper>=1.1.0
# `studio/hear.py`（読みの門・2026-09-06 に書き直し）: 予定の読みは janome（文脈つき）、仮名の正規化は jaconv、
# janome が読めない語の予備が pykakasi。whisper に漢字を禁じて音の仮名を取る（faster-whisper は上）。
janome>=0.5.0
jaconv>=0.3
pykakasi>=2.2
