[pytest]
testpaths = tests
markers =
    live: 生きている道具（`studio/` と親の手続き）の信号。`python -m pytest -m live -q` で 15秒。
          印は `conftest.py` が機械で付ける（決めているのは `studio/livetests.py`）。
          旧道具ごと全部 撃つのは `python -m pytest tests/ -q`（約9分）。
