"""**新しい calc を書くときの、写して埋めるひな型。**

    cp src/calc/_template.py src/calc/<名前>.py

**動画の題材にはなりません。** `_` で始まるので `topic_forge.py` は拾わず、
`PLACEHOLDER` が残っているあいだは `check_tables()` が必ず落ちます。
**埋め忘れたまま台本を書かせる道はありません。**

---

## なぜこれがあるか

calc を1本書くのに20分かかっていて、大半が `check_tables()` でした
（`docs/JOURNAL.md` 2026-08-15 の §6 (a2) 見直し3。**6回持ち越し**）。
在庫の律速は calc の本数なので（`docs/MEANS.md` M14）、ここが遅いと8本/日に乗れません。

## 埋める順番（**docstring は最後です**。2026-08-16 に順番を入れ替えました）

    1. 題材を1行で決める —— 「一般の解説はここで止まる」が言えるか。
       **言えないなら、その題材はやめること。** ただし**この時点では書かない**
       —— **その1行に数字を入れないこと**（2026-08-18 に測って足した）。
       「一般の解説はここで止まる」は**言葉で**言えます。
       **数字が要る主張は、4 を回すまで決まりません。**
       08-18 の `shokibo` は手順1で
       「20年超なら月58,333円まで無税」と決めてしまい、表を出したら **45,833円**
       （58,333は**平均でなく増え方**）でした。**言い換えにこの回いちばんの約12分**。
       落ちたおかげで主張は強くなりましたが（「70,000円には**永久に届かない**」）、
       **強くなったのは表を見たからで、手順1で決めた文からではありません。**
    2. ASSUMPTIONS —— 前提と根拠。**画面と説明欄にそのまま出ます**
    3. 制度の値 —— 条文か公表資料に当たって書く。**思い出しで書かない**
    4. 計算 —— 値を返す関数。表は dict のリストで返す（図解がそのまま食える）
    5. check_tables() —— 下の枠を埋める。**形は `_checks` が持っています**
    6. __main__ —— `=== 見出し ===` の節に分けて印刷する。
       **節が topic_forge のテーマ単位**なので、節を切るほど在庫が増えます
    7. **1回実行して、出た数字を見てから docstring を書く**

       **写すときに丸めないこと**（2026-08-18 20:3x に測って足した）。
       手順7を守って実行してから書いても、**`5.3554倍` を `5.36倍` と丸めて
       書いてしまいます。** 丸めた瞬間に、その数は**どの表にも無い数**になり、
       `_checks.numbers_backed` が落とします。`jidoushazei` を書いた回は
       **15件 → 5件 → 1件と3回鳴り、直すのに約6分**（1周の下限41分の15%）。
       **3回とも中身は同じで、全部「丸めた形」でした。**

           ✗  同じ「重課」で **5.36倍** の幅
           ○  同じ「重課」で **5.3554倍** の幅
           ○  丸めた形を出したいなら、**その形を表にも印字する**

### なぜ docstring が最後なのか（**測って入れ替えました**）

ここは長らく「1. 上の docstring」で始まっていました。**主張の数字は 4 を回すまで
出ないので、順番が実態と逆でした。** 2026-08-16 05:1x の回は、docstring を先に書いた
`zoyo.py` で**3か所が実測と食い違い**、書き直しにこの回いちばんの約10分を使っています
（`docs/JOURNAL.md` の §6 (a2) 見直し3）。

**この順で書いた `yukyu.py` でも、同じずれが1件出ました** —— 先に「10年で150日」と
見積もった数字が、実際に走らせると**12年で121日・20年で241日**でした。
違うのは、**書く前に出ていたので、書き直しが0回で済んだ**ことだけです。

## **格子の刻みは、主題の境目の幅より細かく取ること**（2026-08-18 に踏んだ）

`tsukin.py` の節3は「通勤手当4,200円で、厚生年金の等級をまたぐ人とまたがない人に
分かれる」でした。報酬月額を**10,000円刻み**で並べると、**31点すべてが「またがない」**。
**そこに主題は無い**という表になります。

**嘘ではありません。刻みのせいです。** 標準報酬の境目は 210,000・230,000… の
ような**5,000円ずれた点**にあり、10,000円刻みの格子は**その手前だけを踏みます。**
1,000円刻みにすると **301点中52点が「またぐ」**側に出ました。

    刻み ＞ 境目のずれ  → 境目が格子の穴を通り抜け、**平らな表に見える**
    刻み ＜ 境目のずれ  → 本当の形が出る

**「その主題が消えた」と「その主題が無い」は、表からは見分けられません。**
段・帯・崖を主題にするなら、**刻みを半分にしてもう一度出し、答えが変わらないことを
見ること。** 変わるなら、そこに書けるのは**刻みの話**であって制度の話ではありません。

## 書き終えたら、必ず1回実行して表を目で読むこと

`check_tables()` が見ているのは**値**で、**見出しと値の対応は見ていません。**
2026-08-15 に `taishoku.py` が5年とびの行に「1年あたり」と書いて素通りしました。
1行ぶんの差を出す列は `_checks.per_unit_steps()` を通すこと（割り忘れができません）。
"""
from __future__ import annotations

from . import _checks

PLACEHOLDER = -1  # **埋めるまで check_tables() が落ちます。消さないこと**

ASSUMPTIONS = [
    "＜前提を1文ずつ。読み上げられるので、記号ではなく言葉で書く＞",
    "＜率や上限は「〜パーセント」「〜円」と書き、適用条件を必ず添える＞",
    "＜入れなかったものも1行書く。前提の置き方そのものが独自の視点＞",
]
# **「〇〇率は仮定です」と書いたら、その率の値を同じ行に書くこと**
# （2026-08-16。`_checks.assumption_values` が落とします）。
#
# ここが渡すのは**この文だけ**です。表もモジュールの中身も、台本の書き手には渡りません
# （`src/script_writer.calc_block`）。値の無い前提を渡すと、書き手にできるのは
# **黙って落とす**（→ `verify` が1本まるごと捨てる）か
# **自分で数字を作る**（→ 裏の取れない数字が動画に入る）かの2つだけです。
#
#     ✗  "手取り率は制度の値ではなく、この計算での仮定です"
#     ○  "手取り率は仮定です。年額78万円で100パーセント、500万円で79パーセントとして置いています"
#
# 値が表にしか無くても、**代表値を1つ文に書くこと。**
# `nenkin` はこの形で8回ぶん申し送りを運びました（値は在ったのに、文が持っていなかった）。

# ---- 制度の値。**長く動いていないものだけをここに置く** ----------------
RATE = PLACEHOLDER          # 例: 0.05（率は 0〜1 で。5 と書くと _checks.ratio が落ちる）
CAP = PLACEHOLDER           # 例: 1_950_000

# 速算表があるなら (区分の上限, 税率, 控除額)。最上段の上限は None
TAX_TABLE: list[tuple[int | None, float, int]] = []


def tax_of(base: float) -> float:
    """速算表を引く。**表が無い題材ならこの関数ごと消すこと。**"""
    for cap, rate, sub in TAX_TABLE:
        if cap is None or base <= cap:
            return base * rate - sub
    raise ValueError(f"速算表に当たらない: {base}")


def grid() -> list[dict]:
    """図解の元になる表。**dict のリストで返すこと**（`src/visual` がそのまま食える）。"""
    return []


def check_tables() -> None:
    """制度の値と計算の向きを確かめる。**壊れた数字で台本を書かせない。**

    **形は `_checks` が持っています。値は条文を見て自分で書くこと。**
    """
    if PLACEHOLDER in (RATE, CAP):
        raise _checks.TableError(
            "_template.py のまま呼ばれています。制度の値を埋めること")

    # 1. 法令が名指ししている値（`source` に条文名を書く。外れたとき迷わないため）
    _checks.statutory(RATE, 0.05, "＜値の名前＞", source="＜条文・公表資料＞")
    _checks.ratio(RATE, "＜率の名前＞")

    # 2. 速算表があるなら、形と境目の連続はこの1行で済む
    if TAX_TABLE:
        _checks.bracket_table(TAX_TABLE, tax_of, name="＜表の名前＞")

    # 3. 計算の向き（**この計算の主題そのものを書くこと。**汎用の検査では守れない）
    # _checks.increases_with(lambda x: ..., [100, 200, 300], "＜X＞が増えたのに＜Y＞が増えていない")
    # _checks.decreases_with(lambda x: ..., [0.13, 0.15, 0.17], "＜X＞が増えたのに＜Y＞が減っていない")
    # _checks.greater(a, b, "＜大きいはずのもの＞が＜小さいはずのもの＞以下")
    # _checks.rounding(daily(300_000), 6_667, "標準報酬30万円の日額")  ← **円の額**
    # _checks.close(slope(), 183 / 280, "公表されている係数")          ← **率・比・係数**
    #   `rounding` は `!=` で見るので、率には使えません
    #   （`0.15*0.61/0.14` と `183/280` は最後の1桁だけ違う）。
    #   **同じ数を2通りで出して突き合わせる**ときは `close` のほう。
    # _checks.unique_by(ROWS, lambda r: r["name"], "＜表の名前＞")


if __name__ == "__main__":
    check_tables()
    print("制度の値の検査: 通過")

    # **`=== 見出し ===` の節が、そのまま1本の動画のテーマになります**
    # （`scripts/topic_forge.py`）。節を細かく切るほど在庫が増えます。
    print("\n=== ＜この節が1本の動画になる。何を出した表かを書く＞ ===")
    for row in grid():
        print(row)
