# 常駐の「親」セッションID。1行に1つ。#で始まる行は無視。
#
# **これを読む側が3つあります**（増やしたら全部に効きます）:
#   scripts/stop_check.sh     親では黙る（親の出力は「オーナーへの指示」だけなので）
#   scripts/goal_reminder.sh  親では黙る（親は判断しない。文脈を軽く保つ）
#   scripts/run_marker.py     親の印は「周が回った」に数えない
#
# **親は交代します**（重くなったら新しい親へ移す）。**古いIDを消さずに足すこと。**
# 消すと、古い親が生き残っていたときに黙らせられません。
#
# **足すのは、親を交代させた回の仕事です**（2026-08-24 に足した理由）。
# 8/16 以降の親3人が**1人も載っていませんでした** —— 名簿は交代のたびに
# 書き足しが要るのに、引き継ぎの手順書がそれを言っていなかった。
# **載っていない親は、stop フックに毎回引き止められます**（1日19回×永久）。
#
# **そしてもう1つ: 親は `run_marker.py --ship` を打たないこと。**
# 打つと `data/runs.jsonl` にそのIDの ship が残り、
# stop フックの「印の無い回はここまで来ません」という前提が崩れて、
# **名簿に載るまで毎回止まります**（2026-08-24 に実際に起きた）。
# 親が repo を触った回でも、印は打たない。**印は周を回した子のもの**です。
session_01PXy8TiBxL1SM7AUc6XAMML
session_016PyeT6Afj5KzKQ9xkKE3Kx
session_01GUdDmLixcbtycXJ8s7DRqG
session_01XnvQjcpg5pvqbbAM4ejgmH
session_017yMBL2xfhjbZRhb6D646HG
session_01RzmEZySHAZdDMJmD2z9eUb

# ------------------------------------------------------------------
# **2026-08-25 22:0x — この名簿は、もう「周を回した人」を外せません。**
#
# 8/25 の夜に毎時の回が「親が立てる子セッション」から
# **「親セッションの中のサブエージェント」**へ移りました。
# サブは**親と同じ `CLAUDE_CODE_REMOTE_SESSION_ID`** を持ちます
# （環境変数は1コンテナに1つ）。つまり上のIDは
# **親と、その親が立てたサブの両方**を指します。
#
# だから見分けは**作業コピー（worktree）の道**でやります ——
# サブは必ず `…/.claude/worktrees/<名前>` で、親は共有チェックアウトで走る。
# 実装は `scripts/run_marker.py` の `worktree_tag()` / `actor_id()` / `is_parent()`。
#
# **上の「親は `--ship` を打たないこと」は、サブには当てはまりません。**
# サブは打ちます。印には `session_…#agent-xxxx` が残り、
# 親の足切り（`kind == "start"` にだけ効く）を通り抜けます。
# ------------------------------------------------------------------

# ------------------------------------------------------------------
# **2026-09-02 19:2x — いまの親を足した。08/30 の交代から 3日、誰も足していなかった。**
#
# `docs/trigger_spec.json` の `persistent_session_id`（親の心拍が撃ち込む先）。
# コンテナの `CLAUDE_CODE_REMOTE_SESSION_ID` はこの ID で、`data/runs.jsonl` の
# 印は全部 `session_01AHfq4F…#agent-…` の形（＝ サブは同じ ID を持つ）。
#
# **足していなかった代金**（この回に実測。`scripts/stop_check.sh` を上から追った）:
#   親の Stop フックは名簿で黙れず、下の門まで降りる。そして
#   `scripts/relay.py --check` は **必ず 2 を返す**（子セッション時代の受け渡しの
#   台帳 `data/relay.jsonl` は 08/25 で止まっており、いまの親の行は永久に無い）
#   → **(2.0) が親を毎ターン 2回 引き止め**、そのあと **(2) が 5分に1回 引き止める**。
#   引き止められた親は、答えを出すしかない ＝ **「親がベラベラ喋る」の機械の側の原因の1つ**
#   （オーナーが 09/01〜09/02 に 6回 言った当のもの）。
#   `goal_reminder.sh` も同じ名簿を読み、親のターンに毎回 目標を差し込んでいた。
#
# **サブへの副作用**: `run_marker.is_parent()` は worktree の道で見るので、サブの印は
# 今までどおり打てる。`goal_reminder.sh` は ID しか見ないので、サブの PostCompact でも
# 黙る（UserPromptSubmit はサブでは鳴らない）。**目標の本文は CLAUDE.md で毎回 入る**ので、
# 失うのは差し込みの1回ぶんだけ。
#
# **覆る条件（＝ この手直しが要らなくなる形）**: `stop_check.sh` / `goal_reminder.sh` が
# hook の stdin JSON の `cwd` を読み、`.claude/worktrees/` の外なら親として黙るように
# なったら（`run_marker.worktree_tag()` と同じ規則。**ID の直書きより腐らない**）。
# それまでは、**親を交代させた回が、この名簿に1行 足すこと。**
session_01AHfq4FUVAd5fxDM29yG1Cm
# ------------------------------------------------------------------
