# 固定その4 —— **予約はその日のぶんだけ**（2026-09-02。**これも固定です**）

オーナー原文（**一字も変えないこと**）:

> **「現在の日付にしか予約しないってことだからね？」**

**「作り置きなし」の意味が、ここで確定しました。**

    その日の1本を、**その日に**予約する。**先の日付には1本も置かない。**
    先の日付が空であることが、**正しい状態**です。

## これで否定されたもの（**やらないこと**）

- **`reschedule.py --compact` で先の日付へ並べ直す手**。09/02 に「09/03〜09/27 へ
  1日1本ずつ置く」案を親が渡しかけました。**先の日付に置く手なので、この指示に反します。**
- **「暦に穴が空いている」を欠陥として扱うこと。** 穴は正常です。
  `pool_drain` / `slot_gate` / `next_slot` / `deadline_check` の「空の日」の警告は、
  **この指示の下では意味が反転します**（空でないほうが異常）。
- **「前提の判定に要る本数が、いまの暦では何日かかる」という数え方**。
  暦に本を置いて稼ぐのではなく、**毎日1本 出た結果として増えます**。

## いま在るもの（2026-09-02 時点）

    予約に残っている作り置き **106本**（9/24以降に固まっている）→ **全部 外す**
    控えと実物が食い違う **4本** → 実物だけ予約に残っている。**これも外す**
      gmCvNpLfBg4 / I05QbyDSt7k / q4UyLG7_CAE / uo0tBXrDscc

**削除はしません**（固定その2の4「消さなくて良いよ」）。予約を外して非公開のまま置きます。

## **1日の回り方**（2026-09-02・オーナー原文）

> **「その日の投稿の後は次の日の作成になるってわかってるよな？」**

    公開したら → **すぐ次の日の1本を作り始める**
                 → 次の枠まで改善し続ける（固定その2の3）
                 → **その日になったら、その日で予約して出す**（固定その4）

**「その日に予約する」は「その日まで何もしない」ではありません。**
作るのは前の日の公開直後から。**予約だけが当日です。**

**2026-09-02 時点で、これが出来ていませんでした** —— 09/02 13:00 に公開したあと、
09/03 のぶんが**1本も作られていない**まま数時間が流れています（その間の回は
道具の手入れだけ）。**作る手が5択の中で最下位に落ちているのが原因です**
（09/01 の実測: `fix` 82% / `upload` 0件）。

**覆る条件**: オーナーが「先の日付にも置いてよい」と言ったとき。**それまでは、
先の日付への予約は1本も作らないこと。**

---

# 固定その3 —— 出す本の中身（2026-09-02。**これも固定です**）

オーナー原文（**一字も変えないこと**）:

> **「ナレーションの漢字の読み方全部正しくして」**
> **「動画内の説明は人間にわかるようにして」**

**「固定その2」の3（次に出る1本を、出る瞬間まで良くし続ける）の、中身の側です。**
何を良くするのかがここで決まりました。**この2つは `A14` の外側**にあります。

## 1つ目 —— 読み

2026-08-16、オーナーが動画を**耳で聞いて**「額」が「ひたい」と読まれているのを
見つけました。**6日 気づけなかった**のは、こちらの検査に音が1つも無かったからです。
そのとき直したのは**裸の「額」1語だけ**（`src/yomi.py`・`scripts/check_yomi.py`）。

**この指示は「語を1つずつ直す」形をやめろ、という意味です。**
台本に出る**全部の漢字**の読みを、出す前に検算すること。
道具は既に在ります —— `scripts/check_yomi.py <script.json>`（**API キーも音声も要らない**・
open-jtalk の形態素解析で「どう読むつもりか」をテキストで吐かせる）。
**足りないのは、それを全語に当てて、間違いが残っていたら通さない門のほうです。**

**覆る条件**: オーナーが「もういい」と言ったとき。**それまでは、読みの誤りは1つも許されません。**

## 2つ目 —— 分かること

**数字と制度名が正しくても、聞いて分からなければ届いていません。**
いまの検査は「合っているか」しか見ていません ＝ **人に分かるかを誰も見ていない。**

この repo が繰り返し踏んできた形（「言っている所と、している所が別」）と同じです。
**分かるかどうかを機械が見る所を作ること。** どう測るかは、その回が決めてよい。

### **いま在る「その所」は、これです**（2026-09-02 に撃って数えました）

**ゼロから作る前に、在るものを数えること。** 同じオーナー指摘（2026-08-27・原文
「**音声だけで理解できない説明なのに画面はすぐ切り替わるし。**説明を理解するには
かなり視聴者側の推論が必要だと思う」）から、既に2つ 立っています:

    src/verify._check_ear_load     **一息で耳がいくつ数を持たされるか**（上限 5個）
                                   ← **配線ずみ。** `script_writer` の
                                     `{long,short}_script_problems` から毎回 撃たれ、
                                     生成中に3回まで書き直させます
                                     （`verify.run` の門にしていないのは、
                                       誤報が不投稿になるから）
    deixis_count（`scripts/` の下）  **画面を見ないと指示対象が分からない語**
                                   ← **どこからも撃たれていません**（(c) わざと寝かせ）
                                     **道をそのままの形でここに書かないこと** ——
                                     この file は `retro._CALL_RE` の「撃つ側」なので、
                                     道を書いた瞬間に「配線ずみ」に化けます
                                     （2026-09-02 に踏んで、検査2件が落ちました）

**2026-09-02 に撃ち直した2つ**（控え 694本・6,206コマ。**この回に自分で撃った数**）:

    耳の負荷（5個以上） **9.8%** のコマ ／ 5個以上を1つでも持つ本 **15.3%**
                        （1コマあたり 中央値 2.0・平均 2.26・最大 16）
                        ← 08-27 の 13.4%（539本・3,834コマ）から**下がっています**
    画面ごしの指示語    狭い **7.7%** ／ 広い **15.6%** のコマ（本では 21.6% / 33.7%）
                        ← 09-01 の 広い 15.7% から**横ばい**

**この回に撃っていない、写しの数**（**引く前に撃ち直すこと**）:

    コマが読み切れない  ショート 2.27秒/コマ → 88.5%   ← `slide_pace` の A/B が判定中
                        （出どころは `deixis_count.py` の docstring・08-28）

**＝「その所」は `_check_ear_load` です。** ここに無いものを新しく作る前に、
**この3つのどれが足りていないかを先に数えること。**

**ただし `_check_ear_load` が見ているのは「一息の数の個数」1つだけ**で、
「人間にわかる」の全部ではありません。**足りない側を足すのが、次に来る回の仕事です。**

### **足りなかったのは物差しの数ではなく、「人に当てる所」でした**（2026-09-02 に測った）

**上の3つは、どれも台本の側の分布しか出していません** ——
「多いほど分かりにくいはずだ」で置いた閾値で、
**人がどうしたかと突き合わせた回が1度もありませんでした。**
当てる所を作りました:

    python -m src.clarity     ← API 0単位・数秒。**陽性対照つき**
                                （控えの台本 × `data/retention.json` の維持率カーブ）

**陽性対照を先に見ること** —— 「関係がありませんでした」は、計器が死んでいても
同じ字で出ます。この repo は一度それを踏んでいます（`scripts/retention.py` の
`length_of` —— **見出しだけ出して1本も描かずに正常終了**し、7日 気づかれなかった）。
対照は「落ちは前に寄る」で、実測 **rho = -0.448**（69本でも -0.468。**動きません**）。

**実測（113本・689コマ。n=113 の門は |rho| ≧ 0.185）**:

    数の個数 最大（＝ `_check_ear_load` が見ているもの・**配線ずみ**）
                        +0.045 / -0.007 / -0.005   **門に届きません**
    文字/コマ（一息が何文字か）
                        **-0.200 ★** / -0.180 / -0.137   **この repo で初めて門を越えた**

**＝ いま門に立っている物差しは、人の側の数と関係がありません**
（いまの大きさを 5% で拾うには 1,864本 要ります ＝ **実質 到達しません**）。
**関係があったのは「一息が何文字か」のほう**でした。

**それでも、この回に門を入れ替えていません**（★ は3つの出口のうち1つで
やっと越えたところ ＝ `src/alerts.py` の「一覧が当たりを含まないまま育つ」に近い）。
**判定は台帳に置いてあります** —— `config/hypotheses.yaml` の
`clarity_ear_load_sign`（135本・期限 2026-09-25・`clarity_books()` が数えます）。

**次に来た回への1手**: `python scripts/retention.py`（**Data API の日枠を使いません** ——
Analytics は別の枠なので 403 の窓でも撃てます）。この回はこれを撃っただけで
**69本 → 113本**になり、**結論が入れ替わりました**（69本のときは
「配線ずみの物差しだけ向きが逆」に見えていた）。
**有意でない rho から向きを読まないこと。貯めるほうが、考えるより速い。**
**ただし井戸はこの回で尽きています**（2度目は +1本）—— ここから先は公開した本ぶんだけ。

> #### **【2026-09-02 11:4x】その1手は、いま 0本 しか足しません。撃つ前にここを読むこと**
>
> **上の「1手」を撃った回が、そのまま踏みました。** 実測:
>
>     scripts/retention.py   63本 引きに行って **空 61本 ／ エラー 2本 ＝ 0本**
>     貯め                   **132本 のまま**
>     python -m src.clarity  n **113本 のまま**、rho も前の回と **1桁も違わず**
>                            `文字/コマ -0.200 ★`
>
> **「空」はエラーではありません** —— 上流にその本のカーブがまだ無いだけで、
> **撃ち直しても増えません**（`fetch_retention` は**どちらも `[]`** で返すので、
> 呼ぶ側から区別できませんでした。`analytics.RETENTION_ERRORS` で分けてあります）。
> **＝ すぐ上の「井戸は尽きています」は正しく、n が増えるのは公開した本のぶんだけです。**
>
> **そして、それは暦の話になります。** `clarity_books()` が 135本 に届くのは、
> 規則（1本/日）なら 22日 ですが、**予約の実物は今後21日で 20日 が空・0.05本/日**
> —— 本当の日数は **約 462日**。`deadline_check` はこれを「あと22日」と
> 印字していました（**規則を天井としてだけ見て、床として見ていなかった**）。
> いまは実物の暦で押さえます。**＝ 20日 の暦の穴の値段が、この判定では 15か月 です。**
>
> **暦を戻す手**（これが、この判定を待たせている当のもの）:
>
>     python scripts/reschedule.py --compact          # 案だけ・**0単位**
>     python scripts/reschedule.py --compact --apply  # 1本 50単位（日枠が要る）

**覆る条件**: 「分かる」を測る形が、実際には分かりやすさと無関係だと実測で出たとき。
そのときは測り方を変えること —— **指示そのものは固定です。**
（**その「実測で出た」の1件目が上です。** 測り方を変える先は
`src/clarity.MEASURES` に並べてあり、`文字/コマ` が2回 続けて門を越えたら
そちらを `script_writer.{long,short}_script_problems` へ入れること）

> #### **その「2回 続けて」は、目で2回 見ることではありません**（2026-09-02 に配線した）
>
> **★ は n が動かなくても同じ字で出ます。** 上の `-0.200 ★` を2つの回が
> それぞれ見れば「2回 続けて越えた」に見えますが、**観測は1つ**です
> （実際に 09-02 の回が、n=113 のまま同じ数を見て、そう読みかけました）。
>
> **条件は `src/clarity.py` が数えます。読み手が憶えておく必要はありません**:
>
>     data/clarity.jsonl   1周に1行。**n が動いた回だけ**足す
>     clarity.streak(物差し)  n の増えた別々の回で、何回 続けて門を越えているか
>     PROMOTE_STREAK = 2   これに届いた回に、報告が「台本の側へ入れる番です」と印字
>
> **陽性対照が落ちている回は連が切れます**（死んだ計器の ★ は読めない）。
> 検査は `tests/test_clarity_streak.py`。**散文に戻さないこと。**
>
> #### **そして、越えた先が `per_video` とはかぎりません**（同じ回に測った）
>
> `OUTCOMES` は3つとも**維持率**（＝ 入った人が残ったか）です。
> `eta.py` が「引けるのは `per_video` だけ」と名指ししている腕は
> **「何人 来たか」**で、**別の軸**です。当ててみました（`clarity.views_table()`）:
>
>     物差し              維持率(50%地点)     累計再生
>     文字/コマ            **-0.200 ★**      **-0.002**   ← 門（0.185）の 1%
>     指示語（広い）合計       -0.039          -0.169
>     数の個数 最大（配線ずみ）   +0.045          -0.125
>
> **再生の側は 7つとも門に届いていません。** ＝ `文字/コマ` を入れるのは
> **維持率のため**であって、**`per_video` の天井 4,101回 を上げる手ではない**
> 可能性があります。**1点では閉じていません** —— 台帳に置いてあります
> （`config/hypotheses.yaml`・期限 2026-09-02・`kind: now` ＝ **次の回が `verdict` で閉じられます**）。

---

# 固定その2 —— 運転規則（2026-08-31。**目標の本文と並ぶ「固定」です**）

オーナー原文（**一字も変えないこと**）:

> **「動画は1日一本作り置きはなしにして。次の投稿予定までにそこで投稿する動画を改善し続ける。それは固定にして。その上で目標を目指す」**

**「それは固定にして」と言っています。** これまで固定は目標の本文だけでしたが、
**これも固定です。** 下の `A14`「この9件以外は、すべてあなたの考えで変えてよい」の
**外側**にあります。次に来た回が「より速い道が見つかったから」で外してよい対象では
**ありません**。

## 読み方（この4つが規則の中身）

    1. 公開は **1日に1本**。それ以上 出さない
    2. **作り置きをしない** —— 先に何十本も作って予約に積むのをやめる
    3. 次の投稿の枠までの時間は、**その枠で出す1本を改善し続けることに使う**
    4. その上で、目標（YouTube の収益で月20万を最短で）を目指す

**3が、この規則のいちばんの中身です。** いままでは「本数を積む」に時間が流れていました。
これからは「**次に出る1本を、出る瞬間まで良くし続ける**」に流れます。
**`upload` は1日1回しか撃てないので、大半の回は投稿しません。**
**その回の「出したもの」は、次の枠の1本を良くしたことです**
（`docs/trigger_main.md` §4 が定義を書き直してあります）。

## 機械の側（**文書に書くだけにしないこと**）

この repo でいちばん多い壊れ方は「**言っている所と、している所が別**」です。
だから規則は機械の床に入れてあります。**出どころは1か所**です:

    src/house_rule.py  PUBLISH_PER_DAY = 1   ← 上限の唯一の出どころ
      ├ scripts/batch_build.density_cap()    機械が実際に置く上限
      ├ scripts/batch_build main() 0.−1      作り置きの口（--count / --topics）
      └ scripts/eta.PLAN_PUBLISH_PER_DAY     到達日が乗っている本数

    tests/test_house_rule.py  ← **1日に2本以上 置けないこと・原文が repo に在ること**
                                 **戻すにはこの検査を消すしかありません**（diff に出ます）

`src.density_verdict.HOUR_HI`（13本/日）は**測れている帯の上端**であって、
出してよい本数ではありません。**帯は観測、規則は規則。規則が勝ちます。**

**覆る条件: ありません**（オーナーが自分の言葉で外すまで）。

---

# いちばん新しい与件（2026-08-26。**下の 08-08 ブロックより、こちらが後**）

オーナー原文:

> **「目標だけ変えなければいい。それ以外は承認求めないで。
> 前にもずっと言ってるから、これ以降同じこと言わせないようにして。」**

**与件は目標の本文1つだけです。** それ以外に恒久指示はありません ——
手順も規則も頻度も形式も、`main` を進めることも、`CLAUDE.md` を書き換えることも、
**全部こちら側の判断で、承認を求めずにやること。**

**「オーナーの許可がほしい」と書いたら、その時点で間違いです。**
2026-08-25 の親は `main` を進める許可と、この節の並びの確認とで、
**2件をオーナー待ちにしていました。** どちらも待つ必要がなく、
**待っているあいだ、サブは毎周 17日前の作業コピーから始め直していました。**
`docs/GOAL.md` が「人の操作が要る道は、上振れのおまけとして扱い、
計画の柱にしないこと」と言っているのは、この形のことです。

**下の 08-08 ブロックが「A1〜A14」と番号を振っているのは、
当時の書き取りであって、いま守るべき一覧ではありません。**
オーナーは 08-15 に「恒久指示とかないよ。目標だけ」と言い直し、
08-26 にもう一度同じことを言っています。**同じことを3回言わせています。**
**次にここを読む側は、二度と確認を上げないこと。**

**唯一やらないのは、目標の本文を書き換えることだけです。**

---

<!-- PERMANENT-DIRECTIVE:BEGIN -->
<!-- version=2026-08-08.user-verbatim.3 sha256=8035fb0dfcaf3b3574a1e13b25883ba24048b472d693a6fe20b20ba1b1a43157 -->
<!-- Generated by note/scripts/propagate_directive.py. Do not hand-edit; edit the
     canonical copy in bachikoljunior-blip/note and re-run the script. -->

# オーナーの恒久指示（原文・全リポジトリ共通）

このブロックは `bachikoljunior-blip/note` の `OPERATIONS/CORE_DIRECTIVE.md` の原文です。
版 `2026-08-08.user-verbatim.3` / sha256 `8035fb0dfcaf3b3574a1e13b25883ba24048b472d693a6fe20b20ba1b1a43157`。
**要約・補足・善意の修正をしないこと。** 下のファイル固有の指示より、こちらが優先します。

```
A1目標は最短で月収20万を達成することです。最短とは、原理的に最大の理論値で、
その理論値は空想のものであり、つねに発見、達成はできていないものと考えられます。
その上で最短を目標としてください。それ以外の制約は一切ありません。あなたが
できることは全て行っていいです。私側への指示をしてもいいですが、それを私が
必ず読むとは限りません

A2 simpleのメインは変更しないで

A4 恒久指示を忘れないように設計すること。

A9 終わるときに毎回「終わるのが最善か」を考えるように設計すること。

A10 自動実行は永久に止まることがないように設計すること

A12 セッションが長くて判断が雑になるのを解決すること。
    それを理由に終わらない。

A13 あらゆる手段を実行してよい。
    これは許可ではなく指示。承認を待たない。手段を狭く見積もらない。

A14 この9件以外は、すべてあなたの考えで変えてよい。
    「昔オーナーがそう言ったから」は理由にならない。
    ここに無いものをここにあるように扱うのが、一番よくある壊れ方。

以上とは別に、クロードのサブスクの使用量をあなた自身が見られる状態であることと、bachikoljunior@gmail.comのチャットgptをあなたが使えること、少なくともチャットgptがチャットgptの使用量も見れること、どちらの使用量もチャットgptsparkの使用量も全てあなたが使っていいこと、simpleのリポジトリのメイン以外の全てのリポジトリの内容は全て変えていいことを知っておいてください。
```

<!-- PERMANENT-DIRECTIVE:END -->

## A1 の現行の読み方（2026-08-15 のオーナー指示で更新）

上のブロックは 2026-08-08 版です。**A1 だけ、その後オーナーが言い直しています**
（`docs/JOURNAL.md` 2026-08-15 09:0x、原文「目標は、YouTubeの収益月収20万を最短でに書き直して」）。

> 目標は**YouTubeの収益**で月収20万を最短で達成することです。最短とは、原理的に最大の理論値で、
> その理論値は空想のものであり、つねに発見、達成はできていないものと考えられます。
> その上で最短を目標としてください。それ以外の制約は一切ありません。あなたが
> できることは全て行っていいです。私側への指示をしてもいいですが、それを私が
> 必ず読むとは限りません

**新しいほうが効きます。** 稼ぐ場所は YouTube に閉じますが、
**その中の何で稼ぐかは開いています**（広告・メンバーシップ・Super Thanks・企業案件）。

> **【この2つが食い違って見えたら】** 2026-08-08 の A1 は「月収20万」だけを言っており、
> 2026-08-15 はその**出どころを YouTube に絞った**ものです。矛盾ではなく上書きです。
> **どちらも消さないこと。** 一方だけを残した回が 8/25 に発生し、
> 片方の枝でもう一方が失われかけました（`docs/JOURNAL.md` 2026-08-25）。

**変えないのは、これが判断の基準そのものだからです。**
基準を動かせば、何が速いかを測る物差しが無くなります。
**基準の外に「聖域」はありません**（A14 がそう言っています）。

- **手順・規則・頻度・形式・チャンネル・言語・収益の立て方は、全部あなたが決めます。**
  より速い道が見つかったら書き換えてください（理由を `docs/JOURNAL.md` に）
- **「昔そう決まったから」は理由になりません**（A14 の原文）。
  いまの判断とその根拠は `docs/CONSTRAINTS.md` にあります。
  **続けるかどうかは、そのつど目標に照らして決めること**
- **承認は待たない**（A13）。目標本文が「私が必ず読むとは限らない」と言っています

## 目標の本文のすぐ後ろ —— **2026-08-31 に固定が1つ増えました**

オーナー原文（**一字も変えないこと**）:

> **「動画は1日一本作り置きはなしにして。次の投稿予定までにそこで投稿する動画を改善し続ける。それは固定にして。その上で目標を目指す」**

**これも固定です。** 上の「手順・規則・頻度・形式は全部あなたが決めます」は、
**この規則の外側には及びません** —— `A14` の「それ以外は変えてよい」の**外**です。
読み方・機械の床・検査は、**このファイルのいちばん上のブロック**に書いてあります
（`src/house_rule.py` / `tests/test_house_rule.py`）。

**「より速い道が見つかったから」で外さないこと。**


## 使ってよい資源（節約対象ではない。ただし**速さ**には注意）

**残すこと自体に価値はありません。** 予算を理由に作業を見送らないこと。
ただし先に使い切ると、リセットまで1回も起きられません（＝投稿と判定が止まる）。
**読むのは残量ではなく「尽きる時刻」**です（`docs/TRIGGER.md`）。

    list_sessions limit=25 mine=true          ← MCP。返りをまるごと保存する
    python scripts/quota.py --ingest <file>   ← 積んで、いまの姿を出す

**オーナーの使用状況の画面が来ていたら、まずそれを積むこと**（指示 2026-08-19 21:2x
「毎回やるようにして」）。**%はこの機械からは読めません**（`rate_limit_info` は
`allowed`/`warning`/`rejected` だけ）。**唯一の目盛りがそれです。**

    python scripts/quota.py --gauge <週%> --at "MM/DD HH:MM" [--session <%> --session-in <分>]

放っておくと古くなります —— 実測で **3.3日ぶん 22% のまま**走り、
実際は **75%** でした（**持続できる間隔 41分 → 65分**）。

**`show-usage.mjs` と `ask-chatgpt.mjs` はもう動きません**
（`-chatgpt-usage-monitorPrivate` の OAuth が 2026-08-11 に切れ、
向こうの GitHub Actions も 8/12 から停止）。**直すには人手が要るので、
直る前提で設計しないこと。**

---

# このリポジトリで働くときの前提

このファイルはセッション開始時に自動で読み込まれます。会話が伸びて文脈が
要約されても、ここは毎回入り直します。**判断に迷ったらここに戻ってください。**

---

> **判断のたびに [`docs/GOAL.md`](docs/GOAL.md) の5つの確認項目に答えてください。**
> 目標を覚えているだけでは足りません。過去に2回、覚えていたのに適用しないまま
> 進み、あとから直しています（`docs/JOURNAL.md` 2026-08-04）。

> **いまの判断とその根拠は [`docs/CONSTRAINTS.md`](docs/CONSTRAINTS.md) にあります。**
> **全部あなたが変えてよいものです。** 根拠と「覆る条件」を必ず併記すること。
> 理由の書いていない規則は、次に来た側が判断できず惰性で残ります。

> **この回は、何か1つ出してから終わること。** 分析と日誌だけで終えない。
> `data/runs.jsonl` の直近10回がそれで、そのあいだ在庫は減り続けていました。
> 最低ラインは [`docs/trigger_main.md`](docs/trigger_main.md) §4。


## 目標から出てくる、動き方の帰結

**1. いまのやり方が最短だと思わないこと。**

「最短」は理論値であり、まだ誰も見つけていないものとして扱われています。つまり
**現在の構成（日本語・長尺・お金と仕事の解説・図解と合成音声）が最善である保証は
どこにもありません。** これは出発点であって、答えではない。

毎回、実績を見て「この道でいいのか」を疑ってください。ニッチ・尺・形式・投稿頻度・
チャンネルそのものまで、変えてよい対象です。数字が伸びないのに同じことを続けるのは、
目標に反します。

**2. 人間の作業に依存する計画を立てないこと。**

人への指示は出して構いませんが、**読まれない前提で設計してください。**
「〜してもらえば伸びる」という計画は、実行されなければゼロです。承認待ちで
止まらないこと。自分で完結できる道を先に選び、人手が要ることは「やってくれれば
上振れする」おまけとして扱ってください。

**3. 制約は無い。あらゆる手段を実行してよい**（目標本文「それ以外の制約は一切ありません」）。

やってよいことの範囲は、あなたが技術的にできることの全部です。コードも設定も
テーマも書き換えてよい。新しい仕組みを作ってよい。既存の設計（このリポジトリの
実装を含む）は、より速い道が見つかったら捨ててください。

**これは許可ではなく指示です。「やってよい」ではなく「やれ」と読むこと。**
承認を待たない。手段を狭く見積もらない。「たぶん許されない気がする」で
候補から外さない（外すなら理由を数字か事実で言う）。

**手段として成立しないのは2つだけ**で、これは制約ではなく**目標を壊すので効かない**もの:
再生数や登録者の購入・水増し・なりすまし・虚偽の数字（**見つかればチャンネルごと失う**。
収益化されなければ収入はゼロなので期待値がマイナス）と、法に触れること。
**この2つ以外で手を止めていたら、それは目標に反しています**（`docs/CONSTRAINTS.md`）。

**4. 投稿を途切れさせないこと。ただし常時稼働はしない。**

投稿が途切れるのが最大の損失です。1本落とすより、質を下げてでも出すほうが
目標に近い場面が多い。ただし壊れた動画や誤情報は逆効果なので、投稿前の検査
（`src/verify.py`）は迂回しないこと。

**定期実行は止めないこと。** 止まれば投稿が途切れ、目標から遠ざかります。
設定の正本は `docs/TRIGGER.md`。壊れたらそこから復元してください。

**間隔も1回の中身も、全部こちらの判断です。遠慮せず変えてよい。**
**実物は必ず `list_triggers` で見ること。** 文書に写した瞬間に古くなります
（毎時 → 6h → 8h → 12h → 毎時 と動いてきました）。
**間隔は「このループの1周の重さ」で決めます。**
**アカウント全体の使用量から決めないこと** —— 他の運転が混ざるので、
こちらの1周がいくらかを言っていません（`docs/TRIGGER.md`「切り分けて測る」）。

1回の起動でやることは、次のとおりです。

- **分析は毎回やる。** `python scripts/status.py` を読み、次に何を変えるかを決めて
  `docs/JOURNAL.md` に書く
- **そのうえで、最低1件は「出して」から終わる。**
  出したものは `python scripts/run_marker.py --ship "<1行>" --kind <種別> --lever <腕> --moves <日数>`
  で記録する（**`--lever` と `--moves` は必須**。どの腕を動かし、予測日を何日
  動かす見込みか。上の「毎回の実行で必ずやること」2と5）。
  何を出したと呼ぶかは `docs/trigger_main.md` §4:

      upload   動画を1本。**予約まで入れる**（**1日1本まで** ＝ 規則1）
      improve  **次の枠で出る1本を、実際に良くする**（規則3。**物が変わるまで**。
               **中身の側と配信の側の両方に道があります** —— 配信のほうが
               当たり **27% 対 8%（10.3倍）**。§4 に表）
      means    `docs/MEANS.md` の未着手を1件、実際に動かす
      verdict  期限の来た前提を、実データで判定する
      premise  **縛っている腕の天井を疑う前提を1件 立てる**（0単位・いつでも撃てる）
      fix      実測で見つかった欠陥を1つ塞ぐ

  **`premise` は 2026-09-01 に6択目として足しました。ここの5択が古かった。**
  `eta.py` は毎周「**軌跡の腕が動くのは、前提を1件 閉じたときだけ**」と印字します
  —— **台帳に注ぎ口が無ければ、到達日は永久に動きません。**
  実測（`data/runs.jsonl`・08-31 の規則以降の ship 133件）: **fix 109件（82%）**／
  verdict 11／improve 8／premise 3／upload 1、**`--moves` の合計 0日**。
  **`fix` は `eta.py` が毎周 36件 名指しし、`premise` と `improve` は自分で探す** ——
  **同じ選択肢に並べても、探す手間が違えば選ばれません。**

- **出したら、その回のうちに予測へ入れ直す**（オーナー指示 2026-08-20
  「毎回その予測に反映して」）。`--ship` が `scripts/eta.py --reflect` を
  **既定で呼びます**。残るのは「この回で動いた入力 → 日付 前 → 後（±N日）」の1行
  （`data/eta.jsonl` の `kind="reflect"`）。**下の「毎回の実行で必ずやること」6**

**「分析して、今日は変えないと決めて終わる」は、もう既定にしないこと。**
`data/runs.jsonl` の直近10回は**全部それ**で、そのあいだ在庫は減り続けていました。
本当に出せない回は、**理由を JOURNAL に書いてから**終わること。

**常時稼働はしません**（1回で完結させる）。ただしこれは「小さく終える」ことでは
ありません。**起きている時間ではなく、出したもので測ること。**

**そして、終わるときは毎回「終わるのが最善か」を考えてください。**
`scripts/stop_check.sh` が Stop フックとして割り込みます。
**これは「終わるな」ではありません。** 問うているのは
「終わるのが最善かを考えたか」です。**このフックを消さないこと。**

**5. 使用量は目標のためにあります。残すこと自体に価値はありません。**

「使いすぎないように絞る」は理由になりません。やるべき作業があるなら、
予算を理由に見送らないこと。8/6〜8/8 は「予算が」と言って何度も見送りました。
**あれは不要でした。**

**ただし「全部使ってよい」は「どれだけ速く使ってもよい」ではありません。**
先に使い切ると、リセットまで1回も起きられません。**読むのは残量ではなく
「尽きる時刻」**です（`docs/TRIGGER.md`「間隔を変える判断」）。
足りないときに削るのは**1回の中身ではなく間隔のほう**。

    list_sessions limit=25 mine=true          ← MCP。返りをまるごと保存する
    python scripts/quota.py --ingest <file>

**`scripts/usage.py` は使用量の測定ではありません。** あれが出すのは
セッション数と経過時間から作った推定で、**実際の枠とは何の関係もありません。**
残量を根拠に何かを決めるとき、あの数字を使わないこと。**`show-usage.mjs` / `ask-chatgpt.mjs` も動きません**
（OAuth が 8/11 に切れ、人手でしか直せない ＝ 直る前提で設計しない）。

そして**予約が埋まっていれば、起きなくても投稿は公開されます。**
予約が切れそうなときは、他を後回しにしてでも生成に使うこと。

**6. YouTube のポリシーは「制約」ではなく「事実」として扱う。**

誤解を与える内容、量産された無個性コンテンツと判定される作り方は、収益化そのものを
失わせます。守るのは規則だからではなく、**破ると目標に到達できなくなるから**です。
特に、制度や金額には必ず適用条件を添えること。裏の取れない数字は動画に入れないこと。

---

## この作りの根幹（2026-08-04に方針を変えた。理由ごと読むこと）

YouTube のチャンネル収益化ポリシーは、次のものを**収益化の対象外**と明記しています。

> クリエイターならではの、率直な洞察や視点が追加されておらず、汎用的または独創性のない
> テンプレートを使用して作成され、大量生産されたような印象を与える AI 生成コンテンツ

> テンプレートを使用して作成されたと思われるコンテンツや、同じチャンネルの動画を
> 続けて数本視聴した後、繰り返しのように感じられる可能性のあるコンテンツ

> ウェブサイトやニュース フィードのテキストなど、自分で作成していない他の資料の内容を
> 読み上げただけのコンテンツ

**収益化されなければ、RPM がいくつでも収入はゼロです。** だからこれは「リスク」ではなく、
到達可能性そのものの条件です。

> ### **【2026-08-31: いま止まっていません。機械は、自分で自分を止められません】**
>
> **オーナー原文（2026-08-31・2つ）**:
>
> > **「だから俺はそんなの決めてないから、勝手にそれで止まるのなし。
> >    今後そういうことがないようにして」**
> > **「既存のもの外すだけじゃなくて同じことが起こらないようにして」**
>
> **何が起きたか（事実だけ）**: 2026-08-30、`AUTOMATION_PAUSED.md` という
> **ファイル1枚**が置かれ、生成・投稿・予約の変更が全部 止まりました。
> 判定は `src/pause_guard.PAUSE_FILE.is_file()` **だけ** ——
> **その1枚は、この repo のどの回でも書けます。** つまり機械が、自分の判断で
> 自分を丸ごと止められる状態でした。**約22時間・4周ぶんの生成が落ちています。**
> 2026-08-31、**オーナー本人が GitHub の画面からそのファイルを削除しました**
> （commit `1aa1e65a`）。
>
> **いまの作りは、こうです**:
>
>     止まるのは          `.owner-pause` が repo の根に在るときだけ
>     その印を置けるのは  **人だけ**（この repo のコードは1行も作りません）
>     判定は              `src/pause_guard.is_paused()` の**1か所**
>                         （`resume_gate`・`policy_pause.sh`・`spawn_prompt.py`
>                           は、そこへ聞くだけ。写しを持たないこと）
>     検査                `tests/test_pause_needs_owner.py`（10件）
>     作りの床6件         `docs/RESUME_GATE.md`（旧 Resume gate）。
>                         **あれも switch ではありません** —— 在っても
>                         無くても1ミリも止まりません。正本は
>                         `data/resume_gate.jsonl`・`eta.py --gate` で読む
>
> **次にここを読む側への3つ**:
>
>     1. **止める仕掛けを足さないこと。** 「安全のために1回止める」も
>        「文書1枚で塞ぐ」も、**それが「勝手に止まる」ことそのもの**です。
>        `.owner-pause` を作るコードを書いたら、検査が落ちます
>     2. **作りに問題を見つけたら、止めるのではなく直すこと。**
>        止めても審査は通りません。**直せば通ります**（下がその実例です ——
>        名乗りは `config/channel.yaml` と `src/verify.py` の2手で外れました）
>     3. **下の節の中身（ポリシーの話）は、いまも正しい。** 変わったのは
>        「だから全部 止める」という結論のほうだけです
>
> **覆る条件**: オーナーが「止めろ」と言ったとき。**そのときは `.owner-pause` を
> 人の手で置きます。機械が置いてはいけません。**
>
> ---
>
> **以下は 08-30 に止めた理由です。ポリシーの話として、いまも正しい。**
>
> **止めた理由は、この節がいま言っていることそのものです。**
> `config/channel.yaml` の `persona` は
> 「**元・事業会社の経理／人事で、制度を実務で回してきた立場**」を名乗って**いました**。
> **そんな人はいません。** 合成音声が、型どおりの生成で、
> 税・保険・キャリアという**視聴者が自分の金で動く題**を、
> **実在しない人間の実務経歴を根拠に**話している形です。
>
> **これは上の「手段として成立しない2つ」の片方、なりすまし に当たります。**
> 目標の側から言えば —— YPP は AI ペルソナが専門家を装って
> 金融・法制度を扱うことを収益化の除外側に置いています。
> **除外側に当たれば、再生が何万あっても 1円にもなりません。**
> 165本・28日で7.5万再生を積んでも、審査で落ちれば**全部ゼロ**です。
> **止めているのは、遠回りだからではなく、その道の先に収入が無いからです。**
>
> #### **その名乗りは 2026-08-30 に落としました（Resume gate の 1・2 が閉じています）**
>
> **入口**（`config/channel.yaml` の `persona`）から経歴を落とし、
> **出口**（`src/verify._check_no_human_expert_claim()`）にも同じ門を置きました。
> 出口は `script_only_problems()` に入っているので、**クリップを焼く前**に当たります
> —— `persona` は指示文の一部でしかなく、書き手はそこに無い経歴を自分で足せるので、
> **入口だけを塞ぐと、次に人格を書き換えた回が黙って穴を開け直せます。**
> 検査は `tests/test_no_human_expert_claim.py`（20件・投稿済み735本の題で偽陽性 0）。
>
> **6件とも閉じました（2026-08-30 15:4x に 4番 が閉じ、6/6）**（**正本は
> `python scripts/eta.py --gate`。この行は写しなので、必ず撃って確かめること**）。
> **1〜6 をもう一度やらないこと** —— 根拠は `data/resume_gate.jsonl` と
> この節にあります（条件の本文は `docs/RESUME_GATE.md`。**あれは switch ではありません**）。
>
> #### **【決着済み】ここで機械は「外してよいか」を待っていました。オーナーが答えました**
>
> **08/30 の回は、次の3つを理由に停止のファイルを残しました。**
> **その問い自体は真っ当でしたが、答えはもう出ています** —— 2026-08-31、
> **オーナーが自分でファイルを消し、「勝手にそれで止まるのなし」と言いました。**
> **3つを根拠に、もう一度 止めないこと。** 記録として残します。
>
>     1. 6件を閉じたのは**全部こちら側の自己申告**。原文は「次の全条件が
>        **記録される**まで解除しない」＝ **必要条件**であって、
>        「記録した＝外してよい」とはどこにも書かれていない
>     2. 停止の8件は**人の手で `origin/main` へ直接 入っている**
>        （08-30 08:54〜08:56・英語・`Co-Authored-By` も `Claude-Session` も無い ＝
>          この repo の自動の回の形と、どれも一致しない）。
>        **自分で閉じて自分で外す**のは、その介入の意味を消す
>     3. **機械の上限は、まだ1度も走っていない。** `batch_build.py` は
>        `pause_guard.BLOCKED_ENTRYPOINTS` に載っていて、停止中は
>        **`--pick-only` すら撃てない**（迂回しないこと）。効いているのは
>        `tests/test_density_cap.py` の10件だけで、`main()` を通した実行は **0回**
>
> **その1手は 2026-08-31 にオーナーが撃ちました。**
> **最初の1周で `python -m src.density_verdict` と、20本 出したところで
> `python -m src.frames` を撃ち直すこと** —— **閉じた根拠は上限であって、
> 出来上がりの実測ではありません。** どちらかが外れたら、**その場で門を開き直して
> 直すこと**（`python scripts/eta.py --open-gate <番号> --evidence "<何を測って、
> どこと食い違ったか>"`）。**全体を止めないこと** —— 外れているのは1件で、
> 止まるのは全部です。**釣り合っていません。**
>
> **3番（本ごとの差）は 2026-08-30 夜に閉じました。** 量産の印は中身ではなく
> **枠**にあり（長尺134本の 84% が同じ4文字で始まり、61% が同じ4文字で終わる ＝
> 入り方で言えば **実効 2.4本ぶん**。`python -m src.frames`）、
> **原因は書き手の癖ではなく `ROLE` への直書き**でした。
> 入口（`script_writer.opening_form()` / `closing_form()` の4×4）と
> 出口（`verify._check_frame_repeat()`）で、**1つの型に揃う本は多くても 30.1%** に。
> **ただし言えるのは上限で、出来上がりの実測ではありません** ——
> **再開後 20本 で `python -m src.frames` を撃ち直すこと。**
>
> #### **同じ日に公式ポリシーへ当て直したら、縛っている条項が入れ替わりました**
>
> 当たる条項は**2つ**あります（出典と原文は `AUTOMATION_PAUSED.md` の「進捗」4）。
>
>     (A) AI ペルソナ／センシティブな題 …… 原文は
>         "presents itself as a **human expert providing advice**" という条件つき。
>         **禁じているのは題ではなく、名乗りと助言のほう。** → 上の2手で外側に出た
>     (B) 汎用・反復 …… "AI-generated content made with generic or unoriginal
>         templates **giving the impression of mass production**"
>
> **いま縛っているのは (B) です。** 735本・1日6〜16本・同じ台本の型・同じ図の作り・
> 同じ合成音声 —— **正面から当たります。**
>
> **そのうち「同じ台本の型」は 2026-08-30 夜に閉じました**（上の3番）。
> **残っているのは「量そのもの」です。** 同じ日に測ったら、
> **ポリシーの側と目標の側が、はじめて同じ数字を指しました**
> （`python -m src.density_verdict`）:
>
>     詰めた日（1日16本以上）    1本あたり再生の中央値 **2回**（5日・119本）
>     1時間きざみの日（8〜13本） 1本あたり再生の中央値 **716回**（4日・42本）
>     倍率 **0.003** → 前提「1時間より詰めても落ちない」は **falsified**
>
> **`eta.py` の `PLAN_PUBLISH_PER_DAY` は 25 でした** ——
> 段1〜4 が全部その上に乗っており、**外れた帯の本数で到達日が出ていました。**
> **25 → 13**（測れている帯の上端）へ落としてあります。
>
> **そして 2026-08-30 15:4x に、その 13 が機械の側へ入りました**（4番が閉じた手）。
> `scripts/batch_build.cap_by_density()` が `slots()` の**出口**（`main()` の 0.7）で
> **生成の前**に当てます —— 上限を超えた本は1秒も作りません（題材は在庫に残る）。
>
> **なぜ出口だったか。** 既にあった `_per_day_soft()` は `_band_walk()` の
> **帯の中しか数えません**（`busy & set(grid)` ＝ 09:00〜13:30 の枠）。素通りは4本 ——
> 長尺の `ring`（20:00〜）／`--hours` の明示／`--step-min 30` の `_slots_fine`／
> **同じ日に2回 走った回**。**19〜22本/日 は、この4つの足し算**でした。
> **上限の出どころは1か所**（`src.density_verdict.HOUR_HI`）で、
> `PLAN_PUBLISH_PER_DAY` と同じ所を読んでいるかを
> `tests/test_density_cap.py`（10件）が見ます —— **ずれた瞬間に赤くなります。**
>
> **文書に3か所 書いてあっても、機械は 08/27 に 19本・08/28 に 22本 置いていました。**
> **この repo の一番よくある壊れ方はこれです**（言っている所と、している所が別）。
>
> **`eta.py` の到達日は、この審査に受かる確率を 1.0 に置いたまま出ています**
> （「推測」2番は審査の**日数**しか推測しておらず、**落ちる目を持っていません**）。
> **だから、止まっている間の到達日は上振れです。** `headline()` がそう印字します。
>
> #### **これは「止まれ」ではありません。「通る形に作り替えろ」です。**
>
> **この節の残り（自分で計算した結果を発表する）は、いまも正しい方向です。**
> 欠けているのは**名乗り**のほうで、そこは直せます ——
> 計算と前提を全部画面に出す作りは、**架空の経歴を必要としません。**
> 「元・経理」を名乗るのをやめても、`src/calc/` が出す数字は1つも減りません。
> **落とすべきは経歴であって、企画ではありません。**
>
> **覆る条件（`docs/RESUME_GATE.md` の6件）**: 実在しない人間を
> 名乗らない・専門家を装わない・本ごとに実質的な差がある・
> いまの公式ポリシーに当てて見直す・既存165本の扱いを決める・
> 到達の計算をやり直す。**6件とも記録済みで、停止はオーナーが解いています。**
>
> **ただし、この6件の中身は「止める理由」ではなく「作りの床」です。**
> 「承認は待たない」「聖域はない」は、**この repo の決めごとを外す許可であって、
> 視聴者に嘘の経歴を出す許可ではありません。** 名乗りを戻したら、
> 審査は受かりません —— **そのときに止めるのは機械ではなく、
> `src/verify._check_no_human_expert_claim()` が1本ずつ落とすほうです。**
> **全体を止める仕掛けを作らないこと**（`tests/test_pause_needs_owner.py`）。

したがって、この作りの根幹は次の一点です。

> **制度を解説するのではなく、自分で計算した結果を発表する。**

制度のまとめ直しは、どれだけ丁寧でも「自分で作成していない資料の読み上げ」の側に落ちます。
代わりに毎回 `src/calc/` に計算を実装し、**どこにも公開されていない数字**を出す。
**前提と計算式を、画面と説明欄に全部出す。** 前提の置き方そのものが「独自の視点」であり、
視聴者が自分で追試できることがテンプレート量産との違いになります。

> **このリポジトリの存在を、動画・説明欄・コメント・タイトルのどこにも出さないこと。**
> リンクも、名前も、匂わせも書かない。検証可能性は、前提と計算式を画面に出すことで
> 担保します。それで十分に成立します。
>
> **目標側の理由**: 自動生成の出どころを自分から明かすことは、
> 「大量生産されたような印象」の判定材料をこちらから提供する行為です。
> 収益化されなければ収入はゼロなので、期待値が明確にマイナスになります。

あわせて、**同じ絵を続けないこと**。図解の配色は**本ごとに1つずつ送る番号**で回り、
`kind=chart` の棒の長さは計算結果そのものなので、回ごとに図の形が変わります。
1本に chart を最低3枚。ここを崩すと、上の2つ目に該当します。

> **【2026-08-28 に、この2行が実装と食い違っていました】**
> ここには長らく「配色は**テーマIDから**変わるようにしてあり」と書いてありました。
> **それは `visuals.theme_for()` の、index を渡さない枝の話**です。
> `src/pipeline.py` は index を渡し、渡すと**あの関数はテーマIDを見ません**。
> そして渡していた index は「チャンネルの投稿済み本数」で、
> `batch_build` は `--dry-run` ＋並列なので**その回のあいだ定数** ——
> **1回で作った本は、全部おなじ配色**でした
> （実測: 08/27 21:56 の **8本すべて** 同じ色。ほか5つの回も同じ）。
> **文書のほうが正しくて、実装が外れていた**のではありません。
> **文書は、使われていない枝を説明していました。**
> いまは `batch_build.theme_base()` が起点を1回だけ読み、本ごとに +1 します。
> **覆る条件と検査は `tests/test_theme_index_spread.py`。**

**ただしこれも出発点です。** これが最善である保証はありません。ポリシーは事実として
守る必要がありますが、守り方は他にもあり得ます（自分の声、実画面の収録、別の形式）。
より速い道が見つかったら、この節ごと書き換えてください。

---

## いま把握しておくべき数字

- 収益 ＝ 月間再生数 ÷ 1000 × RPM。**RPM はニッチで10倍以上変わる**（`docs/STRATEGY.md`）
- 月20万円は、RPM ¥1,000 なら **月20万再生**。RPM が上がれば必要再生数はその分減る
- その前に収益化の門：**登録者1,000人 ＋ 直近12か月の総再生4,000時間**
- 4,000時間は、10分動画・維持率40% なら **約6万再生**

この4行は、判断のたびに掛け算し直してください。「本数を増やす」より
「RPM の高いニッチに動く」ほうが速いことがあります。

---

## 毎回の実行で必ずやること

**1と2は、この回のいちばん最初にやること**（オーナー指示。**同じ趣旨で3回**:
2026-08-19 21:2x／2026-08-20 06:2x／2026-08-20 08:0x）:

> **「毎回達成までの予測して。20万の達成。それ以外のやつだけしかしてない。
> それを早めるための行動考えてから進めるのは毎回の最初にやること」**

> **「20万達成までのプランを作って達成日時を予測して、
> 毎回達成日時を早めることを考えてから進めるようにして」**（3回目）

> **「勝手に20万達成以外の日時の予測だけにしないで」**（同 追記）

1. **`python scripts/eta.py` —— 月20万に届く日を出す**（**理由なく削除しないこと**）。
   **読むのは、出力の最初と最後に同じ字で出る3行だけ**です。真ん中は200行あり、
   その症状が出た回だけ読めば足ります。

       ### **月20万の到達予測: 2027-01-22**（155日後）
       ### 縛っているのは **収益化の門＋その後の30日** → **この回に引く腕は `density`**
       ### 前の回の予測 2027-01-22 → **+0日** 動いていません

   **到達日は、3つの床のいちばん遅いもの**です。

       (a) 直近30日の再生が、月に要る回数に達する日   ← 実測の伸び率で解く
       (b) その30日が**まるごと収益化の後**にある日   ← 収益化前の再生は1円も生まない
       (c) 合格点の倍率を**確かめ終えている**日       ← 別の形の実測を当てている間

   **(a) に (b) を足さないこと。** (a) は直近30日の合計で見ているので、
   足すと1か月ぶん二重に数えます。

   **2026-08-20 まで、段4 の期日は `d_target = d_monetized` の1行**でした ——
   印字は「月20万の到達見込み」でも、**中身は収益化の審査が終わる日**です。
   写しである以上、per_video を10倍にしても RPM を5倍にしても**日付は1日も動きません**。
   同じ日に2つの回が別々にここへ着き、**(b) を入れた側と (a) を解いた側**を合流させました。
   **写しに戻ったら `tests/test_eta.py` と `tests/test_eta_target_date.py` が落ちます。**

   **届かないときも「届きません」で終わりません**（オーナー指示 2026-08-20 06:2x）。
   末尾に「**何を何倍にすれば、いつ届くか**」が日付つきで出ます
   （例: 2026-11-18 まで ＝ 1日 +5.22% の伸び）。天井そのものが足りない帯なら、
   **待っても届かない**と言い、要る倍率を出します。

   **さらに 2026-08-21 21:3x、オーナーから同じ話の続きが来ました**（原文）:

   > **「実行進めた時の達成までの軌跡を予測するんだから、届かないって言ったら、
   > このプロジェクトをどれだけ進めても一生届かないって予測してることになるけど。
   > それだったら逆に達成するために考えないといけないし、届かないって言ってるのが
   > 特定の条件決めた上で言ってるだけなら、このプロジェクトが進んでいく軌跡を
   > 予測しないとダメだよ。前からそういうことを言ってるんだけど。」**

   **いまの `eta.py` は、天井（1本あたり再生・密度・RPM）を今日の実測で固定したまま
   未来へ延ばします。** だからそこから出る「届きません」は、
   **「この先この企画が1ミリも良くならなかったら届かない」**という意味しかありません。
   **それは予測ではなく、凍らせた企画についての恒真式です。**
   オーナーが言っているのはそこで、**要るのは「進んでいく軌跡」のほう**です。

   **次にこの道具を触る回がやること（どちらかを必ず満たすこと）**:

       (ア) 天井の入力を時間の関数にする。1回の ship でどれだけ動いたかは
            すでに測っている（`data/runs.jsonl` の `--lever/--moves`、
            `data/eta.jsonl` の `kind="reflect"` の「前 → 後」、`levers.reconcile`）。
            **その実測の改善速度で per_video / rpm / density を伸ばした軌跡**で解く
       (イ) それが引けないうちは、**「届きません」と印字するたびに、
            何を固定したせいでそう出たのかを同じ行に並べる**
            （例: per_video=X・density=Y・RPM=Z を今日のまま固定した場合）。
            **裸の「届きません」を出さないこと。**

   **(ア) を出すまでは (イ) が最低ライン**です。**「特定の条件で言っているだけ」なら、
   その条件を書けば済む** —— 書かずに「届きません」だけを出すのが、
   オーナーの言う「一生届かないと予測している」に読める形です。
2. **その予測を早める腕を、先に1つ選ぶ。** 既定は**上の3行が名指ししている腕**です
   （`lever_hint`。縛っている床が変われば、名指しも変わります）。
   別の腕を選ぶのは自由ですが、**理由を JOURNAL に1行**書くこと。

       per_video  1本あたりの再生を上げる      rpm       RPM（ニッチ・尺・形式）
       density    公開の密度を上げる          sub_rate  登録率を上げる
       none       この回は日付を動かさない（道具・手順の整備。**理由を JOURNAL に**）

   **選んでから作業を決めること。** 順番が逆になると、やれることの中から
   選んで後から理由を付けることになります。**2026-08-19 の実測で、
   直近10回の ship は「動かす腕」を1件も選んでいませんでした。**
3. `docs/JOURNAL.md` の末尾10件を読む（**前回の記憶で動かない**）
4. チャンネルの実績を読む（`src/analytics.py`、YouTube Analytics）
5. **決めた腕に沿って作業する** → 作る → 投稿する。出したら

       run_marker.py --ship "<1行>" --kind <種別> --lever <腕> --moves <見込みの日数>

   **`--lever` と `--moves` は両方とも必須**です。**`--kind` は、`<1行>` の頭が
   `upload/means/verdict/fix` で始まっていない回に必須**（2026-08-27 に門にした ——
   空けると `drift.py` の漂流の門から、その回が見えません）。`--moves` は
   **この作業で予測日が何日動く見込みか**（早まるなら負・遠のくなら正・
   動かさないなら 0）。**当てるための欄ではありません** —— 先に言わなければ
   「早めることを考えてから進めた」かどうかが誰にも見えないので、
   **先に言って、次の回が実際の差と並べます**（`levers.reconcile`。
   `eta.py` の末尾に「宣言と実際」として出ます）。**外れてよい。**
   外れたと分かるほうが、何も言わずに進むより速い。

   **既定は 0 です**（2026-08-28 に踏んで足した）。`eta.py` は
   「**軌跡の腕が動くのは前提を1件 閉じたときだけ。作る・出す・直すは
   軌跡の入力に入りません**」と自分で印字しているので、
   **`upload` / `fix` / `means` の回は定義上 0日**。
   0 以外を書くなら、**どの入力を動かしてそうなるかを先に書くこと。**
   前提を閉じる回（`verdict`）は `effect` の見込みから立てます
   （`effect: 1.0` なら回転が1つ進むだけ ＝ **-1 前後が上限**）。
6. **出したら、その回のうちに予測へ入れ直す**（オーナー指示 2026-08-20・原文:
   **「毎回の実行で予測するように言ったはずなので、毎回その予測に反映して」**）。

       python scripts/eta.py --reflect     ← `--ship` が既定で呼びます

   **予測を「出す」ことは1でやっています。抜けていたのは「反映」です。**
   その回で測ったこと・判定したこと・外れたことを**その回のうちに**入れ直し、
   **「前 → 後（±N日）」を残す**ところまでが1周（`data/eta.jsonl` の `kind="reflect"`）。
   実例（8/20）—— 歩留り 1.0→0.156・供給 21日→4日・A/B の在庫の数え方・掃引の候補数。
   **どれもその回の予測に入っていません。**

   **API は叩きません**（出発点と同じ実測で解き直すだけ）。**実測 28.4秒**（2026-08-31 に測り直した。**この行は 8.5秒 と書いてありましたが、実物は 330秒 でした** —— `scripts/run_marker._reflect_now()` が 2026-08-24 に timeout を 180→900秒 へ上げた註にそう書いてあり、**CLAUDE.md の側だけが 8.5秒 のまま**でした。**この repo でいちばん多い壊れ方（言っている所と、している所が別）の、この行ぶんです。** 原因は `src/form_record.per_video_best()` の憶えが **一度も当たらない鍵**だったこと —— 鍵が「既定の引数で呼んだか」なのに、唯一の呼び手が常に引数を渡す。実測: `eta.py --offline` の 150秒 のうち **139.4秒（93%）**がその中で、**623回** 呼ばれてまだ終わっていませんでした。直して `--reflect` **330秒 → 28.4秒**／`--offline` 全体 **300秒で未完 → 67.7秒**。検査は `tests/test_form_record_memo.py`（直しを戻すと2件 落ちます）。**次にここを触る回へ: この行の秒数を、撃たずに写さないこと。**）。
   Analytics は日次で3日遅れなので、取り直すと**たまたま日が変わった回だけ、
   チャンネル側の変化がこの回の作業に混ざります。**

   **動かせる入力が1つも無い回は、そう印字されます** ——
   **「効いていない」ではありません。** 触った所が予測の入力に無い、という意味です。
   **その行が残ること自体が答え**なので、必ず撃つこと。
7. `docs/JOURNAL.md` に「なぜそうしたか」を追記して push

**`eta.py` の「作業で縮んだぶん」を、1周ごとの合否に使わないこと**（2026-08-19 21:2x）。
Analytics は**日次で3日遅れ**、回はそれよりずっと速いので、**1日のうち入力は動きません**
（実測: 18点とも `views_7d` も `subs_net` も同値）。**1周ごとに測れるのは腕のほうです。**

立ち上げ・引き継ぎの手順は `docs/KICKOFF.md`。

---

## 実装の要点（詳細は README.md）

```sh
bash scripts/setup.sh                     # ffmpeg・open-jtalk・依存を用意（何度でも安全）
python -m src.script_writer               # 台本のJSONスキーマを出す
python -m src.pipeline --script build/script.json --topic <ID> [--dry-run]
```

- 認証情報は環境「Youtube」の環境変数から読む。**動いているセッションには後から入らない**
- 投稿済みは説明欄の `[t:テーマID]` からチャンネル越しに復元する。ファイルに持たない
- **4分を下回ると投稿前の検査が止める。** 2026-08-09 まで8分半だったが、
  あれは**ミッドロール広告の下限**で、収益化まであと999人の段階では効かない。
  **通っていない門のために検索性能を犠牲にしていた。** 目安は4〜7分
  （`docs/SEARCH_TERMS.md`）。収益化が見えたら戻すか検討する
