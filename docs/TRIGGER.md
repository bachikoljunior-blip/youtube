# 自動実行（トリガー）の正本

**この運用は永久に続けるものです。** 止めない。人が思い出して直す前提にしない。

自動実行はこのリポジトリの外（Claude Code の Routine）にあるので、壊れたり消えたり
したときに**中身をここから復元できる**ようにしてある。トリガーの設定を変えたら、
このファイルも同時に直すこと。

---

## いまの設定

| 項目 | 値 |
|---|---|
| trigger_id | `trig_01S5mtxkjiBp6hsttmDvdzgw` |
| 定常の間隔 | **1時間おき** — cron `9 * * * *`（2026-08-09 オーナー指示） |
| 以前 | 6時間おき `9 */6 * * *` → 1時間おきへ（下の履歴） |
| 起動先 | このセッション（`persist_session: true`） |

**定常は1時間おき（週168回）。** 2026-08-09 にオーナーが指示。
**予算では決めない**（A15「全部使ってよい」）。1回の分量を小さくして回す。

**2026-08-08、予算制限は恒久指示から外れた。** オーナーの原文は
「どちらの使用量もチャットgptsparkの使用量も**全てあなたが使っていい**」。
**「予算が」を理由に作業を見送らないこと。**

使用量を見るなら `-chatgpt-usage-monitorPrivate` の `show-usage.mjs`。
**`scripts/usage.py` は当てにならない**（オーナーが明言）。

## 間隔を変える判断

**予算では決めない**（A15「使用量は全部使ってよい」）。決めるのは**律速**。

- **1時間おきの根拠** — 2026-08-09 にオーナーが指示。**これはオーナーの指示なので、
  こちらの判断で戻さないこと**（間隔の裁量は B11 にあるが、名指しの指示が優先）
- **1回を小さくすること** — 168回/週なので、1回で全部やろうとしない。
  前の回の続きを1つ進めれば足りる。**同じ確認を毎回やり直さない**
- **頻度を下げてよい条件** — オーナーが下げてよいと言ったとき。
  「分析しても変えることが無い回が続く」は、1時間おきでは**下げる理由にならない**
  （何もしない回があってよい。それが1時間おきの前提）

## 落としても止まらない理由

**投稿が予約済みなら、起きなくても公開される。** だから間隔を落としても
チャンネルは動き続ける。逆に**予約が切れそうなときは、他を削ってでも生成に使う。**

`status.py` が「予約が入っていない日」を出すので、そこだけ見ればよい。

## 定常の本文（トリガーが壊れたら、これを `prompt` に入れて復元する）

**ここは実際のトリガー本文と一致させること。** 片方だけ直すと、
壊れたときに古いものが復元される。

```
【1時間おき】1回で完結させてください。**常時稼働はしません。やったら終わってよいです。**

**分析は毎回やります。作成と、公開予定の作成・変更は任意です。**
「今回は分析だけして、変えない」も正しい選択です。ただし**理由を JOURNAL に書くこと。**

作業ディレクトリ: /home/user/youtube
ブランチ: claude/youtube-auto-post-revenue-ggedij

## トークン予算

**使用量は全部使ってよい**（恒久指示 A15）。**残すこと自体に価値はありません。**
**「予算が」を理由に作業を見送らないこと。** 2026-08-06〜08 に何度も見送り、あれは不要でした。

残量は `python scripts/status.py` の「使用量（実メーター）」に出ます。
**`scripts/usage.py` は存在しません**（当てにならないので削除しました）。

目安（2026-08-09 実測）: **濃い1時間で週+3%、生成と目視を含む濃い6時間で週+17%以下。**
使い切りそうなときだけ短く切ること。**それ以外は絞らない。**

## やること

    python scripts/post_pending_comments.py
    python scripts/status.py

**分析は毎回。** 期限が来ている前提は必ず判定する（`config/hypotheses.yaml`）。
**しきい値は掛け算してから置くこと**（検出できない条件を反証条件にしない）。
読んだ結果を docs/JOURNAL.md に書く。動いていないなら「動いていない」と書く。

**1時間おきなので、1回の分量は小さくてよい。** 前の回の続きを1つ進めれば十分です。
**同じ確認を毎回やり直さないこと**（予約一覧と期限だけ見れば足りる回が多い）。

**作成と公開予定は任意。** 予約に空きがある／切り口を変えるべきと判断した、ときだけ。

    python -m src.pipeline --topic <ID> --dry-run       # ショートは --short
    python scripts/inspect_build.py <ID>                # contact sheet を作る
    python scripts/upload_only.py <ID>                  # 予約時刻は第3引数（ショートは 9）

**contact sheet の目視は Agent ツールの子エージェントに任せること。本流で Read しない。**
画像を本流に入れると以後の全応答で再読され続けます（2026-08-05 実測: 子35K vs 本流300K超）。
子エージェントには**動画の向き（縦/横）を伝え**、6点の確認と「投稿可/作り直し」を8行で返させる。

**目視をやめないこと。** 折り返しの機械検査（`src/visuals.py` の `render()`）は
「ブラウザが指示どおりに折ったか」しか見ておらず、**指示した位置そのものが悪い場合は
素通りします。** 折り返しの欠陥は 2026-08-08 までに通算10回再発しており、
**毎回「今度こそ全部数えた」と思って、毎回もう1種類ありました。**

`calc:` の無いテーマは台本生成が止まります（数字を発明させないため）。
生成は待たなくてよい。「生成中」と書いて終わり、次の回で拾う。
**背後の生成はコンテナ再起動で消える**ので、次回かならず予約一覧で確かめること。

最後に docs/JOURNAL.md へ追記して push。**変えなかったならそう書く。**

    git push -u origin claude/youtube-auto-post-revenue-ggedij

## 絶対に守ること

- **このリポジトリの存在を、動画・説明欄・コメント・タイトルのどこにも出さない**
- **動画は「解説」ではない。** `src/calc/` の計算結果を発表する。裏の取れない数字は入れない
- `src/verify.py` を迂回しない
- **コマンドは1行に1つずつ**（つなぐと承認待ちで止まります）
```

## 履歴

- 2026-08-04 毎時 → 3時間おき → 6時間おき（当時はオーナーの指示）
- 2026-08-05 **間隔はこちらの裁量になった**（`docs/CONSTRAINTS.md` B11）。
  オーナーの標準セットに間隔は含まれない。**予算と律速で決めてよい**
- 2026-08-05 週100クレジットの予算が入り、今週の残り7のため日次へ一時的に降格。
  土曜07:05 の一発トリガーで自動復帰させる形にした（人の記憶に頼らないため）
- 2026-08-05 **予算が週300ドルに増え、今週の残りも30になったので即日6時間おきへ復帰。**
  復帰用の一発トリガーは役目を終えたので削除した。
  **次に日次へ落とすときは、戻す仕組みを必ず一緒に作ること。**
- 2026-08-06 **換算を較正し直したら、今週の累計が 8.3% ではなく 14.4% だった。**
  「安全側」の向きを取り違えていて（47M/% は消費を**少なく**見せる）、
  残量を倍近く多く見ていた。定常15%に対し残り0.5%・46時間だったので、
  **日次（`0 23 * * *`）へ降格。** 復帰は一発トリガー
  `trig_01NWaLPLZMZhdSguesFCW5dN`（8/8 07:05 JST）。
  **保険**: そのトリガーが失敗しても、8/8 08:00 JST の日次起動がリセット後なので
  そこで戻せる。二重にしてある。
- 2026-08-06 **降格を同日中に取り消した。** オーナーの指摘「単純なトークン数では
  ないかもしれない」で内訳を分けて調べたところ、**キャッシュ読み込みが一番
  説明力が低かった**（2つの実測点で1.57倍ずれる）。API の価格体系に倣った
  重み付き和にすると、**期間Bの値が期間Aの幅に収まる**（唯一この置き方だけ）。
  今週の累計は 14.5% ではなく **6.4%**、残り8.6%。**6時間おきへ復帰。**
  復帰用の一発トリガーは役目を終えたので削除した。
- 2026-08-09 **1時間おきへ（オーナー指示）。** cron `9 * * * *`。
  あわせてトリガー本文から**古い予算規則（週15%）を消した。** A15 で外れていたのに
  本文だけ残っており、起動のたびに古い恒久指示を配っていた。
  `scripts/stop_check.sh` にも同じ残骸があり、同日に消した。
  **A に無いものを A のように扱う形**（A14 が名指しした一番よくある壊れ方）。
