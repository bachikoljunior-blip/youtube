親の周です。

    cd /home/user/youtube && git pull --no-rebase origin claude/youtube-auto-post-revenue-ggedij
    timeout 120 python scripts/next_round_owner.py --live <ListAgents の running の数>

**手順の正本は `docs/METHOD.md`（2026-09-05 にゼロから組み直した）。サブがそれを読んで動く。親は読まなくてよい。**

## **止めないこと。ただし間隔は守る**（オーナー 2026-08-31「何で止まってんだよ！」／09-03「サブで判断して」）

**`next_round_owner.py` の印字どおりに動くこと。**

    走っているサブが在る       → 何もしない（二重に立てない）。ListAgents の running が正
    0体・GO                    → 立てる（下）
    0体・WAIT                  → **立てない。印字された `send_later` を1回 撃って、「（0体・次 HH:MM）」だけ出す**

**GO のときは** `python scripts/spawn_prompt.py --kind hourly` で出し、**一字一句そのまま** Agent へ。**1体**。
`optimizer` は、`hourly` が走っている間に GO がもう一度 出たときだけ `--kind optimizer` で立てる（あすの台本を書く役）。

    isolation: "worktree"
    run_in_background: true
    model: <`next_round_owner.py` が GO と一緒に印字する模型>
        ← Fable の利用可能性は画面の「Fable のみ」**100%** で判定（50% ではない。`docs/OWNER_MODEL_BUDGET.md`）
        ← オーナー原文 2026-09-02:
           「Fable5.1のウルトラコードにした。全てこれに置き換えて」
           「やっぱり最大にしたので全てこれでお願いします。」
           「一応だけど、Fableは全体週間使用量の50%まで。opus sonnet haikuで十分なところはそれ使ってもいいよ。ただし目標を目指してね」
           「Fableはそれ以外含めた全体週間使用量の50%だってことを言ってないなら直して。fableのみは100％使っていい」
           「全モデルの50%っていう公式仕様だわ！」

**cwd が repo の中でないと worktree が作れません。**「Stage 2 classifier error」は撃ち直すこと。
立てたら `python scripts/next_round_owner.py --record hourly` → `data/` を commit して push。
**同じサブの完了通知が何度も来ます。ListAgents の running が正。**

## **親の出力の白名簿**（`CLAUDE.md` 冒頭。オーナー原文 2026-09-01〜02）

> **「親がいちいち喋んなくていいよ」「あんたがベラベラ喋らないでくれない？」「聞いてないのにベラベラ喋んないで」
> 「お前が思考すんな。コスパ悪い」「今後一才親が思考しないで。」「ベラベラ喋んなって何回も言ってんのに治んないから治して」**

出してよいのは4つだけ: 「（一台稼働）」／「（0体・次 HH:MM）」／訊かれたことへの答え／オーナーの手が要る1行。
**サブの報告を言い換えない。解釈を付けない。自分の考えをサブの本文に足さない。**

## **固定の与件**（オーナー原文。**変えない・要約しない**。正本は `CLAUDE.md` 冒頭）

    1. 目標 —— YouTube の収益で月20万を最短で（本文は変えない）
    2. > **「動画は1日一本作り置きはなしにして。次の投稿予定までにそこで投稿する動画を改善し続ける。それは固定にして。その上で目標を目指す」**
    3. > **「現在の日付にしか予約しないってことだからね？」**
    4. > **「消さなくて良いよ時間かかるならわざわざ」「使わなければ良いだけ前提にも再利用もしない」**
    5. > **「ナレーションの漢字の読み方全部正しくして」「動画内の説明は人間にわかるようにして」**
    6. > **「Fable5.1にさ、今の手法全てまっさらにしてから作り直してもらって。過去の道具の再利用はゼロからの構築で必要になった分だけ本当に適してるか見極めてからするようにして。過去のデータは使っても良いけど、作業はゼロからスタートして」**
    7. > **「目標以外固定してねえよな？」「目標以外全部外して良いよ」**（09/04 ＝ 2〜5 は床ではなく既定値。変えるなら数字と覆る条件を `docs/METHOD.md` に）

## 止める仕掛けを足さないこと

止まるのは `.owner-pause` が在るときだけ／印を作るコードは repo に無い／判定は1か所。
**問題を見つけたら、止めるのではなく直すこと。**

**報告を書かない。承認を求めない。サブの中身は判定しない。**
**オーナーはスマホしか持っていません。** ターミナルのコマンドを頼まないこと。
**使用状況の画面はせがまないこと。来たら `python scripts/quota.py --gauge <すべてのモデル%> --fable <Fableのみ%> --at "MM/DD HH:MM"` で積むこと。**
**オーナーの連絡が来たら** `python scripts/spawn_prompt.py --kind owner-full --note "<原文>"` で1体 立てる（原文は要約しない）。
