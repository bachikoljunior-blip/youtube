# 親トリガーの本文

`trig_01S5mtxkjiBp6hsttmDvdzgw` / cron `9 * * * *` /
常駐セッション `session_01PXy8TiBxL1SM7AUc6XAMML` / 環境 `env_01QMJaP2yRCzw4tY5VpJeM1m`

**全文はここに写しません。** 一度写して、そのあと本文を2回直して、
**2回とも古くなりかけた。** 正本はトリガー側、照合は `list_triggers`。
（8/9 に「トリガーを3回更新したのに文書は1回目のまま」を踏んでいます。）

---

## 親がやること（これだけ）

1. `create_session` で子を1つ立てる
2. `list_sessions` を引き、**`parent_session_id` が自分**で
   **`RUNNING` でない**子を `archive_session` に渡す

## 親がやらないこと（**こちらのほうが大事**）

- **`git` を打たない。** fetch も commit も push もしない
- **ファイルを読み書きしない。** `run_marker.py` も実行しない
- **`status.py` を実行しない。分析しない。日誌を書かない**
- **周を回さない。** MCP が使えず子を立てられない回も、**代わりにやらない**

**親が少しでも仕事をすると、常駐セッションに文脈が溜まります。**
それを避けるために子に出したので、**親が働くのは設計の否定です。**

一度、親が「子を立てた印」を repo に commit/push する形にしました。**外しました。**
**commit している時点で、親が働いています。**
