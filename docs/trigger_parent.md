# 親トリガーの本文

`trig_01S5mtxkjiBp6hsttmDvdzgw` / cron `9 * * * *` /
常駐セッション `session_01PXy8TiBxL1SM7AUc6XAMML` / 環境 `env_01QMJaP2yRCzw4tY5VpJeM1m`

**全文はここに写しません。** 一度写して、そのあと本文を1回直して、
**すぐ古くなりかけた。** 正本はトリガー側で、照合は `list_triggers` でやること。
（8/9 に「トリガーを3回更新したのに文書は1回目のまま」を踏んでいます。）

**このトリガーは 2026-08-10 まで、自分で周を回す「本番」だった。**
オーナー指示で **子を立てるだけの親**に変えた。

---

## この方式の要点（全文はトリガー側）

1. 枝を合わせ、`run_marker.py` で二重発火でないか見る（窓30分）
2. `create_session` で子を1つ立てる（`source_url` / `source_revision` / `tags` 必須）
3. **立てたその場で `--spawned <ID>` を打って push する**
4. `--sweep` で90分より古い子を畳み、`--swept` で記録
5. 何も起きていない回は日誌に書かない
