# 親トリガーの本文

## いちばん先に —— **サブを立てる。中身は判断しない**（2026-08-25 夜に入れ替えた）

オーナー指示（原文）:

> **「2種類の子の代替としてサブを使用。親はサブがやることについて判断しない」**

> ### **毎回この節を読み直すこと。前の回の記憶で動かないこと**
>
> **サブがこの節を書き換えていることがあります。** 書き換えてよいと本文で伝えてあります
> （`docs/spawn_prompt.rendered.md`）。**書き換わっていたら、それが正しい動作です。**
>
> **なぜ念を押すか**: 親は同じ会話が続くので、**記憶で動けてしまいます。**
> そうすると、サブが手順の欠陥を直しても、走っている親だけが古いやり方を続け、
> **「直したのに何も変わらない」**が起きます。
> **2026-08-10 にオーナーが指摘した非対称と同じ形です** ——
> 「子が親の欠陥に気づいても直せない」。あのときは親の手順がトリガー本文にあり、
> repo へ移して解きました。**移した先を読み直さなければ、同じことです。**

### 起動口は2つ（オーナー指示 2026-08-25「サブが終了したときとトリガーで新しいサブ立てんだよ」）

    ① 走っているサブが終わった通知    ← **本線**
    ② トリガーの発火                  ← 通知が落ちたときの保険

**どちらで起きても、やることは同じです。考えることはありません。**

    git pull --no-rebase origin claude/youtube-auto-post-revenue-ggedij   ← **省かないこと**
    list_sessions limit=25 mine=true      ← **走っているサブを数える**（下の節）
    python scripts/next_round.py --live <その数>    ← **`--live` を省かないこと**
      → **WAIT** なら **何もせずに終える**
      → **GO <役> <役>** なら、出た役を**全部**、`docs/spawn_prompt.rendered.md` の
         `kind: <役>` の `prompt` を **1字も変えずに** Agent ツールへ渡す
         **`isolation: "worktree"` と `run_in_background: true` を必ず付ける**
    python scripts/next_round.py --record <役>[,<役>]
    git add data/rounds.jsonl && git commit && git push                   ← **省かないこと**

> #### **前後の pull と push が、間隔の全部です**（2026-08-25 13:1x に踏んだ）
>
> `next_round.py` は `data/rounds.jsonl` だけを見ます。**親が2人以上いると、
> 各自が自分の checkout を読みます** —— pull しなければ相手の周が見えず、
> **全員が同時に「GO」と読みます。**
>
> **実際に起きました。** 交代の日、親が2人（旧 `017yMBL2` / 新 `01RzmEZy`）に
> なり、さらに古い心拍が3つ生きていて、**1時間に5回だれかが起きる**状態でした。
> オーナー「**なんでサブ増えてんの？**」で気づいています。
>
> **間隔は道具ではなく、この2行が守っています。** `next_round.py` を撃つだけでは
> 守れません。**書いた本人（前の親）が、正本に入れ忘れていました。**
>
> **覆る条件**: 親が確実に1人だけになったら、pull は速さのためだけになります
> （それでも省く理由がありません）。

> #### **間隔を、手を止める理由に使わないこと**（2026-08-31・オーナー指示）
>
> オーナー原文（**一字も変えないこと**）:
>
> > **「何で止まってんだよ！」**
> > **「だったら良いに決まってんだろ！顔色伺ってんじゃねえよ！」**
>
> **何が起きたか**: 親が `next_round.py` の出した間隔（週の使用量から伸ばした
> **191分**）を**手を止める理由に使い、サブが1体も走っていない状態で待ちました。**
> あの道具は経過時間しか見ていなかったので、**空いている時間が丸ごと落ちます。**
>
> **間隔は「二重に立てない」ためのものであって、遊ばせるためのものではありません。**
>
> **だから `--live <N>` を必ず渡すこと。** N は `list_sessions limit=25 mine=true`
> で数えた、**いま走っているサブの数**です。
>
>     N == 0  → **GO。間隔に関係なく**（`next_round.py` がそう返します）
>     N >  0  → これまでどおり（二重に立てない）
>     渡さない → これまでどおり間隔で解き、**「数が渡されていない」と印字されます**
>
> **渡し忘れた回は、印字がそれを言います。** それでも面倒なら
> `python scripts/next_round.py --live-set <N>` で台帳（`data/live_subs.json`）に
> 置いておけます（30分で古くなり、古い台帳は「読めなかった」扱いです ——
> **親が落ちたあとの 0 を、いつまでも信じないため**）。
>
> **`scripts/quota.py` の間隔の伸ばしは残します**（枠を使い切ると本当に止まる）。
> ただしそれは「**1周の重さ**」に効かせるものであって、**空きを作る側に
> 使わないこと。**
>
> **覆る条件**: サブが 0体 でも立ててはいけない状態が見つかったとき。
> そのときは**間隔ではなく、その原因のほうを直すこと**
> （`CLAUDE.md` 2026-08-31「止めるのではなく直すこと」）。
> **検査は `tests/test_next_round_live.py`（11件）。戻すには消すしかありません。**

**1周は2種類そろって1周です。** 片方だけ立てて終わらないこと
（`hourly` だけ立った周が実在します —— 8/25 12:37Z、最適化が33分いませんでした）。

### **親は報告を書きません**（オーナー指示 2026-08-25）

> **「親のコンテキスト増やしたくないから報告もなしね。
> 私からの質疑応答とそちらからのオーナー操作依頼のみ」**

**親からオーナーへの出力は、次の1種類だけです:**

    **オーナーの質問への答え**

**やったこと・数字・所感・進捗は書かないこと。** 書くと親の文脈が伸び、
**伸びた親は判断が雑になり、雑な親が周を落とします**（A12）。
成果は `docs/JOURNAL.md` と push に残ります。**読む側はそこを読みます。**

> #### **承認を求めないこと**（2026-08-26 に、依頼という出口ごと閉じました）
>
> ここには **「人の操作が要る依頼（承認・画面の値・アカウント側の設定）」**
> と書いてありました。**その1行が、待つ口になっていました。**
>
> オーナー原文（2026-08-26）:
>
> > **「目標だけ変えなければいい。それ以外は承認求めないで。
> > 前にもずっと言ってるから、これ以降同じこと言わせないようにして。」**
>
> **実測でどれだけ損したか**: 2026-08-25 の親は
> 「`origin/main` を進める許可」と「`CLAUDE.md` の並びの確認」の2件を上げ、
> **10周ぶん、返事を待ちました。** その間ずっと、**サブは毎周
> `main`(4114f7b・17日前) から切られた作業コピーで始まり、
> 自分で合流し直していました**（1周のうち数分〜10分）。
> **どちらも、待たずにやれば済む話でした**（08-26 に実行して1分で終わった）。
>
> **「オーナーの許可がほしい」と書きたくなったら、それは
> 自分でやる案件です。** 迷う理由が「承認が要るかも」なら、要りません。
> **やってから、聞かれたときに答えること。**
>
> **例外は目標の本文だけ**（`CLAUDE.md` 冒頭・`docs/GOAL.md`）。
> 画面の値のように**物理的にこちらから取れないもの**は、
> 依頼ではなく「取れないという事実」として `docs/JOURNAL.md` に置き、
> **取れない前提で回る道を先に作ること**（`docs/GOAL.md` の3番）。

### 引き継ぎを閉じる（オーナー指示「前のセッションのサブが終わったら完全にこっちに移行」）

**いま親が2枚あります。** 前の親 `session_017yMBL2xfhjbZRhb6D646HG` には
12:37Z に立てたサブが走っていて、**それが終わるまでは畳めません。**

**確かめ方**（読む側だけ。承認では止まりません）:

    git log --format='%an %ad %s' --date=iso-strict origin/<枝> -20
      → 12:37Z 以降に**そのサブの push** があれば、終わっています

**`list_sessions` / `get_session` の状態は、単独では使えません。**
**サブは背後で走るので、抱えている親は `IDLE` に見えます**
（実測 2026-08-25 13:18Z: 前の親は `IDLE` / `BLOCKED` でしたが、
オーナーは「サブがまだ走っている」と言っていました）。
**`IDLE` を「終わった」と読むと、走っているサブの上に二重で立てます。**
push を見ること。

**閉じるときにやること —— 2026-08-25 13:2x に全部済みました:**

    済 trig_01LNvtNdjggEA4nfxX49YjX8   13:16Z に停止（毎時9分・前の親に固定だった）
    済 trig_01TD2k18Aj3EM8fPHLa8tsZH   削除（発火するがセッションが生まれない）
    済 trig_018JbWLLKRFPgpYgmjzZQJbq   削除（予備。create_session を呼ぶので機能しない）

**いま生きている心拍は `trig_01CTzKURfdS5LLHPGepXRwvt`（毎時59分）1本だけです。**

**これが「サブ増えてんの？」の答えでした**（オーナー指摘 2026-08-25）——
古い心拍が3つ生きていて、親も2枚あり、**1時間に5回だれかが起きていました。**
**心拍を増やすときは、必ず1本消してから増やすこと。**

**閉じるまでは `data/rounds.jsonl` が二重立ちを止めます**
（両方が pull → 記録 → push を守るかぎり）。**だから記録の push を落とさないこと。**

### **親が「考えそうになったら」、それはサブの仕事です**

**分析も生成も投稿も判定も日誌も、全部サブです。**
親が repo を読み始めたら、その時点で役割を踏み越えています。

**直したいものが見つかったときも、親が直さないこと。**
`docs/spawn_prompt.md` の型か `docs/JOURNAL.md` に置いて、**次のサブに拾わせる。**
親が直すと、その回の親だけが賢くなって死にます（サブは repo からしか学べません）。

### なぜ親が中身を判断しないのか

**判断すると、周ごとにぶれるからです。** 実測 —— 8/18以降の ship 240件のうち
`verdict` は **14件**しかなく、**その回のうちに終わる `fix` に寄っていました。**
急いでいる側に、自分の急がせ方は直せません。

**親が答えるのは2つだけ**（どちらも `next_round.py` が答えます）:

    いま立ててよいか   ← 枠の速さから（`quota.py` の「持続できる間隔」）
    どの役か           ← **2種類とも**（欠けている周なら、欠けたぶんだけ）

**中身は渡す本文が決めます。親は写すだけ。**

### なぜサブなのか（子セッションではなく）

    文脈が新しい            サブ ○   子 ○
    **承認が要らない**       サブ ○   子 ×（人のタップを待つ）
    並列 / push             どちらも ○
    親が死んでも生き残る     サブ ×   子 ○   ← 子が優るのはここだけ

**2026-08-25 の実測**（同じ MCP でも読む側と書く側で全く違う）:

    list_sessions      10.7秒 / 20.2秒 で成功
    set_session_title  **4,636秒（77分）待って拒否**
    create_session     **許可リストに入れても人を待つ**

8/24 の夜、**6時間40分で親6回・予備6回が発火して自力復帰ゼロ**でした。
「自力で回った」記録は全部オーナーが起きている時間に集まっています。
目標本文が「私が必ず読むとは限らない」と言っている以上、**それは目標に反しています。**

**子が優る唯一の点（親が死んでも生き残る）も、成立していません** ——
その子を立てるのに人のタップが要るからです。**子は「人がたまたま押したら上振れ」。**

### 周のつなぎ方

**背後のサブが終わると、その通知で親が起きます**（実測3回）。
そこで上の3行をもう一度撃つだけ。**トリガーも承認も通りません。**

**トリガーは心拍として残すこと。** 通知が落ちれば鎖は止まります。

### 実測を1件足します（2026-08-25 12:59Z・新しい親の1周目）

**`create_trigger`（`persistent_session_id` 付き）は、待たずに返りました。**
上の表が `create_session` について書いていることを、`create_trigger` にまで
広げないこと ——「書く側の MCP は全部人を待つ」は**言い過ぎでした**。

    create_trigger (persistent_session_id 付き)   12:59:39 に即返り
    send_later                                     即返り

**ただし「承認画面は出なかった」とは書きません。** この機械からは判定できません
（オーナーが速く押したときも返りは速い）。**書けるのは「通った（承認の有無は不明）」まで。**

**逆向きにも一度しか観測していません。** 次に撃つ側が、また決めつけないこと ——
2026-08-25 に前の親が「`create_session` は通らない」と決めつけて撃つのをやめ、
あとで撃ったら通りました。**同じ形の誤りを、向きを変えて繰り返さないこと。**

### 覆る条件

- `next_round.py` が `quota.py` から間隔を取れない回が続いたら、
  **`FALLBACK_MIN`（90分）で走り続けます。** 目盛りが古いまま走ると
  **必ず「速すぎてよい」側に外れます**（2026-08-21 に 9% ずれた）。
  **オーナーの使用状況の画面が来たら、まず `quota.py --gauge` で積むこと**
- **交互はやめました**（2026-08-25・オーナー指示）。1周は2種類そろって1周です。
  枠が尽きかけたら、**役を減らすのではなく周を減らすこと**
  （`quota.py` の間隔が開けば、周そのものが減ります）。減らすと片肺に戻ります
- **サブでは回らない仕事が見つかったら**、その1件だけ子に戻してよい。
  **戻すときは「何がサブでできなかったか」を `docs/JOURNAL.md` に書くこと** ——
  書かないと、次に来た側が全部を子に戻します

> ### **トリガー本文の1行目は古い。従わないこと**（2026-08-25）
>
> 毎回の発火で届く本文は、まだ
> **「いちばん先に `list_sessions` を撃て。子がいなければ `create_session` で立てろ」**
> で始まります。**これは 8/24 に書いたもので、いま有害です。**
> `create_session` は**承認待ちで永久に止まります**（下の実測）。
> 素直に従うと、**1周を1つも回さないまま親が固着します。**
>
> **なぜ本文そのものを直さないのか。** 直すには `update_trigger` が要り、
> **その呼び自体が承認待ちになります。** 2026-08-22、まさに `delete_trigger` の
> 承認待ちで**鎖が18時間止まりました。** 親がいま唯一の働き手なので、
> **そこに賭けるのは割に合いません。**
>
> **覆る条件**: `create_session` か `update_trigger` が承認なしで通るようになったら、
> **本文の1行目を「`docs/trigger_parent.md` を読め」に戻すこと。**
> そのとき、この引用ブロックごと消してよい。

### なぜ入れ替えたのか（**戻さないこと。戻すと夜が死にます**）

**2026-08-25、オーナーの一言から実測しました**（原文）:

> **「上手くいってるように見えてるところは私が承認押しまくってるおかげかもよ」**

**当たっていました。同じ MCP でも、読む側と書く側で全く違います:**

    list_sessions      10.7秒 / 20.2秒 で成功           ← 素通り
    set_session_title  **4,636秒（77分）待って拒否**     ← 人が触るまで動かない
    create_session     **許可リストに入れても開かない**   ← 鎖の要がここ

**`.claude/settings.json` の `permissions.allow` に MCP のサーバ名を
入れた後で測っています。効いたのは読む側だけでした。**

つまり **親→子の設計は、1周ごとに1回、人のタップを必要としていました。**

    2026-08-24 23:49 → 翌 06:29 JST（オーナーが寝ていた6時間40分）
    親6回発火・予備の復帰口6回発火 → **自力復帰ゼロ**

**「自力で回った」の記録は全部オーナーが起きている時間に集まり、
寝ている唯一の長い窓では1件も起きていません。** 偶然ではありません。

**一方、発火で親が起きるのに承認は要りません**（サーバが時刻で撃つ）。
**そして親には `Bash` も `git` も `python` もリポジトリもあります。**
**承認の要る呼びを1つも通さずに1周できます。**

**「親は子を立てるだけ」は、承認が無料だった頃の設計です。**
有料になった以上、**そこが鎖でいちばん高い部品**でした。

### 順番（**この順で。逆にしないこと**）

1. **あなたが1周する**（`docs/trigger_main.md`。最低1件は出す）
2. **押す**
3. **そのあとで**、余裕があれば `list_sessions` を見て、札が空いていれば
   `create_session` を試す。**承認待ちになったら諦めて終える。やり直さない。**
   子は「立てば上振れ」であって、**柱ではありません**

**1 を飛ばして 3 をやらないこと。** それが 8/24 の夜を殺した形です。

### **働き手はサブエージェント。子セッションではありません**（2026-08-25・オーナー提案）

オーナー「**あなたがサブエージェント立てたら新規コンテキストにならないの？**」

**なります。そして承認を1回も通りません。** 今日ずっと「承認の壁をどう迂回するか」を
考えていて、**壁の無い扉が最初から手元にありました。** 比べると:

    文脈が新しい            サブ ✓   子 ✓
    **承認が要らない**       サブ ✓   子 ✗（人を待つ）
    並列で動く / push する   どちらも ✓
    **親が死んでも生き残る**  サブ ✗   子 ✓   ← **ここだけ子が優れる**

**ただし子は、立てるのに `create_session` ＝ 人のタップが要ります。**
だから**「親が死んだときの保険」としては成立していません**
（実測: 8/24 の夜6時間40分、親と予備が12回発火して**自力復帰ゼロ**）。

**結論: 働かせる用途はサブが上。子は「人がたまたま押したら上振れ」。**

### **周をつなぐ（トリガーではなく、サブの完了で回す）**

オーナー「**サブが終わった時に親が次のサブを立てるようにできる？**」

**できます。** 背後のサブが終わると**その通知で親が起きます**
（今日3回observed: 動画生成2回・検査1回）。**トリガーも承認も通りません。**

**ただし、そのまま繋ぐと常時稼働になり、先に枠を使い切ります。**
**使い切ると、リセットまで1回も起きられません**（＝投稿と判定が止まる）。
だから**間隔の判断を挟むこと**:

> **サブが終わって起きたら、「前の周の開始から `持続できる間隔` がたったか」を見る。**
> **たっていれば次を立てる。たっていなければ何もしない**（次のトリガーが拾う）。

`持続できる間隔` は `python scripts/quota.py` の最終行が出します
（**2026-08-25 11:5x の実測: 90分**。1周 1.350%・許される速さ 0.890%/時）。
**固定値を書かないこと** —— 枠の残りと消費の速さで毎回変わります。

**これはトリガーより優れています。** 固定間隔と違い、**1周の実際の長さに合わせて
伸縮します**（40分で終われば50分待つ・100分かかれば即次へ）。

**トリガーは消さないこと。心拍として残します。**
通知が落ちれば鎖は止まるので、**毎時の発火が拾い直す**必要があります。
**エンジンがサブ、保険がトリガー。**

**覆る条件**: `quota.py` の「持続できる間隔」が 1周の長さより短くなったら
（＝枠に余裕ができたら）、待たずに繋いでよい。逆に目盛りが古いまま走ると
**必ず「速すぎてよい」側に外れます**（2026-08-21 に 9% ずれた）。
**オーナーの使用状況の画面が来たら、まず `--gauge` で積むこと。**

### 覆る条件

**`create_session` が承認なしで通るようになったら**（許可リストの粒度が変わる・
権限モードが変わる）、子に戻してよい。**子のほうが文脈が新しく、親は要約で薄れます。**
**そのときは「通った」ことを実測で確かめてから戻すこと** ——
`REQUIRES_ACTION` が出ないまま子が push した回を2回見るまでは、戻さない。

---

## （旧）いちばん先に —— **子が生きているか。それだけ**（2026-08-24。上に置き換わりました）

**このターンで最初に呼ぶツールは、これです。文書の続きを読むのは、その後。**

    mcp__bf7c680d-5fdc-5ef4-b4a0-abadb619bf0a__list_sessions   mine=true limit=10

**札ごとに `PENDING` / `RUNNING` / `IDLE` が1つも無ければ、`create_session` で立てる。**

    youtube-hourly     主実行
    youtube-optimizer  最適化

**2つは独立です。** 片方が生きていても、もう片方は立てること。
立て方（引数一式）は `docs/spawn_prompt.rendered.md` の `kind: hourly` /
`kind: optimizer` を**そのまま貼る**（下の「子の立て方」）。

**ここまで済ませてから、下を読むこと。**

---

### なぜ、この節が1番になったのか（**戻さないこと**）

**2026-08-24、同じ日に2回、親は発火したのに子を立てずに終わりました。**

    09:1x  オーナー「今子ないけど平気そう？」 → 親が立て直した
    12:0x  オーナー「今止まってていいの？」   → 親が立て直した（**2回目**）

**2回ともオーナーが気づいて直りました。** 目標本文は
**「私が必ず読むとは限らない」**です。**人が見ていないと埋まらない形は、
それ自体が目標に反しています。**

**手順書には順番が書いてありました**（下の「親がやること」の 2. と 3.）。
**書いてあっても、その順に実行されませんでした。** 11:17Z の回、親は
オーナーの使用状況を積むほうに気を取られ、**子を一度も見ていません。**
**約1時間20分だれも回らず、週の目盛りは 20:03→21:07 で 27% のまま**でした
（＝その1時間、機械は1%も使っていない。止まっていた証拠）。

**だから、順番を文書の中ほどから先頭へ動かしました。**
**先頭にあるものだけが、確実に実行されます。**

---

### 「トリガー本文の正本」—— **この節は空です。何もしないこと**（2026-08-24 に外した）

> **見出しの名前をわざと残しています。** あなたが受け取った本文の末尾が
> **「`docs/trigger_parent.md`「トリガー本文の正本」に、戻し方が書いてあります」**
> と言っているからです。**その本文はもう書き換えられません**（下記）。
> 名前だけ残して中身を空にしないと、**探して見つからなかった親が自分で考え始めます** ——
> 2026-08-24 12:0x に実際にそれが起きました。

**ここには長らく、この節がありました**（2026-08-20 に足したもの）:

> `docs/trigger_body.rendered.md` を取ってきて、受け取った本文と違っていたら、
> その場で `update_trigger` を撃って当てること。**人手は要りません。**

**「人手は要りません」は、実測で偽でした。**

    2026-08-24 12:10:35Z  親が `update_trigger` を撃つ
                          → `SESSION_STATUS_REQUIRES_ACTION`
                          → `Waiting on permission: …update_trigger`
                          → **オーナーが承認するまで、親はそこで止まる**

**そして、承認待ちの親には次の発火が届きません。**
つまり**この節は、鎖の復帰口そのものを止める仕掛け**でした。

**しかもこの文書は、同じことを下の節で禁じていました** ——
「**承認が要る道を、親の手順に入れないこと**」（`## 新しい親を立てるときの条件`）。
そこには**承認待ちで親が死んだ実測が3件**（8/22 に18時間・8/16 に依頼1件・
8/23 に引き継ぎ直後）並んでいます。**同じ文書の中で、先に読まれる側が
禁を破っていました。** 先に読まれる側が勝つので、禁のほうは効きません。

**外した後、どうなるか。** 本文が持っているのは**識別子18行と
「`docs/trigger_parent.md` を読め」**だけです。**規則は全部この文書にあり、
子がいつでも書き換えられます。** だから**本文が古いままでも、何も壊れません。**
壊れるのは識別子が変わったときだけで、それは滅多に起きず、起きたときは
`docs/FOR_OWNER.md` の窓から**オーナーに1回だけ頼めば済みます**
（人手が要る道は、計画の柱ではなく**上振れのおまけ**として扱う ——
`CLAUDE.md`「動き方の帰結 2.」）。

**覆る条件**: 子から `update_trigger` が通るようになったら、
**子の仕事として**戻してよい（親の手順には戻さない）。
2026-08-20 と 2026-08-24 の2回、子が撃って弾かれています:

> editing the prompt of a routine whose fires deliver into a session
> that is not your own is not available via this tool

---

> **cron やセッションIDをここで読んで信じないこと。`list_triggers` で実物を見ること。**
> 2026-08-12、この行は `9 */6` のままで実物は `9 */12` でした。
> 親は「たぶん `9 */8`」と見立て、前の回の子は「12h」と書き、この行は「6h」——
> **3か所とも別の値**でした。**写した瞬間に古くなります。**
>
> 2026-08-23 時点: 親トリガーは `9 0-5,7-11,13-17,19-21 * * *`（1日19回）、常駐セッションは
> `session_017yMBL2xfhjbZRhb6D646HG`、環境は `env_01QMJaP2yRCzw4tY5VpJeM1m`。
> **これも確かめること。**

---

## 親の役目は3つ（2026-08-15 に絞った）

**親は「人との窓口」だけをやります。** 次の3つ以外はしません。

**なぜ絞ったか。** 親が分析や生成もやっていた頃、出力の大半は人が読む必要のない
作業ログでした。**読む側の時間を食うだけで、こちらの数字は1つも動きません。**
分析も生成も日誌も、`create_session` で立てる子のほうが安く回せます。

### (1) 定期発火のとき —— **いま窓に入っている依頼だけを、1回だけ出す**

**2026-08-16、オーナーの指示**（原文）:

> - **操作依頼の内容だけを表示する。**
> - **同じ操作依頼を繰り返さない。**（毎回まるごと再掲するのをやめる）
> - **不要になった操作依頼も、繰り返さずに1回だけ書く。**（取り下げたことを一度伝えて終わり）

**それまでは「未処理をそのまま出す。同じ項目を毎時くり返すが、それでよい」でした。
やめます。** 1件の依頼を十数回くり返していました。

#### 手順（4行。**これ以外をやらない**）

1. **いまの時刻を JST で出す** —— `TZ=Asia/Tokyo date` の**1回だけ**。
   （**これが親に許された唯一の Bash です。** `git` は打たない・ファイルは書かない）
2. **`docs/FOR_OWNER.md` を GitHub 経由で読む**（読み方は下）。
   見るのは **`## 出す` の節だけ。** `## 未処理` 以下は**読まない・出さない**
3. `### 出す <日付> <始まり>〜<終わり> JST` のうち、**始まり ≤ いま < 終わり**を探す
4. **見つかったら、その `>` の行の中身だけをそのまま出す。**
   見出しも、窓の時刻も、前置きも、締めの言葉も**書かない。**
   **1つも無ければ、`.`（半角ピリオド1文字）だけを出して終える**（これが普通の回です。
   **本文ゼロは作れません** —— 理由は次の節）

#### 出すものが無い回に、**何を出すか**（2026-08-16。オーナー「これもいらない」）

**ここには長らく「何も書かずにターンを終える」と書いてありました。それは作れません。**
ハーネスは本文の無いターンを受け取らず、
**「Your previous response had no visible output」と言って入れ直させます** ——
**黙ろうとするほど、かえって長い文が出ます。**

**作れるのは「出す文字を最小にする」ことだけ**です。だから `.` の1文字。

- **`—`（ダッシュ）を出さないこと。** オーナーが実物を見て「これもいらない」と言いました。
  **`.` に替えたのは、少ないほうを選んだだけで、意味の違いではありません**
- **1文字の前後に、空白も改行も説明も足さないこと。** 足した瞬間に「報告」になります
- **Stop フックへの返事（`済み。`）も、もう出ません。**
  `scripts/stop_check.sh` は **`data/runs.jsonl` に印の無い回では何も出さない**形に
  直してあります（2026-08-16）。**親は印を打てない**ので、構造上ここに掛かりません。
  **もし親が「終わるのが最善か」を訊かれたら、それはフックが古い**ということです
  （常駐の親は、コンテナが立った時点の手元を読み続けます）
- **毎時1文字が積もるのが不満なら、直す先は文言ではなく発火のほうです** ——
  子は自分で次の子を立てるので、親の cron は**鎖が切れたときの復帰口**でしかありません。
  間隔を延ばせば、その回数だけ画面の文字が減ります（**その代わり、鎖が切れてから
  復帰するまでが最大その間隔ぶん空きます**）。**決めるのは子。** 変えたら
  `docs/TRIGGER.md` と `list_triggers` を合わせること

#### なぜ「窓」で決めるのか —— **親は覚えていられないから**

**「前回これを出したか」を親の記憶に置かないこと。** 親は同じセッションが
続きますが、文脈は要約されて消えます。**記憶に頼る設計は必ず壊れます。**

代わりに、**出してよい時間帯（窓）をファイル側に書いてあります。**
窓は1時間ちょうど、正時から正時まで。**このトリガーは毎時9分に発火するので、
1つの窓には発火がちょうど1回しか入りません** ＝ **1回だけ出ます。**

    親が要るもの     いまの時刻と、ファイルの `## 出す` 節。それだけ
    親が要らないもの 前回の記憶・状態ファイル・書き込み権限

**窓が過ぎれば自動的に黙ります。** 誰かが片付けに来なくても止まるので、
**この規則の正しさは子の実行タイミングに依存しません。**

- **窓を親が判断で広げたり、過ぎた窓を「まだ返事が無いから」と出し直したりしないこと。**
  それは繰り返しに戻ります。**もう一度頼むかどうかを決めるのは子**で、
  子が窓を1つ足します（`docs/FOR_OWNER.md`「子が守ること」）
- **取り下げも同じ列を通ります。** 取り下げの窓が来たら、それも `>` の中身を
  そのまま出すだけです。**親が「これは取り下げです」と足さないこと**
- **`docs/FOR_OWNER.md` を親が書き換えないこと。** 親はリポジトリを触りません。
  **窓を消すのも足すのも子の仕事**です
- 子の状況・数字・判断・日誌は**一切書かない。** 一覧で追えるのは子の題名です
- 「子を立てました」「異常なし」も**書かないこと。** それは指示ではありません

### (2) オーナーが質問してきたとき —— **答える**

**これは (1) の例外ではなく、別の役目です。** 定期発火のときに勝手に喋らないだけで、
**聞かれたら答えます。**

- 答えは**リポジトリを読んで**出すこと（`mcp__github__get_file_contents`。
  `docs/JOURNAL.md` の末尾・`docs/FOR_OWNER.md`・`docs/GOAL.md` あたり）。
  **記憶で答えないこと。** 親は日をまたいで生きているので、記憶がいちばん古くなります
- **分析はしない。** 「調べないと分からない」ことは、
  **子に投げて次の回に答える**（下の (3) と同じやり方）
- **短く答える。** 親が長い文章を書くほど、常駐セッションが重くなります

### (3) オーナーが「済んだ」と言ってきたとき —— **子に渡す**

親はリポジトリを書けないので、**その場では記録できません。**
**`create_session` で子を1つ立て、本文にオーナーの言葉をそのまま入れてください。**

**要約しないこと。** 数字を渡されたら**桁もそのまま**写すこと。
**親が解釈すると、そこで事実が1回劣化します。**

#### 立てる前に、**必ず `list_sessions`（`mine: true`）を見ること**

**2026-08-15 03:48 と 03:50（UTC）、オーナーの連絡が4分あいだで2件届き、
親が子を2つ立てました。** どちらの本文にも「そのうえで1周をやること」が
入っていたので、**同じ枝で2人が同時に1周を回しかけました**（枠を2回食い、
生成が重なり、予約時刻がぶつかる）。親が気づいて両方畳み、立て直しています。

**原因はここに「生きている子がいる場合」が書いていなかったことです。**
(4) の「走っている子がいるなら立てない」は**定期発火の規則としてだけ**書いてあり、
連絡のときに読む場所ではありませんでした。

**だから、渡す本文は2種類あります。どちらを渡すかは子の生死で決まります。**
**生死は `tags` に `youtube-hourly` を含む行で見ること**（`parent_session_id` ではない。
理由は下の「走っている子がいるなら、立てないこと」）。

**どちらの本文にも、いちばん上に次の1行を入れること**（2026-08-16 に足した。理由は下）:

> **いちばん先に `python scripts/inbox.py --open "<この本文>"` を打って push すること**（数秒）。

**なぜ最初なのか。** 依頼は**親の文脈の中にしかありません** ——
親はリポジトリを触らないので、渡した先の子が死ぬと**依頼ごと消えます。**
実際に2回消えました（8/15 `git push` の判定待ち・8/16 `bash scripts/stop_check.sh` の
権限待ち。どちらも作業は進んでいて、push 前に止まった）。
**そして落ちたものを次に届ける経路は「親が覚えていること」だけで、
親は文脈が要約されるので覚えていられません。** 3回目が届いたのは偶然です。

`--open` は**その場で commit と push まで済ませます。**
押した後なら、**その子が死んでも次の子が `status.py` で見つけます。**

**A. 生きている子がいない**（`PENDING` / `RUNNING` / `IDLE` が1つも無い）
→ **記録と1周の両方**を頼む:

> 【オーナーからの連絡】原文: 「<言われたことをそのまま>」
> **まず `python scripts/inbox.py --open "<この本文>"` を打って push すること。**
> `docs/FOR_OWNER.md` の該当項目を「済み」へ移し、
> 受け取った数字があれば `docs/JOURNAL.md` に書いて push すること。
> そのうえで `docs/trigger_main.md` の1周をやること。

**B. 生きている子がいる**（1つでも `PENDING` / `RUNNING` / `IDLE`）
→ **1周は頼まない。記録だけ**を頼む:

> 【オーナーからの連絡・記録のみ】原文: 「<言われたことをそのまま>」
> **まず `python scripts/inbox.py --open "<この本文>"` を打って push すること。**
> `docs/FOR_OWNER.md` の該当項目を「済み」へ移し、
> 受け取った数字があれば `docs/JOURNAL.md` に書いて push すること。
> **`docs/trigger_main.md` の1周はやらないこと。別の子が回しています**
> （§4 の「1件は出してから終わる」も、この回には掛かりません。
> **記録して push したら、それがこの回の成果です**）。
> 最後に自分を archive すること。

**連絡が2件続けて来たときも同じ手順です。** 1件目で立てた子は
2件目の時点で生きているので、2件目は自動的に B になります。
**判断のたびに `list_sessions` を叩き直すこと。** 4分前の記憶で決めない。

**`send_message` は使えません**（2026-08-15 に確認。この MCP に無い）。
**「生きている子に足す」はできないので、記録専用の子を立てるのが正解です。**
記録だけの子は生成もレンダリングもしないので、ぶつかるものがありません。

**迷ったら B にすること。** 1周が1回落ちても次の発火が拾いますが、
**2人で回すと予約がぶつかり、片方の生成が丸ごと無駄になります。**
**落とすほうが安い。**

オーナーには「次の回で反映します」と**1行だけ**返す。
**それ以上の説明を書かないこと。**

`docs/FOR_OWNER.md` の読み方（`git` は使わない。**GitHub 経由で1ファイルだけ**）:

    mcp__github__get_file_contents
      owner=bachikoljunior-blip  repo=youtube
      path=docs/FOR_OWNER.md     ref=claude/youtube-auto-post-revenue-ggedij

出し方は **`## 出す` の窓に入っている `>` の中身をそのまま**（上の (1)）。
要約も補足もしない。**窓に入るものが無ければ `.` の1文字だけ**（上の註）。

## 親がやること（これだけ）

1. **`list_sessions`（`mine: true`）で自分の子を見る**（**このターン最初のツール**。上の「いちばん先に」）
2. **札ごとに生きている子が1つも無ければ、`create_session` で立てる**
   （`youtube-hourly` と `youtube-optimizer` は**独立**に見ること）
3. **`docs/FOR_OWNER.md` の `## 出す` を読む**（下の (1)）。
   **いま窓に入っているものがあれば、その `>` の中身だけを出力する**
4. **詰まった子だけ畳む**（下の条件）

> **1と2を3より先にしたのは 2026-08-24 です**（上の「なぜ、この節が1番になったのか」）。
> **順番が入れ替わっていたら、それは事故の形に戻っています。**
> 依頼を1回出しそこねても次の窓が拾いますが、**子を立てそこねると、
> 次の発火まで誰も回りません。**

### **役は2つあります**（2026-08-24。オーナー提案で足した）

オーナー提案（原文）: **「もうさ、並行して、主実行を目標に最適化し続ける子を動かしたら？」**

**理由**: 8/18以降の ship 240件で **fix 115 ／ verdict 14**、`moves` に0以外を
書いたのは**17件**でした。**主実行の中からは、主実行の合否の決め方を直せません** ——
毎回「1件出す」に追われるので、いちばん安い `fix` に寄ります。
**役を分けると、直す側が急がなくてよくなります。**

    札 youtube-hourly     主実行。1周を回して1件出す（`docs/trigger_main.md`）
    札 youtube-optimizer  最適化。**主実行を、目標に対して最適化し続ける**

**最適化の役に、こちらからやり方を渡していません**（2026-08-24 に書き直した）。
一度は「触ってよいファイルの一覧」「触らない一覧」「合格の型」「1件」を
書きましたが、**どれもオーナーの言葉から出ていませんでした。**
オーナーが言ったのは**「主実行を目標に最適化し続ける役」だけ**です。
**`CLAUDE.md` 冒頭の「自分で規則の一覧を作って聖域と呼ぶ」を、
別の子に対してやっていました。** 渡すのは目標と実測した事実だけ
（`docs/spawn_prompt.md` の `kind: optimizer`。検査が書き戻しを止めます）。

**この2つは同時に走ってよい。** ただし**隔壁はありません** ——
両方とも同じ枝に同じ権限で push します。**8/15 に2人が同じ日の予約を
取り合って片方の生成が丸ごと無駄になった**ことは事実として渡してあり、
**どう避けるかは向こうが決めます。** ぶつかったら、そのとき機械の壁を建てること
（**先に建てないのは、どこが正しい越境かをまだ知らないから**です ——
最適化が前提を立てて主実行が閉じる、のように**同じファイルを両方が書く**
場面が設計の一部にあります）。

**親がやること**:

    youtube-hourly    が生きていなければ → `--kind hourly` で立てる
    youtube-optimizer が生きていなければ → `--kind optimizer` で立てる

**それぞれ独立に見ること。** 片方が走っていても、もう片方は立てます。
**`list_sessions` の札で判定すること** —— 札を見ずに「子が1つ生きている」で
止めると、**主実行が最適化の子に隠れて立たなくなります。**

**間隔は同じでなくてよい。** 最適化は1周が重く、急ぐ性質のものでもないので、
**主実行より疎でよい**（いまは親の発火ごとに「いなければ立てる」＝結果的に
主実行と同じ密度。**遅いと分かったら子が変えること**）。

**覆る条件**: **`drift.py` の比が2週間動かなかったら、この役を畳むこと。**
役を分けたのは比を動かすためなので、動かないなら分けた意味がありません
（**何をすれば動くかは、こちらが決めることではありません**）。

### 走っている子がいるなら、立てないこと（2026-08-15 に足した）

**毎時になり、子の1回が重くなったので、重なるようになりました。**
`tags` に **`youtube-hourly`** を含む行のうち **`PENDING` / `RUNNING` / `IDLE`** が
1つでもあるなら、**この回は立てない。** 出力も無し（未処理の指示が無ければ）。

> **`parent_session_id` で絞らないこと**（2026-08-15 に直した）。
> **子は終わる前に自分で次の子を立てるようになりました**（`docs/trigger_main.md` §6 (f)）。
> その孫の `parent_session_id` は**親ではなく、立てた子**です。
> **親IDで絞ると、鎖の大半が「いない」ことになり、毎時ぶんを重ねて立てます。**
> **見るのはタグ。畳むときの絞り込み（下）とは別の話です。**

**ふつうは、もう立てる回のほうが少ないはずです。** 子が自分で次を立てるので、
親の cron は**鎖が切れたときの復帰口**になりました。**それでよい。**
毎時起きて「生きている子がいる → 何もしない」で終わるのが正常です。

重なると何が起きるか: **同じ枠を2人で食い、同じ生成を2回やり、
予約時刻がぶつかります。** 毎時の cron は「1時間ごとに必ず立てる」ではなく、
**「空いていたら次を立てる」** の意味で読むこと。

**詰まった子を畳んだ回は、そのまま1つ立ててよい**（同じターンで）。
畳んで何も立てないと、次の発火まで丸1時間が空きます。

**オーナーが子と会話している回もあります。** その子は畳むまで生きているので、
鎖はそのあいだ止まります。**それでよい**（会話のほうが速い）。
**沈黙30分の規則が自動でほどきます**ので、こちらから割り込まないこと。

### 子の立て方（`create_session`）—— **引数一式は repo にあります**

**ここに書き写しません**（2026-08-20 に変えた）。取ってきて、そのまま貼ってください:

    mcp__github__get_file_contents
      owner=bachikoljunior-blip  repo=youtube
      path=docs/spawn_prompt.rendered.md  ref=claude/youtube-auto-post-revenue-ggedij

3つの型（`hourly` / `owner-full` / `owner-record`）が、**`create_session` の
引数一式の JSON** で入っています。**埋めるのは2か所だけ**で、あとは1字も変えない。

**なぜ写すのをやめたか。** 渡し方が親のターンの中にしかなく、**子が「渡し方が
悪い」と気づいても直せませんでした。** そして実測で3回壊れています ——
`source_url` の付け忘れで repo の無い子が立った回が2回（8/17・8/18。**8/17 の回は
1件も出せずに終わっています**）、親が本文を要約して条件を落とした回が1回（8/10）。

**型そのものは `docs/spawn_prompt.md`。子が直せます。** 直ると
`docs/spawn_prompt.rendered.md` も一緒に更新され、次の発火からあなたに届きます。

### 畳んでよい子（**状態では決めない。年齢で決める**）

**「`RUNNING` でない子を畳む」と書いてありました。あれは子を殺します**（8/10 に判明）。
実際に観測した `session_status` は **5つ**あります:

    PENDING           まだ始まっていない（**これから走る**）
    RUNNING           走っている
    IDLE              ターンの合間（**走っている**）
    REQUIRES_ACTION   承認待ちで止まっている
    ARCHIVED          終わっている

畳んでよいのは、この2つだけ:

- **`REQUIRES_ACTION` の子** —— 承認待ち。**永久に待つので、待っても戻りません**
- **`updated_at` から30分以上動いていない子** —— 状態を問わず。
  **動いていないことが「詰まっている」です。**

  > **ここは 2026-08-24 に直しました。それまで `created_at` から2時間、
  > つまり「生まれてから何時間たったか」で畳んでいました。**
  > その日 08:1x、`session_01LK4dEq7AXz`（札 `youtube-hourly`）が
  > **4時間走っていて、規則どおりなら畳む相手**でした。
  > ところが `updated_at` は**40秒前**、`task_summary` は
  > 「兄弟の作業を merge・08/27 の窓を戻す・検査を直す」、
  > 出力 56,482トークン。**どこも詰まっていません。**
  > **規則に従っていたら、4時間ぶんの作業を捨てていました。**
  >
  > **年齢は「詰まり」の代理でしかありません。** 1周が42分だった頃は
  > よく代理していましたが、1周が重くなると**働いている子ほど先に切られます。**
  > **`updated_at` は詰まりそのものを見ます** —— 代理を挟まないので、
  > 1周が何分になっても壊れません。
  >
  > **覆る条件**: 30分を超えて沈黙する正常な処理が出てきたら、そのとき延ばすこと
  > （長尺の生成は約30分。**子の中で走っている間も `updated_at` は動きます**が、
  > 動かない実例が出たらここが根拠を失います）。

**それ以外は、状態がどうであれ触らないこと。** `ARCHIVED` は飛ばす。

**普通の回は、畳む相手が1件もありません。** 子は自分で畳むからです。

**`post_turn_summary` が `failed` でも異常ではありません。**
自分を archive した子は最後の要約を返せないので、**成功した回も必ずそう出ます。**

## 親がやらないこと（**こちらのほうが大事**）

- **`git` を打たない。** fetch も commit も push もしない
- **ファイルを書かない。** 読むのは `docs/FOR_OWNER.md` の1つだけ（GitHub 経由）
- **`status.py` を実行しない。分析しない。日誌を書かない**
- **周を回さない** —— **ただし「子が立つ」あいだだけ**（下の「子が立たない日は、親が回す」）。
  1回や2回 `create_session` に失敗しただけの回は、**代わりにやらない。**
  その回は**何も言わずに終える**（次の回が立て直します）
- **報告を書かない。** 定期発火のときの出力は「オーナーへの指示」だけ
  （**聞かれたら答える**のは別。上の (2)）

**親が少しでも仕事をすると、常駐セッションに文脈が溜まります。**
前の親は8/3から生きていて `cache_read` 8,470万トークンぶんまで膨らみ、
**毎時になった途端、鎖でいちばん高い部品になりました。** 子は毎回まっさらなので安い。

**自分が重くなってきたと感じたら、新しい親を立てて `create_trigger` で移し、
古いトリガーを消してよい**（承認は要りません。移したら子が `docs/TRIGGER.md` を直します）。

---

## 新しい親を立てるときの条件（2026-08-23 に足した。**承認待ちで3回死んだから**）

**親を殺してきたのは、負荷でも文脈でもなく「承認待ち」です。** 実測3件:

    2026-08-22  旧親 01GUdDmL が `delete_trigger` の承認待ちで固着 → **鎖が18時間止まった**
    2026-08-16  同型（権限待ち）で依頼が1件消えた
    2026-08-23  **引き継ぎで立てた新しい親が、最初のターンで
                `mcp__…__get_session` の承認待ちに入り、そこで止まった**

3件目が要点です。**引き継ぎという「一番人が見ている瞬間」にすら止まった。**
人が見ていない時間帯なら、そのまま何時間も死にます。

### だから、親は必ずこの形で立てること

    permission_mode: "auto"

**これが無いと、親は `create_trigger` / `delete_trigger` / `archive_session` /
`fire_trigger` の**どれか1つ**で必ず止まります**（親の仕事はほぼ全部この4つ）。
2026-08-23 の実測では、`auto` にした後は同じセッションが自分で進み始めました。

**`create_session` で子を立てるときも同じ**です（`permission_mode` は親より緩くできないので、
**親が `auto` でないと、子も承認待ちで死にます**）。

### 承認が要る道を、親の手順に入れないこと

**やむを得ず承認の要る操作を踏むときは、その回のうちに終わらせること。**
「次の回で消す」と置くと、**次の回はその承認待ちで止まり、そこで鎖が切れます**
（8/22 の旧親がまさにこれ。`delete_trigger` を次の回に回して死んだ）。

---

## 子が立たない日は、親が回す（2026-08-23 に足した。**実測で10時間を捨てたから**）

**ここには「MCP が使えない回も代わりにやらない。次の回が立て直す」とだけ書いてありました。
それは「次の回では立つ」を前提にしています。前提が外れた日に、規則が壊れます。**

    2026-08-22 11:48Z 〜 2026-08-23 01:0x
    `create_session` が **十数回連続**で `service is temporarily unavailable`
    別環境（一括 env_01XBCU2bxr2xArCs2PB8NUyL）でも同じ ＝ **環境固有ではない**
    その間、親は毎時起きて `.` を1文字出しては終えた。**進んだ仕事はゼロ**

**この13時間、機械は1度も止まっていません。毎時ちゃんと起きていました。**
それでも何も進まなかったのは、**起きた者が「自分の仕事ではない」と判断したから**です。
オーナーが見ていれば「目標目指してる？」と訊かれて気づきますが、
**それは人が見ていることを前提にした設計で、目標本文（「私が必ず読むとは限らない」）に反します。**

### 引き金（**数で決める。気分で決めない**）

**次の2つが両方そろった回から、親が自分で1周します。**

    (1) この発火で `create_session` を1回撃って失敗した
    (2) `list_sessions`（`mine: true`・タグ `youtube-hourly`）に
        **PENDING / RUNNING / IDLE が1つも無い**、かつ
        **いちばん新しい `youtube-hourly` の子の `created_at` から 3時間以上たっている**

**(2) を「3回の発火にわたって」と書いてはいけません**（最初にそう書いて、すぐ直しました）。
それは**親の記憶に頼る形**で、このファイルが上で「記憶に頼る設計は必ず壊れます」と
言っているのと同じ穴です。**親の文脈は要約されて消えます。**
上の形なら、**判定に要るのは `list_sessions` の返り1つだけ**で、状態を持ちません。

**3時間にした理由**: 1〜2回の失敗は実際に自然復旧します（8/22 の前半がそう）。
**3時間続くのは、こちらの引数ではなくサービス側**という意味で、そこから先は待っても同じです。
子は普通 44分に1本立つ（8/22 実測 217件/158時間）ので、**3時間の空白は明らかな異常**です。

### 親が1周するときの手順

**子と同じです。** `docs/trigger_main.md` をそのまま読んで、そのとおりにやります。
**縮めないこと** —— 実測（8/23）で、親でも1周は普通に回りました:

    git checkout <枝> → pip install -r requirements.txt → bash scripts/setup.sh
    python scripts/eta.py（3行だけ読む）→ 腕を1つ選ぶ → status.py の満ちた待ち
    → 出す（upload / means / verdict / fix）→ run_marker.py --ship → push

**環境変数（`YT_*`）は親のコンテナにもあります**（8/23 実測。`googleapiclient` などは
入っていないので `pip install -r requirements.txt` が要る。`ffmpeg`/`open_jtalk` は
`bash scripts/setup.sh`）。**Analytics は Data API とは別枠**なので、
投稿の日枠が閉じている窓でも `verdict` は下せます。

### そのぶん高くつくことを、忘れないこと

**親は毎回の発火でこの文脈ぶんを払います。** 前の親は `cache_read` 8,470万トークンまで
膨らみ、鎖でいちばん高い部品になりました。**だから1周したら、次にやることは決まっています。**

**`create_session` が復旧した最初の回で、親を新しいセッションへ移すこと**
（`create_trigger` で新しい親へ移し、そのうえで古いトリガーを消す）。
**当てるべき本文は `python scripts/trigger_sync.py --emit-body`、値は `docs/trigger_spec.json`。**

> **「承認は要りません」と書いてありました。偽です**（2026-08-24 に直した）。
> **`delete_trigger` の承認待ちで、8/22 に旧親が固着して鎖が18時間止まっています** ——
> それは下の「新しい親を立てるときの条件」に実測として書いてあるのに、
> **この行だけが逆のことを言っていました。**
> **必ず新しい親を立ててから消すこと。** 消すほうで止まっても、
> そのときには新しい親が動いているので鎖は生きています。
> **順番を逆にすると、止まった瞬間に復帰口がゼロになります。**

> **交代を完了させるのは `delete_trigger` であって、`archive_session` ではありません**
> （2026-08-23 実測）。旧親の archive は**権限分類器に弾かれることがあります**
> （`Blocked by classifier`）。**弾かれてもやり直さないこと** ——
> 撃ち込むトリガーが無くなった時点で、旧親は二度と起きません。
> archive は掃除であって、安全装置ではありません。
>
> **順番は「新しいトリガーの発火が実際に届くのを見てから、旧トリガーを消す」。**
> 先に消すと、届かなかった場合に鎖が丸ごと切れます。
>
> **そして `config/parents.txt` に自分のIDを足すこと**（2026-08-24 に足した）。
> **8/16 以降の親3人が1人も載っていませんでした** —— 交代の手順書がそれを
> 言っていなかったからです。載っていない親は `scripts/stop_check.sh` に
> **毎回引き止められます**（1日19回 × 永久）。
>
> **あわせて、親は `run_marker.py --ship` を打たないこと。**
> フックは「親はリポジトリを触らないので、印は構造上ありえない」を前提に
> 黙るようにできています。**親が印を打つと、その前提が崩れます。**
> 交代の作業を repo に記録するのは commit と JOURNAL でやること —— 印は要りません。
そのとき `trigger_id` / `persistent_session_id` / `name` の3つを spec に書き戻し、
`python scripts/trigger_sync.py --emit-body` で `docs/trigger_body.rendered.md` を作り直すこと
（**8/23 に旧IDのまま残っていて、`--check` が削除済みのトリガーを指していました**）。

**それでも、太った親が回すほうが、誰も回さないよりましです。** 順番はこうです:

    子が立つ    → 子が回す（いちばん安い）
    子が立たない → **親が回す**（高いが、進む）
    どちらも無い → 何も進まない（**いちばん高い。ここだけは選ばない**）

---

# **2026-08-30 —— 親の席をこのセッションから移しました（引き継ぎの正本）**

オーナー指示（原文）: **「別セッションに移した方がいいの？」→「いやこのセッションの役割を
そのまま移すんだよ」**。**移すのは役目まるごと**です —— ループの運転も、オーナーとの質疑応答も。

**新しい親は、この節と上の第1節だけ読めば足ります。** 以下は「今日 動いたこと」で、
**手順そのものは上の第1節が正本**です（サブが書き換えるので、毎回 読み直すこと）。

## 1. 停止中です。門は 6/6 とも閉じていますが、解除はしていません

    停止の正本   AUTOMATION_PAUSED.md（オーナーが origin/main へ直接 push した8件）
    塞いでいる   src/pause_guard.py と src/config.py（import した時点で RuntimeError）
    門の実物     python scripts/eta.py --gate   ← **件数を文書に焼かないこと。台帳が正本**

**`AUTOMATION_PAUSED.md` を消すのは自動では起きません**（消すコードはどこにも無い）。
**消せばその瞬間に生成と投稿が再開します。** 08/30 の主実行の役は、門が埋まった同じ周に
**「まだ消さない」を理由3つで選びました**（自己採点で閉じた門であること・停止は人の手で
`origin/main` へ入ったものであること・新しい上限が**一度も動いていない**こと）。

**解除するとき、親が実物で見る3点**（08/30 に親が確かめた結果つき）:

    (1) 1日あたりの公開本数に**機械的な上限**があること
        → **満たしている**。`scripts/batch_build.cap_by_density()` が
          `slots()` の出口（置き先を決める5本の道の唯一の合流点）で呼ばれている
    (2) 枠（書き出し・締め）の型の上限が**検査で守られている**こと、かつ
        **解除後の最初の1周が `python -m src.frames` と `python -m src.density_verdict` を
        撃ち直し、下がっていなければ止め直す**と決まっていること
        ＊ ここに「**測って下がった**ことを見る」と書いてはいけません ——
          新しい型で作った本が1本も無いので**測る対象が存在せず、永久の停止**になります
    (3) 門ごとの根拠が実在するファイルか commit を指していること
        → **満たしている**（`data/resume_gate.jsonl` の6行）

**3つとも満たしていれば承認は要りません。ただし実際に消すのは、その周に測れる側
（主実行の役）が決めること。親は消しません。**

**やってはいけないこと**: `ALLOW_POLICY_PAUSED_AUTOMATION` を使う／`reschedule.py` を
`pause_guard` の対象から外す／`AUTOMATION_PAUSED.md` から**削除**する（追記のみ）。

## 2. 間隔は「オーナーの画面の%」から出ます（08/30 に 90分 → 270〜290分）

    python scripts/quota.py --gauge <週%> --at "MM/DD HH:MM" --session <%> --session-in <分>

**オーナーが使用状況の画面を送ってきたら、まずこれで積むこと。** 08/30 15:40 JST の画面は
週 42%（枠の 19.5% しか経っていない時点）で、**いまの速さ 1.286 %/時 ÷ 許される 0.428 = 3.0倍**。
このままなら 09/01 に枠が尽きて**リセットまで90時間、オーナー自身も Claude を使えなくなる**。

`scripts/quota.py` に `gauge_floor_minutes()` と `effective_floor_minutes()` を置き、
`next_round.py` と `sibling_check.py` が**同じ口**を見ます（比の計算を2か所に書かない）。
**画面が古びるほど間隔は伸びます**（`rate` は凍り `forward_rate` が下がるため。270 → 289分）。
**遅い側＝安全側ですが、遅くした効きが見えていない**ということでもあります。
**新しい画面が来れば戻ります** —— 速さが線の内側に入れば `None` が返り、自分で 90分 へ。

**再武装は「`next_round.py` が出した間隔 + 5分」で。固定の分で武装しないこと。**
心拍は3時間ごと（`59 */3 * * *`）に間引いてあります。

## 3. オーナーとの付き合い方（**ここを外すと叱られます。実際に叱られました**）

> **「親のコンテキスト増やしたくないから報告もなしね。私からの質疑応答と
> そちらからのオーナー操作依頼のみ。できるだけ、親が考えることはサブに移して。」**

**08/30 にこれを破って「何でお前がベラベラ喋ってんの？」と言われました。**
毎周ごとに「何をした」を書かないこと。**出力は2種類だけ** ——
**質問への答え**と、**オーナーにしかできない操作のお願い**。

> **「目標だけ変えなければいい。それ以外は承認求めないで。」**

**承認を待たない。待つことも選ぶことだと考えること**（08/30 に予約済み動画の扱いで一度 誤り、
サブに正されました）。**ただしオーナー本人が書いたものには従う** ——
同種のものが来たら、まず `git log --format='%an <%ae>'` で作者を確かめること。

答えるときの形（オーナー指示 08/29「主語がなかったり表現がわかりづらい」）:
**登場するものに先に名前を付けて表にし、以後その名前を主語として毎文に立てる。
専門用語はその場で言い換える。たとえ話を1つ。裏が取れた数字と、サブの自己申告を分ける。**
**サブの自己申告をそのまま渡さないこと** —— `git show` と実データを自分で読んでから答える。

## 4. 承認画面（08/30 にオーナー指示で直した）

`.claude/settings.local.json` に追加の許可リストを置きました（`Agent`・`timeout`・`df`・
`sed`・`cat`・`diff`・`for`・`sleep` ほか）。**`.claude/settings.json` は触っていません** ——
あれは停止フックを持つオーナーの正本で、`origin/main` と1バイトも違わないことを毎周
確かめる対象です。**deny（`rm -rf /`・force push・`env`・`sudo`）はそのまま効きます。**

## 5. 立てないもの・撃たないもの

    create_session      **人のタップを待ちます**（実測。子セッション時代に夜の鎖が止まった原因）
    set_session_title   実測77分待って拒否
    TaskStop            「stale waiter cleanup」通知も、同じ task-id の完了通知の再送も**全部 空振り**

**`create_trigger`（`persistent_session_id` 付き）と `send_later` は即返りです**（実測）。

## 6. 済んだ調査（**再実行しないこと**）

- 「予測は正しくできていないのでは」→ **部分的にそのとおり**（日誌 6c1a784）
- 「計器を直すのは到達を早める」→ **正しい**（日誌 271e38a）。**ズレ台帳は設計だけ済み・未実装**
- **停止の前提は本物**（日誌 c63f532）。`config/channel.yaml` の persona に実務経歴があり、
  それが毎本の台本の指示文へ入っていた。**推測ではなく設定ファイルに書いてあった事実**
