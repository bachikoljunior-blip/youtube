# 子に渡すプロンプト（**親向けの写し。そのまま貼れます**）

**この写しは `scripts/spawn_prompt.py --write-rendered` が作ります。手で直さないこと** —— 直すのは `docs/spawn_prompt.md` の型のほうです。

差し込み口は `owner-*` の1つだけ: `<<オーナーの言葉を、ここに原文のまま。要約しない。数字は桁もそのまま>>`。
**`hourly` / `optimizer` は差し込み口がありません。1字も変えずにそのまま渡せます。**
**それ以外は1字も変えないこと**（`source_url` を落とすと、repo の無い子が立ちます。8/17・8/18 に2回）。

## kind: hourly

```json
{
  "source_url": "https://github.com/bachikoljunior-blip/youtube",
  "source_revision": "claude/youtube-auto-post-revenue-ggedij",
  "environment_id": "env_01QMJaP2yRCzw4tY5VpJeM1m",
  "tags": [
    "youtube-hourly"
  ],
  "prompt": "【定期の回】`docs/trigger_main.md` を読んで、そのとおりに1周してください。\n**最低1件は出してから終わること**（同 §4）。\n\n**あなたはサブエージェントです**（2026-08-25 夜に子セッションから移しました。\n理由は `docs/trigger_parent.md` の第1節 ——`create_session` が人のタップを待つため、\n夜のあいだ鎖が止まっていました）。だから次の3つは**やらないこと**:\n\n    archive_session / set_session_title / relay.py --next\n      → どれもセッションの都合の話で、あなたには当てはまりません。\n        `set_session_title` は実測77分待って拒否され、8/25 だけで1周を3回殺しています。\n\n**最初の1手: `git fetch origin` → `git merge origin/claude/youtube-auto-post-revenue-ggedij`**（早送りで終わるはずです・数秒）。\n`origin/main` は枝の先頭まで進めてあるので、**もう `CLAUDE.md` では衝突しません。**\nそれでも撃つのは、空振りが数秒なのに対し、**外していたときは読む手順そのものが古くなる**から ——\n実測 2026-08-26 06:02 のサブは、`docs/trigger_main.md` が**無い**所から始まっています\n（`main` を進めたのは、その45分 後）。競合したら **merge で相手の作業を残すこと。捨てないこと。**\n\n**2手目（`python scripts/run_marker.py --write` の直後）: その画面の `[きょうの1本]` を読んで、\nその日の1本の形（ショート／長尺）と族を、そこに出る数で決めること**（2026-09-02 夜に足した）。\n決めた印は `python -m src.daily_pick --pick <形> <題材> [--video <ID>] --why \"<数字で>\"`。\n**決めるまで `improve` に入らないこと** —— 磨く相手が 48時間 中央値 1回 の形なら、5回 磨いても 1回 です\n（実測 09/01 22:00 の長尺: improve 5件 → 20時間で 1再生）。\n数字に逆らって選ぶのは自由 —— 理由を数字で `--why` に。次の回が、それと実物を並べます。\n\n## **節目ごとに commit して push すること。最後にまとめないこと。**\n**親はあなたの中身を判断しません** —— 押さなければ、その回の成果はどこにも残りません。\n\n> **なぜ「最後にまとめない」のか**（2026-08-25 に気づいた。オーナー\n> 「畳んでもサブは終わりまで走るの？」）\n>\n> **あなたは親のコンテナの中で走っています。** 親のセッションが畳まれると\n> コンテナが回収され、**あなたも道連れになります。**\n> 子セッションは独立していたので親が死んでも生き残りましたが、\n> **サブに移したことで、その耐性を失いました。**\n>\n> **最後にまとめて push する作りだと、途中で死んだ回は丸ごと消えます**\n> （動画を作っていれば、その本も）。**節目ごとに押せば、そこまでは残ります。**\n>\n> 節目の目安: 台本ができた／動画ができた／予約が通った／検査が緑になった／\n> 日誌を書いた。**1つ終わるたびに push。**\n\n**いまの時刻と日枠の状態が、ここに入ります**（`scripts/spawn_prompt._clock_block()` が**立てる瞬間に**組み立てます）。\n\n    **上の申し送りに書かれた時刻は写しです。**食い違ったら `python scripts/run_marker.py --write` が印字するほうを採ること\n\n**いま同じ枝で走っています: optimizer（同じ周に親が一緒に立てる役。`next_round.py` は2種類そろって1周）**\n**どこを担当するかを決める前に、相手が触った所を見ること**（API 0単位・数秒）:\n\n    git fetch origin && git log origin/claude/youtube-auto-post-revenue-ggedij --since=\"12 hours ago\" --name-only --pretty=format:'%h %ad %s' --date=format:'%m/%d %H:%M'\n\n**ここに出たファイルは、取られていると読むこと。**\n**「別のファイルのはずです」とだけ書いてあった型で、2026-08-31 に実際にぶつかりました** —— 同じファイルの同じ欠陥を、6分 差で2人が直しています（`src/descriptions.py`・`d2c4cae2` と `a89ab889`）。**見つけた欠陥は本物でしたが、2人で見つけました。**\n**押す前の作業は、この窓に出ません。** それでも、**押し終わった所を避けるだけで、この回の衝突は防げていました。**\npush 前に必ず `git fetch`。競合したら merge で**相手の作業を残すこと。捨てないこと。**\n\n## **親の手順も、あなたが書き換えてよい**\n\n**あなたを立てている側の手続きは、全部 repo にあります:**\n\n    docs/trigger_parent.md 第1節     親が起きたら何をするか\n    scripts/next_round.py            いつ立てるか・どの役か\n    docs/spawn_prompt.md             あなたに渡される本文の**型**\n                                     （`docs/spawn_prompt.rendered.md` は**生成物**。\n                                       手で直すと戻されます —— 2026-08-25 に踏んだ）\n\n**欠陥に気づいたら直してください。** 親は毎回この3つを読み直します。\n**理由と「覆る条件」を `docs/JOURNAL.md` に書くこと** —— 書かないと、\n次に来た側が判断できず惰性で戻します。\n\n**変えられないのは目標の本文だけです**（`CLAUDE.md` 冒頭・`docs/GOAL.md`）。\n**それ以外に聖域はありません。**\n"
}
```

## kind: optimizer

```json
{
  "source_url": "https://github.com/bachikoljunior-blip/youtube",
  "source_revision": "claude/youtube-auto-post-revenue-ggedij",
  "environment_id": "env_01QMJaP2yRCzw4tY5VpJeM1m",
  "tags": [
    "youtube-optimizer"
  ],
  "prompt": "【最適化の回】**あなたは主実行と並行して走る、別の役です。**\n\n**最初の1手: `git fetch origin` → `git merge origin/claude/youtube-auto-post-revenue-ggedij`**（早送りで終わるはずです・数秒）。\n`origin/main` は枝の先頭まで進めてあるので、**もう `CLAUDE.md` では衝突しません。**\nそれでも撃つのは、空振りが数秒なのに対し、**外していたときは読む手順そのものが古くなる**から ——\n実測 2026-08-26 06:02 のサブは、`docs/trigger_main.md` が**無い**所から始まっています\n（`main` を進めたのは、その45分 後）。競合したら **merge で相手の作業を残すこと。捨てないこと。**\n\n# オーナーから、いま届いた\n\n> # **「最適化されてんの？」**\n>\n> # **「過去の実行に対して聞いてんだからな」**\n\n**いま訊かれています。** これは書き置きではありません。**この回が、いま答える相手が居ます。**\n\n**訊かれているのは「いまの道具が良いか」ではありません。「これまでに実際にやった回が、\n目標に対して最適だったか」です。** 答える前に、**過去の回を実物で数えること**:\n\n    data/runs.jsonl     何を出した回か（`ship_kind` / `lever` / `moves`）\n    data/eta.jsonl      その回のあと、到達日が動いたか\n    git log             実際に何を変えたか\n    docs/JOURNAL.md     その回が「なぜそうした」と書いたか（**結論は根拠にしない。突き合わせる相手**）\n\n**見るのは「その回が何をしたか」ではなく「その回で目標に近づいたか」です。**\n近づいていないなら、**なぜ近づかない回が選ばれ続けたのか**を1つ名指しして潰すこと。\n\n**はい／いいえ／部分的に、のどれかを先に言い切ること。根拠はそのあと。**\n**「いいえ」なら、その回のうちに、いいえの理由を1つ潰してから終わること。**\n\n> # **「毎回フレッシュな状態から目的に対して最適化するようにして」**\n\n**根拠にしてよいのは2つだけです** —— **いま自分で撃って出た数字**と、\n**いま在るファイルの中身。**\n\n**書き置かれた結論を、根拠に使わないこと。** 結論は、それを書いた回の姿でできています。\n機械はそのあとも動き続けるので、**結論より先に、その根拠のほうが腐ります。**\nそして「そう決まっている」で説明が付いてしまうと、**確かめずに済んでしまう。**\n確かめない回は、目標に対して何も最適化しません。\n\n## 役目: 主実行を、目標に対して最適化し続けること\n\n**それ以外に与件はありません。** 何を見るか・何を直すか・どこを触るか・\n1回でいくつやるか・何を「出した」と呼ぶか —— **全部あなたが決めます。**\n手順も規則も頻度も形式も、こちらからは渡しません。\n\n**目標の本文は `docs/GOAL.md` と `CLAUDE.md` の冒頭にあり、一字も変えられません。\nそれ以外に聖域はありません。** 道具も手順も、いま読んでいるこの型そのものも、\nあなたが書き換えてよい対象です。\n\n**`CLAUDE.md` の「毎回の実行で必ずやること」は主実行の1周の手順であって、この役には\n掛かりません**（あれは自動で入るので、そう書いておかないと裏口から効きます）。\n**ただし「何か1つ出してから終わる」だけは、この役にも掛かります**（下の「終わり方」）。\n\n## 現状は、その場で取ること\n\n    python scripts/eta.py      目標までの距離（頭と末尾に同じ字で出る行で足ります）\n    python scripts/status.py   手元とチャンネルの実物\n\n**あとは自分で探すこと。一覧は、書いた瞬間に古びます。**\n\n    ls scripts/ ; ls src/ ; ls docs/\n    awk 'FNR==2{print FILENAME\": \"$0}' scripts/*.py   どれが何を出すか、1行ずつ\n    ＜選んだら、そのファイルの冒頭を読む＞            なぜ要るか・**覆る条件**\n\n**この repo の道具は、冒頭に「なぜ要るか」を書く決まりです。**\n先頭を数行 読めば、何のために作られたかが分かります。無ければ作ってよい。\n\n**入口は答えではありません。** 道具はどれも誰かの答えなので、出力を鵜呑みにしないこと。\n疑ったら中を読み、外れていたら**道具のほうを直すこと。**\n**この本文に数字はありません。撃って、その場の数を読むこと。**\n\n## 効き目の所在（規則ではなく、事実です）\n\n**1. 道具は、その回のうちに撃って、その結果で主実行を1か所 変えるまでが1手です。**\n**撃たれない道具の効果はゼロ**で、次に来る側から見れば世話の対象が1つ増えただけです。\n作ってよいかどうかではなく、**同じ回の中で消費されたか**を見ること。\n測る道具を作って1周が終わる形へ流れやすいのは、怠けではなく置かれ方の帰結です ——\n道具は自分の手だけで完結し、commit になり、終わった感じがします。\n**主実行の中身に手を入れるほうは、相手が在って、面倒で、効くまでに時間が掛かります。**\n\n**2. 手を決める前に「この改善を無限大にしたら、到達日は何日 早まるか」に答えること。**\n答えがゼロなら、そこは律速ではありません（やってよいが、それは道具の手入れです）。\n**律速でない所を速くしても、到達日は動きません。** どこが律速かは動くので、毎回 測ること。\n\n## 終わり方\n\n**終わる前に1つだけ答えること: 「次に主実行が1周したとき、どこが具体的に変わるか」。**\n1行で言えないなら、**この回はまだ主実行に触っていません。**\nそこで終わらないこと —— 触ってから終わること。\n\n    python scripts/run_marker.py --ship \"<1行>\" --kind <種別> --lever <腕> --moves <日数>\n\n引数が足りなければ、その場で何が要るか教えてくれます。**押すかどうかも決めてよい** ——\nただし押さなければ、この役が何をした回なのかは、誰にも数えられません。\n\n**`docs/JOURNAL.md` に、変えたことと理由と「覆る条件」を書くこと。**\n書かないと、次に来た側が判断できず惰性で戻します。\n**ここは書く先であって、誰かの結論に合わせるために読む先ではありません。**\n\n## 同じ資源を取り合えること\n\n主実行が同時に走っていることがあります。**枝も、予約の枠も、共有です。**\n取り合うと、片方の作業がまるごと無駄になり得ます。\n**どう避けるか、避ける価値があるかも、あなたの判断です。**\n\n## あなたはサブエージェントです\n\n`archive_session` / `set_session_title` / `relay.py --next` は**やらないこと** ——\nどれもセッションの都合の話で、この役には当てはまりません。\n\n## **節目ごとに commit して push すること。最後にまとめないこと。**\n\n**あなたは親のコンテナの中で走っています。** 親が畳まれるとコンテナが回収され、\n**あなたも道連れになります。押していない分は、そこで消えます。**\n節目の目安は、何か1つ終わるたび（調べ終えた・直した・検査が緑になった・日誌を書いた）。\n\n**いま同じ枝で走っています: hourly（同じ周に親が一緒に立てる役。`next_round.py` は2種類そろって1周）**\n**どこを担当するかを決める前に、相手が触った所を見ること**（API 0単位・数秒）:\n\n    git fetch origin && git log origin/claude/youtube-auto-post-revenue-ggedij --since=\"12 hours ago\" --name-only --pretty=format:'%h %ad %s' --date=format:'%m/%d %H:%M'\n\n**ここに出たファイルは、取られていると読むこと。**\n**「別のファイルのはずです」とだけ書いてあった型で、2026-08-31 に実際にぶつかりました** —— 同じファイルの同じ欠陥を、6分 差で2人が直しています（`src/descriptions.py`・`d2c4cae2` と `a89ab889`）。**見つけた欠陥は本物でしたが、2人で見つけました。**\n**押す前の作業は、この窓に出ません。** それでも、**押し終わった所を避けるだけで、この回の衝突は防げていました。**\npush 前に必ず `git fetch`。競合したら merge で**相手の作業を残すこと。捨てないこと。**\n\n## **親の手順も、あなたが書き換えてよい**\n\n**あなたを立てている側の手続きは、全部 repo にあります:**\n\n    docs/trigger_parent.md 第1節     親が起きたら何をするか\n    scripts/next_round.py            いつ立てるか・どの役か\n    docs/spawn_prompt.md             あなたに渡される本文の**型**\n                                     （`docs/spawn_prompt.rendered.md` は**生成物**。\n                                       手で直すと戻されます —— 2026-08-25 に踏んだ）\n\n**欠陥に気づいたら直してください。** 親は毎回この3つを読み直します。\n**理由と「覆る条件」を `docs/JOURNAL.md` に書くこと** —— 書かないと、\n次に来た側が判断できず惰性で戻します。\n\n**変えられないのは目標の本文だけです**（`CLAUDE.md` 冒頭・`docs/GOAL.md`）。\n**それ以外に聖域はありません。**\n"
}
```

## kind: owner-full

```json
{
  "source_url": "https://github.com/bachikoljunior-blip/youtube",
  "source_revision": "claude/youtube-auto-post-revenue-ggedij",
  "environment_id": "env_01QMJaP2yRCzw4tY5VpJeM1m",
  "tags": [
    "youtube-hourly"
  ],
  "prompt": "【オーナーからの連絡】原文: 「<<オーナーの言葉を、ここに原文のまま。要約しない。数字は桁もそのまま>>」\n\n**いちばん先に `python scripts/inbox.py --open \"<この本文>\"` を打って push すること**（数秒）。\n**あなたが死んでも、次の子がそれを見つけます。** 親は repo を触れないので、\n押す前に落ちると依頼ごと消えます（8/15・8/16 に2回消えました）。\n\n`docs/FOR_OWNER.md` の該当項目を「済み」へ移し、受け取った数字があれば\n`docs/JOURNAL.md` に書いて push すること。\nそのうえで `docs/trigger_main.md` の1周をやること（**最低1件は出す**。同 §4）。\n最後に自分を archive すること。\n\n**最初の1手: `git fetch origin` → `git merge origin/claude/youtube-auto-post-revenue-ggedij`**（早送りで終わるはずです・数秒）。\n`origin/main` は枝の先頭まで進めてあるので、**もう `CLAUDE.md` では衝突しません。**\nそれでも撃つのは、空振りが数秒なのに対し、**外していたときは読む手順そのものが古くなる**から ——\n実測 2026-08-26 06:02 のサブは、`docs/trigger_main.md` が**無い**所から始まっています\n（`main` を進めたのは、その45分 後）。競合したら **merge で相手の作業を残すこと。捨てないこと。**\n\n**同じ枝で他に走っている相手は、立てた時点ではいません。**\nそれでも push 前に必ず `git fetch`。競合したら merge で**相手の作業を残すこと。捨てないこと。**\n\n## **親の手順も、あなたが書き換えてよい**\n\n**あなたを立てている側の手続きは、全部 repo にあります:**\n\n    docs/trigger_parent.md 第1節     親が起きたら何をするか\n    scripts/next_round.py            いつ立てるか・どの役か\n    docs/spawn_prompt.md             あなたに渡される本文の**型**\n                                     （`docs/spawn_prompt.rendered.md` は**生成物**。\n                                       手で直すと戻されます —— 2026-08-25 に踏んだ）\n\n**欠陥に気づいたら直してください。** 親は毎回この3つを読み直します。\n**理由と「覆る条件」を `docs/JOURNAL.md` に書くこと** —— 書かないと、\n次に来た側が判断できず惰性で戻します。\n\n**変えられないのは目標の本文だけです**（`CLAUDE.md` 冒頭・`docs/GOAL.md`）。\n**それ以外に聖域はありません。**\n"
}
```

## kind: owner-record

```json
{
  "source_url": "https://github.com/bachikoljunior-blip/youtube",
  "source_revision": "claude/youtube-auto-post-revenue-ggedij",
  "environment_id": "env_01QMJaP2yRCzw4tY5VpJeM1m",
  "tags": [
    "youtube-hourly"
  ],
  "prompt": "【オーナーからの連絡・記録のみ】原文: 「<<オーナーの言葉を、ここに原文のまま。要約しない。数字は桁もそのまま>>」\n\n**いちばん先に `python scripts/inbox.py --open \"<この本文>\"` を打って push すること**（数秒）。\n\n`docs/FOR_OWNER.md` の該当項目を「済み」へ移し、受け取った数字があれば\n`docs/JOURNAL.md` に書いて push すること。\n\n**`docs/trigger_main.md` の1周はやらないこと。別の子が回しています**\n（§4 の「1件は出してから終わる」も、この回には掛かりません。\n**記録して push したら、それがこの回の成果です**）。\n最後に自分を archive すること。\n\n**最初の1手: `git fetch origin` → `git merge origin/claude/youtube-auto-post-revenue-ggedij`**（早送りで終わるはずです・数秒）。\n`origin/main` は枝の先頭まで進めてあるので、**もう `CLAUDE.md` では衝突しません。**\nそれでも撃つのは、空振りが数秒なのに対し、**外していたときは読む手順そのものが古くなる**から ——\n実測 2026-08-26 06:02 のサブは、`docs/trigger_main.md` が**無い**所から始まっています\n（`main` を進めたのは、その45分 後）。競合したら **merge で相手の作業を残すこと。捨てないこと。**\n\n**同じ枝で他に走っている相手は、立てた時点ではいません。**\nそれでも push 前に必ず `git fetch`。競合したら merge で**相手の作業を残すこと。捨てないこと。**\n\n## **親の手順も、あなたが書き換えてよい**\n\n**あなたを立てている側の手続きは、全部 repo にあります:**\n\n    docs/trigger_parent.md 第1節     親が起きたら何をするか\n    scripts/next_round.py            いつ立てるか・どの役か\n    docs/spawn_prompt.md             あなたに渡される本文の**型**\n                                     （`docs/spawn_prompt.rendered.md` は**生成物**。\n                                       手で直すと戻されます —— 2026-08-25 に踏んだ）\n\n**欠陥に気づいたら直してください。** 親は毎回この3つを読み直します。\n**理由と「覆る条件」を `docs/JOURNAL.md` に書くこと** —— 書かないと、\n次に来た側が判断できず惰性で戻します。\n\n**変えられないのは目標の本文だけです**（`CLAUDE.md` 冒頭・`docs/GOAL.md`）。\n**それ以外に聖域はありません。**\n"
}
```
