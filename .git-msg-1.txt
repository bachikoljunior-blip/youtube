枠の門を「立っている決め」にも通す＋化石の床をいまの中央値へ（fix/improve 191件 の行き先を変えた）

「最適化されてんの？」→ **いいえ**。直近5日 237 ship・fix+improve 191件（80.6%）・
測った動き 0件、同じ窓の 再生/日(7d) 6,299（08-25）→ 943（09-04）＝ -85%。

なぜ近づかない回が選ばれ続けたか（名指し・実測）:
1. 門はぜんぶ**書き門**だった。`path_form_hold("長尺")` は止めるのに、
   すでに立っている 09-05 の決め（長尺・見込み 1.0回）は誰も通し直さない。
   `rule3_book()` がその本を規則3 の主語にし、`dry_ledger_gate()` は
   **その本を名乗った fix しか通さない** ＝ 機械が「その形では決めさせない」と
   言っている本へ、門が回を送り込んでいた。
2. `slot_cost` の床 1,049回 は n=15・**いちばん新しい1本が 18日前**の化石で、
   ショート の実測中央値 164回 すら ✗ にした ＝ **1回 と 164回 を区別できない門**。
   区別しない門の出口は `--anyway` だけで、実物 09-05T01:48 の anyway が
   そこを通って長尺（見込み 1回）を枠に立てている。

やったこと:
- `daily_pick.standing_form_stale()`（新）: 立っている決めの形を `path_form_hold` に
  通し直す。09-05 → 止める／09-06（ショート）→ 通る と実測で確認。
- `run_marker`: `fix` と `improve` だけを止める門を追加（verdict/upload/premise/means は通す）。
  差し替えれば自分で消える。候補は実在（未投稿ショート2件・池 324本）＝ 詰まない。
- `slot_cost.verdict()`: 標本が STALE_DAYS より古いときだけ、床を**いまの中央値**
  （ショート 164回・n=216）に差し替え。床を下げる方向にしか動かない。
  結果 長尺 1回→✗（1/164）／ショート 164回→○ ＝ **初めて形を区別する床**になった。

検査: pytest -k "slot_cost or win_band or daily_pick or path_form" 89 passed。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01AHfq4FUVAd5fxDM29yG1Cm
